SUMMARY: Leo，边界与交互维度评审完毕（只读，未改任何文件）。总体结论：七项推演中五项通过——①删光全部规则后被 validateRulesClient 客户端拦截（「规则表至少一条」，且发生在确认框之前，不会发出 0 条请求）；删光组内原子序列化出 `()`、删光全部条件项序列化出 `" -> ACTION"`，两者客户端均有对应报错（括号组不可为空/至少一个条件项），后端也会各自拒绝（括号内期望原子/表达式不可为空），双层兜底成立仅文案措辞略异；③COUNT_NORMAL ⚠ 提示与后端口径完全一致——前端 refsOfRule 与后端 PolicyExpression.referencedAtoms 同为跨括号组的语法级原子集合（含取反原子、含组内原子），`!A3` 双侧同样算「含 A3」，是共享语义选择而非分歧；④moveRule 交换写法在 Vue 3 reactive 数组上响应性正确，索引赋值可追踪，上移/下移后预览 tag、radio、disabled 态全部跟随（仅 ：key 用索引是远期小患）；⑥提交体 `{rules: string[], thresholdPct}` 与 VqmsPolicyParamController 的 Map+List<String> 期望结构吻合，thresholdPct 清空成 null 时服务端优雅落默认 50，reductionHin

FINDINGS:
[MAJOR] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 203（selectPreset）
  P: τ 回填用 `|| 50` 兜底：后端契约允许 τ∈[0,100]（FreeformPolicyConfig/FreeformPolicyValidator 均按 [0,100] 校验，el-input-number :min="0" 也允许设 0），而 JS 里 0 是 falsy——持久化 τ=0 的合法配置重新打开构建器时会被显示成 50；若用户未察觉直接点「校验并应用」，实库 freeform_threshold_pct 被静默改写 0→50（成功 toast 不回显 τ），构成政策参数的静默漂移。这违反本项目「显性失败不静默降级」的既定原则。
  S: 改为显式判空：`const t = state.value.freeformThresholdPct; ffDialog.thresholdPct = (t === 0 || t) ? t : 50;` 或 `t ?? 50`。同文件 getState 里乙阈值回填无此问题（parseInt("0")=0 直接赋值）。
[MINOR] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 266-268（getState 乙阈值回填）
  P: 对 param_value 脏值健壮性不足：parseInt("abc")→NaN 会塞进 el-input-number（显示为空）；且 handleApply 的 `form.thresholdPct != null` 对 NaN 为 true，data.thresholdPct=NaN 经 JSON.stringify 变 null，服务端静默取默认 50——脏值被无声吞掉而非提示。正常写路径经 Service 层恒为整数字符串，脏值需直改 DB 才会出现，故降为 MINOR。
  S: 解析后加 `Number.isFinite(v)` 守卫：非法时置 undefined 并 msgWarning 提示「当前生效阈值非整数」，让用户知情后重选，而不是静默落默认。
[MINOR] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 62（el-dialog）与行 328-334（.rule-preview）
  P: 长内容滚动缺失：对话框无内部 body 高度上限，Element Plus 2.13 下只有整层 overlay 滚动——规则多时（16 条 × 每条约 150-250px，远超视口）头部说明与底部「校验并应用/取消」按钮都会滚出视野，需滚过全部规则卡才够到 footer。项目 ruoyi.scss 行 78 已有现成约定 `.el-dialog.scrollbar .el-dialog__body { max-height:70vh; overflow:auto }` 且 tool/gen/index.vue 在用，本页未采用。
  S: 给该 el-dialog 加 `class="scrollbar"`（一行改动即复用既有 70vh 内滚样式）；预览溢出本身已有 ellipsis 处理，但建议补 `:title="serializeRule(r)"` 或 el-tooltip——目前长表达式尾部（含 "-> 动作"段）被截断且无处悬停可看全文，只能去高级折叠面板点「从构建器生成文本」。
[MINOR] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 242-250（parseAdvancedText）对照 rulesDsl.js 行 138 与后端 FreeformPolicyConfig.MAX_RULES
  P: 16 条上限的拦截点不完全一致：addRule 有 ≥16 即时拦截，validateRulesClient 在应用前兜底（两处文案均「规则数超上限 16」，与服务端 MAX_RULES=16 及其报错文案一致，最终闸门统一在客户端、不会漏到服务端拒绝）——但「解析文本到构建器」路径粘贴 20 行会照单全收并 toast 成功，用户直到点应用才知道超限。同理高级文本里未知原子（如 XX/A5）、多个 "->" 也都放行到 apply 才由服务端报错（结果仍正确拒绝，仅报错时机与文案不同）。属设计声明「校验以服务端为准」范围内的体验债。
  S: parseAdvancedText 成功后若 lines.length>16 给即时 warning；可选：parseRuleLine 里校验 atom ∈ ATOM_OPTIONS 使构建器内联暴露非法原子。
[NIT] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 199-201（selectPreset 回填的逐行 catch→newRule()）
  P: 防御分支会静默吞错：freeformRules 中某行前端解析失败时不报错、悄悄替换成一条默认规则（A1->EXCLUDE_REPORTED）。实际上该路径接近死代码——后端 currentState 只在 loadFreeformConfig 校验通过后才下发 freeformRules（损坏态走 catch 分支不下发该键，此时走的是 `!length → [newRule()]` 整表播种，横幅已有 ⚠ 规则表损坏提示），故实际风险低。但若未来后端 canonical 形态与前端口径出现偏差，此处会把损坏证据换成看似合法的默认行。
  S: catch 分支至少 console.warn 或在规则卡上打「源行无法解析」标记，保持显性。
[NIT] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\index.vue 行 68（v-for :key="ri"）与行 223-228（moveRule）
  P: `[arr[ri], arr[target]] = [arr[target], arr[ri]]` 在 Vue 3.5 reactive Proxy 上索引赋值可触发响应性，交换逻辑与越界守卫正确；上移/下移后预览 tag、连接词 radio、处置下拉、按钮 disabled 态均随对象数据重渲染、跟着走，功能无误。但 `:key="ri"` 以索引为 key，DOM 就地复用，将来若加过渡动画或卡片内引入非受控内部状态会出隐患。
  S: newRule() 时生成自增 id 作稳定 key。
[NIT] C:\work\VQMS\RuoYi-Vue3\src\views\vqms\policyparam\rulesDsl.js 行 24 与行 137-138；index.vue 行 212
  P: ATOM_RE（行 24）声明后从未使用，死代码；16 这个魔数在前端硬编码两处（addRule、validateRulesClient），与后端 FreeformPolicyConfig.MAX_RULES 三地重复。另 handleApplyFreeform 仅弹 errors[0]，多条错误需逐轮修（validateRulesClient 已收集全量却只用第一条）。均为小问题。
  S: 删除 ATOM_RE；rulesDsl.js 导出 `export const MAX_RULES = 16` 两处引用；msgWarning 可考虑拼接前 2-3 条错误或用换行列表。
