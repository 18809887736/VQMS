<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.vqms.management.mapper.VqmsRuntimeDailyMapper">

    <!-- 幂等重算：同 stat_date 覆盖（S4 第一版按日期区间重算，§12.2） -->
    <insert id="upsert" parameterType="com.ruoyi.vqms.management.domain.VqmsRuntimeDaily">
        insert into vqms_runtime_daily
          (stat_date, in_service_min, exit_grid_min, exit_nongrid_min, offline_min,
           rated_capacity_kw, rate_pct, shortfall_pct, penalty_score)
        values
          (#{row.statDate}, #{row.inServiceMin}, #{row.exitGridMin}, #{row.exitNonGridMin},
           #{row.offlineMin}, #{row.ratedCapacityKw}, #{row.ratePct}, #{row.shortfallPct},
           #{row.penaltyScore})
        on duplicate key update
          in_service_min = values(in_service_min),
          exit_grid_min = values(exit_grid_min),
          exit_nongrid_min = values(exit_nongrid_min),
          offline_min = values(offline_min),
          rated_capacity_kw = values(rated_capacity_kw),
          rate_pct = values(rate_pct),
          shortfall_pct = values(shortfall_pct),
          penalty_score = values(penalty_score)
    </insert>
</mapper>
