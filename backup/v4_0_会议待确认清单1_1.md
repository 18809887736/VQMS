# VQMS 技术会议 —— 待确认事项清单（v1.1）

> **v1.1（2026-08-15 刷新）**：基于 v1.0（2026-08-14 会议版）更新状态，下次会议用；v1.0 保留存档不删。
> **自 v1.0 以来的变化**：新增 **#16 电压等级维度已拍板并落地**（2026-08-15，修订待办 A8，commit `22770f0`）；#13 补充 UI 就绪状态与容差种子同步连带项；#2 按值域口径刷新；同日修正：**`10.0.0.35`（鸡西 QCzt AVC 盒子）源机排除出核对范围**（Leo 定——不纳入 VQMS 外部源、完全不用考虑，外部源唯一 = 10.0.0.9）。其余待定项不变。
>
> **文档修订**动作（v4.0 已提交版怎么改）另见 [项目规划_v4_0_修订待办.md](项目规划_v4_0_修订待办.md)；本清单聚焦**会上要拍什么板 / 派什么活**，交叉项已互相标注。
>
> 决策性质标签：【口径】会上能当场定 ｜ 【架构】需讨论共识 ｜ 【资源】要协调现场/取数据，派活 ｜ 【合规】涉考核合规 ｜ 【授权】需 Leo 拍板授权 ｜ 【管理】排期人力

---

## ✅ 已由 Leo 拍板 —— 会上对齐执行，不再表决

### 2026-08-15 新增（v1.0 → v1.1 变化）

**#16 电压等级维度定位：不建独立统计维度，字典 + 前端筛选**　【架构·已定】
500kV / 220kV / 66kV及以下**不建独立统计维度**：① 附件6 考核口径是**厂级**（投运率/调节合格率 + 额定容量计罚），不按等级各考一摊；② 统计按 `busbar_num` 落，`v_grade` 经 join `busbar` 恒可得；③ 搁置轨统计 DDL 未定，不加冗余列。等级是**母线属性**（`busbar.v_grade`，0=500kV / 1=220kV / 可扩展）。
→ **已落地（commit `22770f0`）**：RuoYi 字典 `vqms_v_grade`（0=500kV、1=220kV、2=66kV及以下·预留，编码对齐 `busbar.v_grade` 勿改，`sql/vqms.sql` 第六节）+ 前端母线维度 **5 页**（curve / daily / monthly / yearly / threshold）等级筛选与母线级联 + daily/monthly/yearly 表格等级列 + 修订待办 A8 + CLAUDE.md ✅条目。
**边界**：avc-runtime / avc-regulation 为并网主体（厂级）口径，**不加**该筛选。

**会上只需对齐执行项**：
- ① 后端落地时 list 接口行须带 `vGrade` 字段（join `busbar`），前端 `dict-tag` 才能渲染；
- ② `/vqms/busbar` 母线列表接口就绪后，前端母线下拉从本地种子占位换接口拉取（页面已留注释标记）；
- ③ 500kV 母线补录（#13）落地后，UI 自动出现 500kV 分组，无需再改前端；66kV 将来出现也只加字典值 + `busbar` 行，不动表结构。

### 2026-08-14 已定（v1.0 存档内容，结论不变）

**#6 免考分工边界：判定归对端、应用归 VQMS**　【架构，已定】
免考的逐设备判定（每台 AVC 无功设备实测 Q vs 双向极限 + 方向匹配 + 全设备 AND）是**对端 JS 引擎义务**，VQMS 不背；VQMS 只收对端给的 `yx501`（全厂一个，二态 0/1），在统计层**应用**（PENALIZED + yx501=1 → 剔除该档分母）。
→ 连带定调 [修订待办 A2](项目规划_v4_0_修订待办.md)：`RegulationJudge` 只出 `{QUALIFIED, PENALIZED}`，免考不进 judge 判定（`EXEMPT` 是后置读 yx501 的应用结果，非判定产物）。

**会上只需对齐执行项**：
- ① 确认走 `yx501` 委托路线（VQMS 不自建免考判定）；
- ② 催对端实现 `yx501` 的时间表；
- ③ `yx501` 到位前过渡期：免考走人工 / 留空；
- ④ 对端算 `yx501` 时是否把 **SVC/电容等非发电机无功设备**纳入 AND（VQMS 无法自核验，需对端确认覆盖率）。

> 原「会议第一刀」（免考 #6）已定；#1 `T_econ`、#4 重叠、#10 退出原因（yc521/522）、#11 并网信号（yc511/512）也已解决。**当前第一刀建议 #9 门控点补录**（投运率 + 调节合格率都吃它）或 **#3 缺数据策略**（四套候选）。

---

## A. 会上能当场拍的（业务口径 / 参数）

> 📌 判定整定参数的**存储与 CRUD 方案已定**（自建表 `vqms_judge_param`，见 [修订待办 A6](项目规划_v4_0_修订待办.md)）。`T_econ`=5 写死、分档阈值 1/5 政策值锁定、指令 5 分钟间隔 → **#1/#4 已解决**；本节只剩 **#2 `T_fast` 默认值、#3 缺数据策略（A7）、#5 解码失败计数** 待定。

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| ~~1~~ | ✅ **已定**：`T_econ` = **5**（写死）——现场指令 5 分钟间隔，窗口收敛 5 分钟，无上限隐患消除 | 【口径·已定】 | A6 |
| 2 | `T_fast` 默认值：**建议 4**（值域 [1,5) 整数，A6 已定值域与锁定项，会上只定默认数）；分档阈值 1 / 5 分钟为政策值已锁，无需再议 | 【口径】 | 草稿 §2.4；A6 |
| 3 | 缺数据策略：四套候选 + 数据举例见 [数据不可用处理策略](数据不可用处理策略.md)，会上选一套（**推荐乙稳健**，阈值 50% 可整定；可选 +丁透明标记） | 【口径】 | 草稿 §2.8；A7 |
| ~~4~~ | ✅ **已定**：指令实际 5 分钟间隔 + 窗口 5 分钟 → **不重叠**，无需截断 | 【口径·已定】 | A6 |
| 5 | 解码失败：剔除+归因上报 vs 计不合格，数据举例见 [数据不可用处理策略](数据不可用处理策略.md) | 【合规】 | 修订待办 A3 |

## B. 架构方向（建议会上达成共识）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| ~~7~~ | ✅ **方向已定（因 #6）**：judge 只出 `{QUALIFIED,PENALIZED}`、门控前置、免考后置读 `yx501`——按 [修订待办 A2](项目规划_v4_0_修订待办.md) 三阶段实现，别照 v4.0 原文三态写 | 【架构·已定】 | A2 |
| 8 | `tolerance_v` 新角色：`average_SV`/`plan_SV` 已定**废值**（旧模型两字段都没了）→ 旧模型不可用，**倾向 ② 保留作展示/审计**（判定不用，禁用旧 `\|average−plan\|≤tol` 口径）；会上确认 | 【架构】 | 修订待办 C；CLAUDE.md |
| ~~16~~ | ✅ **已定（2026-08-15，v1.1 新增）**：电压等级不建独立统计维度，字典 + 前端筛选，见上「已拍板」节；执行项 ①②③ 会后跟后端 | 【架构·已定】 | 修订待办 A8 |

## C. 要 Leo 协调资源 / 取数据（会上一时定不了，派活）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| 9 | 门控点 `yc_num` 补录（远方就地总 / AVC投退）——`gate_enabled=0` 空转，投运率 + 调节合格率都吃它；**对照 [数据源头（草稿）§一](数据源头（草稿）.md) 门控信号条目要编号** | 【资源】 | CLAUDE.md open follow-up |
| ~~10~~ | ✅ **已解决**：退出原因 = `yc521`/`yc522`（对端 JS 把闭锁信号归类算的三态结论值：`0`=未退出 / `1`=电网免责 / `2`=非电网计罚）。见 [数据源头（草稿）§四 缺口②](数据源头（草稿）.md)。剩执行项：催对端实现 + 对端确认归类规则 | 【资源·已解决】 | 数据源头 §四 |
| ~~11~~ | ✅ **已解决**：并网信号 = `yc511`/`yc512`（对端 JS 算的母线单元信息，编码 = 带电×10 + 并网机组数；**电厂并网 = 任一 yc ≥ 10**）。见 [数据源头（草稿）§四 缺口①](数据源头（草稿）.md)、[JS_并网与母线号.sql](外部DB/JS/JS_并网与母线号.sql) | 【资源·已解决】 | 数据源头 §四 |
| 12 | 真实样例数据：**10.0.0.9 已查（2026-08-14）——`qheatavchisdb` 是合成测试库，无生产数据**（指令每天一条非 5 分钟、循环码位零变化验证不了）；循环码 / 越限 / 真实脏写须**向对端 AVC 系统要生产样本**。~~另注意第二台源机 10.0.0.35 别漏核~~（**已排除**：2026-08-15 Leo 定，10.0.0.35 不纳入范围，外部源唯一 = 10.0.0.9）。见 [qheatavchisdb_样本导出](外部DB/qheatavchisdb_样本导出.md) | 【资源·已查明】 | 草稿 §2.8；样本导出 |
| 13 | 500kV 母线补录 + 阈值表首批值 + 外部源（10.0.0.9，唯一源——10.0.0.35 已排除不用考虑）现场可访问性核对。**v1.1 状态更新：UI/字典侧已就绪**（#16/A8 已落地，500kV 补录进 `busbar` 表后前端自动分组）；⚠️ **连带**：`sql/vqms.sql` 容差种子仍为 v3.2 旧值（`int` 300/500 伏特），须同步为 `decimal(10,3)` kV（1.000/1.500）后方可首启部署（已派活，勿遗漏验收） | 【资源】 | CLAUDE.md open follow-up；修订待办 A8 |

## D. 独立、需 Leo 授权（不开会也尽快）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| 14 | `docs/tmp.md` 明文凭证：git 历史擦除 + 密码轮换，仓库公开前必做，需 Leo 授权（破坏性操作，不主动执行） | 【授权】 | 修订待办 E；CLAUDE.md Security |

## E. 如果要开工

| # | 要定什么 | 性质 |
|---|---|---|
| 15 | D1~D6 确定轨的排期 / 里程碑 / 人力 / 验收标准——要给团队派活的话，会上得有时间线 | 【管理】 |

---

### 会议建议节奏（v1.1）

1. **已定项只过执行项**（不重议）：#6 免考（④ SVC/电容覆盖率须对端确认）、#1/#4（`T_econ`=5、不重叠）、#10/#11（催对端实现）、**#16 电压等级（新，执行项 ①②③ 跟后端）**。
2. 当场定口径：#2 `T_fast` 默认值（建议 4）→ #3 缺数据策略（推荐乙）→ #5 解码失败计数，一圈定完。
3. 架构共识：#7（A2 三阶段实现）、#8 `tolerance_v` 角色（倾向②展示/审计）。
4. 派活定人定时：**#9 门控点（建议第一刀）**、#12 生产样本（只向对端 AVC 系统要，10.0.0.35 已排除）、#13 500kV 补录（UI 已就绪只等数据；⚠️ 先验收 `sql/vqms.sql` 容差种子同步）。
5. #14 授权会下处理；#15 排期若开工则定。
