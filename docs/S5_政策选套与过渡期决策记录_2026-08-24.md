# S5 政策选套与过渡期决策记录（2026-08-24，Leo 拍板，留档备查）

> 本文档记录搁置轨收官阶段的三项过渡期决策及其工程落地，供后查审计。

## 一、S5 政策选套：乙（阈值剔除+计数）✅ 已应用

- **拍板**：Leo 2026-08-24 指定「乙（推荐）：缺失超阈值剔分母+计数」，阈值取默认 **50**（可用度百分比）。
- **落库**：`vqms_policy_param` 四约定键已写入 ry_vqms 实库（迁移 `sql/migrations/2026-08-24_s5_apply_yi.sql`，幂等可重放）：
  | 键 | 值 |
  |---|---|
  | undecodable_mode | EXCLUDE_REPORTED |
  | invalid_tier_mode | EXCLUDE_REPORTED |
  | partial_missing_mode | EXCLUDE_REPORTED |
  | partial_missing_threshold_pct | 50 |
- **代码链路接通**：RegulationPipeline 现按配置逐指令评估并落 `disposition` 列——未选套时 NULL（只记不判）、选套后实时生效；策略参数页改选即切（见 §四）。
- **⚠ 两项 UNVERIFIED 随正式政策文件复核**：
  1. **边界方向**：实现取「可用 ≥50% 用剩余、<50% 剔除」（策略文档 §3.2 口径）；正式版 §2.8 措辞为「缺失 ≥50% 整窗剔除」——两份权威文档相反，烧在 evaluate() 比较方向里、UI 不可整定。
  2. **与固定分母拍板的衔接**：披露合格率仍按全量分母（§2.7 拍板不变）；EXCLUDE_REPORTED 落在罚款缺额链（剔免考推荐读法同族），逐行 disposition 列可审计、可切换重算。

## 二、真实数据样本：过渡期继续工具造数

- **拍板**：暂以 `tools/avc-data-gen` 合成场景（26 场景 + IT 准入体系）为验证基准，生产样本催办转为长期挂起项。
- **含义**：真实数据回放验收（§5.1）、循环码轮转实证、daily/monthly/yearly 三页语义定义继续挂起；合成基准已覆盖的结论（19 场景 oracle 准入、差分测试等）维持有效。
- **恢复条件**：拿到现场样本即启动回放验收流程。

## 三、点号核对 + gate_enabled：由项目侧定默认态

- **决定（Leo 授权项目侧定）**：**维持合成点号体系 + 门控空转（gate_enabled=0）作为联调前默认态**。
- **依据**：真实现场点号未核对（合成占位 3009 在真实库撞号必须换）；门控置 1 前置过滤开始拦截，误配假点号会静默丢判定——空转是唯一安全默认。
- **切换路径（现场核对后执行，一处改种子一处开关）**：
  1. 按「docs/数据源头（草稿）.md 定号一览」核对真实点号（投退 yx1001 / 远方就地 yx2003 / 免考旗 yx501 / 并网 yc511·512 / 退出原因 yc521·522 / 主母线指示 / 实时电压）；
  2. 更新 `vqms_yc_point_map` / `vqms_busbar.realtime_yc_num` 种子值；
  3. 测试环境先置 `gate_enabled=1` 跑门控端到端联调（§4.2 bullet），通过后生产置 1。
- RuntimePipeline 的五信号常量集中在类头一处，换号同批。

## 四、「一键切换」能力验证（本轮补强）

四套预设同权实现、页面四卡片任选即切，行为等价性经两层证明：

| 层 | 测试 | 证明 |
|---|---|---|
| L0 管线矩阵 | `RegulationPipelineTest.allFourPresets_*` | 同一条部分缺失指令（可用度 40%），仅换配置即得四种处置：甲=正常记账 / 乙=剔除计数 / 丙=计不合格 / 丁=挂起标记 |
| L1 实库切换 | `S5PolicySwitchIT`（2 例） | applyPreset 轮换甲→丙→丁→乙，逐套断言 vqms_policy_param 四键库态 + currentState 识别；乙阈值覆盖(40%)持久化；终态恢复乙 |

**切换语义**（§8.7 既定）：apply 只写配置即时生效于后续判定；历史结论不自动连带，更新须经理化任务（Quartz sys_job，当前暂停）按日期区间显式重算。

## 五、回归证据

- `docs/测试/证据/S5_PolicySwitchIT_一键切换通过_surefire_2026-08-24.xml`（2/2）
- `docs/测试/证据/S5_全模块回归228_2026-08-24.log`（224+4 全绿 BUILD SUCCESS）

## 六、遗留提示

1. 乙档两项 UNVERIFIED（上表 §一）随正式政策文件到位复核；
2. 若政策最终否决乙（如要求固定分母不剔除），策略参数页改选其他套别即可，工程零改动；
3. Quartz sys_job 当前暂停——统计自动出数需上线拍板后启用。
