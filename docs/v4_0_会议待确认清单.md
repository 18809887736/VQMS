# VQMS 技术会议 —— 待确认事项清单

> 2026-08-14 Leo 与技术团队会议用。列出需 Leo 拍板或派活的开放决策项。
> **文档修订**动作（v4.0 已提交版怎么改）另见 [项目规划_v4_0_修订待办.md](项目规划_v4_0_修订待办.md)；本清单聚焦**会上要拍什么板 / 派什么活**，交叉项已互相标注。
>
> 决策性质标签：【口径】会上能当场定 ｜ 【架构】需讨论共识 ｜ 【资源】要协调现场/取数据，派活 ｜ 【合规】涉考核合规 ｜ 【授权】需 Leo 拍板授权 ｜ 【管理】排期人力

---

## ✅ 已由 Leo 拍板（2026-08-14）—— 会上对齐执行，不再表决

**#6 免考分工边界：判定归对端、应用归 VQMS**　【架构，已定】
免考的逐设备判定（每台 AVC 无功设备实测 Q vs 双向极限 + 方向匹配 + 全设备 AND）是**对端 JS 引擎义务**，VQMS 不背；VQMS 只收对端给的 `yx501`（全厂一个，二态 0/1），在统计层**应用**（PENALIZED + yx501=1 → 剔除该档分母）。
→ 连带定调 [修订待办 A2](项目规划_v4_0_修订待办.md)：`RegulationJudge` 只出 `{QUALIFIED, PENALIZED}`，免考不进 judge 判定（`EXEMPT` 是后置读 yx501 的应用结果，非判定产物）。

**会上只需对齐执行项**：
- ① 确认走 `yx501` 委托路线（VQMS 不自建免考判定）；
- ② 催对端实现 `yx501` 的时间表；
- ③ `yx501` 到位前过渡期：免考走人工 / 留空；
- ④ 对端算 `yx501` 时是否把 **SVC/电容等非发电机无功设备**纳入 AND（VQMS 无法自核验，需对端确认覆盖率）。

> 原「会议第一刀」（免考 #6）已定；#1 `T_econ`、#4 重叠也因「指令 5 分钟间隔 + T_econ=5」解决（见 A6）。**新第一刀建议 C 节资源类**（门控点 / 退出原因 / 并网信号，投运率分母依赖）或 **#3 缺数据策略**（A7 四套候选）。

---

## A. 会上能当场拍的（业务口径 / 参数）

> 📌 判定整定参数的**存储与 CRUD 方案已定**（自建表 `vqms_judge_param`，见 [修订待办 A6](项目规划_v4_0_修订待办.md)）。`T_econ`=5 写死、指令 5 分钟间隔 → **#1/#4 已解决**；本节只剩 **#2 `T_fast` 默认值、#3 缺数据策略（A7）、#5 解码失败计数** 待定。

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| ~~1~~ | ✅ **已定**：`T_econ` = **5**（写死）——现场指令 5 分钟间隔，窗口收敛 5 分钟，无上限隐患消除 | 【口径·已定】 | A6 |
| 2 | `T_fast` 默认值（5 min）+ 分档阈值（1、5 min）初始值确认 | 【口径】 | 草稿 §2.4 |
| 3 | 缺数据策略：四套候选 + 数据举例见 [数据不可用处理策略](数据不可用处理策略.md)，会上选一套 | 【口径】 | 草稿 §2.8；A7 |
| ~~4~~ | ✅ **已定**：指令实际 5 分钟间隔 + 窗口 5 分钟 → **不重叠**，无需截断 | 【口径·已定】 | A6 |
| 5 | 解码失败：剔除+归因上报 vs 计不合格，数据举例见 [数据不可用处理策略](数据不可用处理策略.md) | 【合规】 | 修订待办 A3 |

## B. 架构方向（建议会上达成共识）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| ~~7~~ | ✅ **方向已定（因 #6）**：judge 只出 `{QUALIFIED,PENALIZED}`、门控前置、免考后置读 `yx501`——按 [修订待办 A2](项目规划_v4_0_修订待办.md) 三阶段实现，别照 v4.0 原文三态写 | 【架构·已定】 | A2 |
| 8 | `tolerance_v` 新角色：判定改指令级包络后不再是判定核心，存什么 / 留不留 | 【架构】 | 修订待办 C；CLAUDE.md open follow-up |

## C. 要 Leo 协调资源 / 取数据（会上一时定不了，派活）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| 9 | 门控点 `yc_num` 补录（远方就地 / AVC投退）——现在 `gate_enabled=0` 空转，投运率 + 调节合格率都吃它；现场拿点位编号 | 【资源】 | CLAUDE.md open follow-up |
| 10 | 退出原因来源（关联电网/非电网）：`warn_info`/`yc` 是否真有 AVC 退出原因点位？没有的话电网/非电网只能人工标注，**标注流程要定** | 【资源/合规】 | 投运率 |
| 11 | 并网信号来源：投运率分母 = 并网运行时间，但并网信号从哪来、多机组怎么合并，还没定 | 【资源】 | 投运率 |
| 12 | 真实样例数据：解码（循环码位）、越限分支、缺数据策略都得真实 `warn_info`/`his_curve` 验证，现在只有退化稳态合成数据——要不要现在去 10.0.0.9 取真实样本？ | 【资源】 | 草稿 §2.8 |
| 13 | 500kV 母线补录 + 阈值表首批值 + 外部源（10.0.0.9）现场可访问性核对 | 【资源】 | CLAUDE.md open follow-up |

## D. 独立、需 Leo 授权（不开会也尽快）

| # | 要定什么 | 性质 | 关联 |
|---|---|---|---|
| 14 | `docs/tmp.md` 明文凭证：git 历史擦除 + 密码轮换，仓库公开前必做，需 Leo 授权（破坏性操作，不主动执行） | 【授权】 | 修订待办 E；CLAUDE.md Security |

## E. 如果要开工

| # | 要定什么 | 性质 |
|---|---|---|
| 15 | D1~D6 确定轨的排期 / 里程碑 / 人力 / 验收标准——要给团队派活的话，会上得有时间线 | 【管理】 |

---

### 会议建议节奏
1. **免考 #6 已由 Leo 定**（判定归对端、VQMS 读 yx501 应用）—— 会上只对齐执行项（见上）。
2. #1/#4 已定（`T_econ`=5、5 分钟间隔）；剩 #2 `T_fast` 默认值 + #3 缺数据（A7）+ #5 解码计数 一圈当场定完。
3. 架构 #7–8 达成共识（#7 即 A2，方向已定、对齐实现）。
4. 资源类 #9–13 派活、定负责人与取数时间。
5. #14 授权会下处理；#15 排期若开工则定。
