# 一、AVC 装置投运率 —— 数据源头（草稿）

> **门控信号总览**（2026-08-14 Leo 补；2026-08-18 更新定号）——判定/统计的**前置过滤**，两个信号都经 `yc_history` 读取（yx 类开关量，合成库统一写 `yc_history`、真实系统在 yx_history——与 yx501 免考同款处理），当前 `gate_enabled=0` 空转（真实现场核对后置 1）：
>
> | 信号         | `yc_num`                          | 判什么                                                      | 影响                                             |
> | ------------ | --------------------------------- | ----------------------------------------------------------- | ------------------------------------------------ |
> | AVC 投退     | 合成占位 3009；**真实候选 `yx1001`**（⚠️ 真实库 3009 另有含义，见下） | AVC 装置投入还是退出                                        | 投运率逐分钟记「投/退」；退出的时段，调节指令不该算 |
> | 远方就地总   | **2003（2026-08-18 已定）**        | 电厂在「远方」（调度/AVC 控）还是「就地」（本地手动）        | 就地模式下 AVC 没有闭环控制权，指令不该算电厂的锅  |

> **`yc_num` 定号一览（2026-08-18，与远方就地总同批考据；证据链均出自对端配置库 `QHeatAvcRtdb`，落地前现场核对）**：
>
> | 信号 | 定号 | 证据链 | 状态 |
> |---|---|---|---|
> | AVC 投退 | `yx1001`（候选） | `AVC_INFO.AVCStatusYxNum=1001`（对端登记的装置状态点）；`CHUNNEL_YX` 转发通道 3/4 均转发 yx1001 给调度 | ⚠️ 语义待现场核对；**勿用 3009**——真实库 yx3009 = 四号机组下闭锁总信号（`JS_DATA` js109），与合成占位号**撞号不同义** |
> | 远方就地总 | `yx2003` | `JS_DATA` js14「远方/就地合并」= OR(yx12, yx23)；`CHUNNEL_YX` 转发；`warn_info` 事件 `warn_type=2, obj_num=2003` | ✅ 2026-08-18 已定 |
> | 主母线号 | `yc3` | 对端自建 `BUSBAR_GROUP`（220kv母线组）登记 `MainBarYcNum=3`——对端已有同款"主母线号指示点" | 待现场核对值域（预期 0/1 = 东/西母线） |
> | 实时母线电压·东母（busbar 0） | `yc8` | `BUSBAR.realVYcNum=8`（东母实时电压）；`CHUNNEL_YC`= 1#高压采集 ×0.01；`PlanVReferenceYcNum=8` 同点自证（对端自己的电压基准也用它） | kV；待现场核对 |
> | 实时母线电压·西母（busbar 1） | `yc14` | `BUSBAR.realVYcNum=14`（西母）；`CHUNNEL_YC`= 2#高压采集 ×0.01；`PlanVReferenceYcNum=14` | 同上 |
> | 实时总有功功率 | `yc216`（1号机）+ `yc316`（2号机），**VQMS 求和** | `GENERATOR.pYcNum`（0号机=216 / 1号机=316）；`CHUNNEL_YC` = 1#/2#机组采集 ×100；**无现成总加 JS 节点**（可选请对端仿 yc11/17 模式补一个） | kW；待现场核对 |
> | AVC 闭环无功设备（免考） | **不另定号** | 已被 `yx501` 接口取代（见 §二.5）；对端内部消费点号存档备查 | 架构已定 |

## 1. AVC 投退

**来源：`yc_history` 表（合成库）/ `yx_history`（真实系统，yx 点——与 yx501、yx2003 同款处理）**。**合成占位 3009；真实候选 `yx1001`（2026-08-18 考据）**：

- **证据链**：`QHeatAvcRtdb.AVC_INFO.AVCStatusYxNum = 1001`——对端登记的 AVC 装置状态点；`CHUNNEL_YX` 转发通道 3/4 均把 yx1001 转发给上级（装置状态对外上报的点）。
- **⚠️ 语义待现场核对**：「AVCStatus」预期 = 装置投/退，但也不排除是装置通信/闭锁状态——核对以现场为准。
- **⚠️ 撞号警示（勿沿用 3009 到真实库）**：合成库占位号 **3009 在真实库是另一回事**——`JS_DATA` js109「四号机组下闭锁总信号」输出 **yx3009**（退出原因细分信号之一，见 [JS计算引擎说明.md](外部DB/JS计算引擎说明.md)）。若把合成占位 3009 直接配到真实环境，VQMS 会把"四号机组下闭锁"当"AVC 投退"读——**静默语义错位**。真实现场核对后必须换号（候选 yx1001）。

| 字段      | 含义                       |
| --------- | -------------------------- |
| `yc_num`  | 合成 3009；真实候选 `1001`（yx 点，见上） |
| `yc_time` | 时间戳                     |
| `yc_data` | `1` = AVC 投；`0` = AVC 退 |

## 2. 远方就地总

**来源：`yc_history` 表**（门控信号）。**`yc_num` = `2003`（yx2003，2026-08-18 定号）**——对端**现成派生点**，无需对端新增实现：

- **派生规则**（对端 JS 引擎，配置库 `JS_DATA` 14 号「远方/就地合并」）：`yx2003 = OR(yx12, yx23)`
  - `yx12` = 220kV 正母 远方(1)/就地(0)；`yx23` = 220kV 副母 对应点（样本期内无变位事件，编号推定）
  - 编码：`1` = 远方（任一母线远方）；`0` = 就地（全部就地）
- **佐证**：`warn_info` 真实事件 `warn_type=2, obj_num=2003`「远方就地总合/分」（2021-05-12/13 样本，与 yx12 变位联动一致）；`CHUNNEL_YX` 通道 3/4 均转发该点。证据链：[JS计算引擎说明.md](外部DB/JS计算引擎说明.md)、[QHeatAvcRtdb.md](外部DB/QHeatAvcRtdb.md)（`JS_DATA`/`CHUNNEL_YX`）、[qheatavchisdb.md](外部DB/qheatavchisdb.md)（warn_info 样本）

| 字段      | 含义                    |
| --------- | ----------------------- |
| `yc_num`  | `2003`（见上）          |
| `yc_time` | 时间戳                  |
| `yc_data` | `1` = 远方 / `0` = 就地 |

> ⚠️ **语义注意（2026-08-18）**：① **OR 合并**——「一母远方、一母就地」的混合态总点 = 远方；日后要分母线精度可直读 `yx12`/`yx23`（现设计取厂级总点，够用）。② **500kV 正母（`yx34`）未并入**总点——500kV 母线组本就待现场补录，届时确认是否扩并。③ 真实现场部署与配置库样本是否一致仍待核对，核对前 `gate_enabled=0`（同 3009 保守策略；合成库联调时测试环境手动置 1）。

## 3. 实时总有功功率记录（多台机组相加）

**来源：`yc_history` 表**。**`yc_num` = `yc216`（1号机有功）+ `yc316`（2号机有功），VQMS 相加得全厂总有功（2026-08-18 定）**：

- **证据链**：`QHeatAvcRtdb.GENERATOR.pYcNum`（一号机组=216 / 二号机组=316，对端登记）；`CHUNNEL_YC` 通道映射（1#/2#机组采集，系数 100）与之互证。
- **无现成总加节点**：`JS_DATA` 里只有总无功（yc11 正母 / yc17 副母 = yc217+yc317），没有总有功累加节点——两个原始点由 VQMS 各读一条相加即可；若想省事可请对端仿 yc11/17 模式补一个 JS 累加节点（可选，非必需）。

| 字段      | 含义                                |
| --------- | ----------------------------------- |
| `yc_num`  | `216` / `316`（见上）              |
| `yc_time` | 时间戳                              |
| `yc_data` | 单机有功，如各 ~`57400`；全厂 = 两者之和，如 `114800` |

---

# 二、AVC 调节合格率 —— 数据源头（草稿）

> 对应附件6 §二 调节合格率；分档考核见 [调节合格率分档考核.md](政策口径/调节合格率分档考核.md)。

## 1. 调度的指令（发令事件）

电压或无功指令。本项目**暂时只考虑电压指令**，无功指令留有接口以便后续开发。

**来源：`warn_info` 表**（注：**不从 `his_curve_sv.plan_SV` 获取**）

| 字段        | 含义             | 备注               |
| ----------- | ---------------- | ------------------ |
| `warn_type` | 通常为 `5`       | 现场人员整定       |
| `obj_num`   | 对象编号         | 现场人员整定       |
| `warn_info` | 目标值 或 增量值 | 文本内含数值，见下 |

指令有两种形态（`warn_info` 文本示例）：

- **增量值**：`收到远方遥调执行指令:辽宁母线电压增量指令编码值处理,2202.`
  - 编码 `2202` 解码（左起 4 位）：
    - **第 1 位 `2`** —— 方向：`2` = 增加，`1` = 减少
    - **第 2 位 `2`** —— 循环码（具体含义待确认）
    - **第 3–4 位 `02`** —— 增量幅值，每单位 100V → `02` = **200V**
  - ⇒ `2202` = **电压增加 200V**
  - 💡 **增量值 + 当前实时母线电压 = 绝对目标值**（与"目标值"形态殊途同归）。例：当前电压 `234.25 kV` + `0.2 kV`（200V）= 目标 `234.45 kV`。两种指令形态最终都产出一个绝对目标电压。
- **目标值**：`收到远方遥调执行指令:主省220KV目标值,22315.` —— 代表 **223.15 kV**
- 其他类型：预留

> 一条指令由（`warn_type`、`obj_num`、目标值或增量值）表征。本项目给个初始值后，保留 **CRUD 的读取、更新**功能。

## 2. 主母线号

**来源：`yc_history` 表**。**`yc_num` = `3`（2026-08-18 定）**——对端**现成登记点**：

- **证据链**：对端配置库自建 `BUSBAR_GROUP` 表（`GroupNum=0`「220kv母线组」）登记 **`MainBarYcNum=3`**——对端已有与 VQMS `busbar_group.main_indicator_yc_num` 同款的"当前主母线号指示点"，直接复用（与 yx2003 同理：找对端现成登记，不新增实现）。
- **佐证**：`warn_info` 真实样本「220Kv副母线非主母线，调节退出!」（[外部数据源.md](外部DB/外部数据源.md) warn 样本清单）——对端调节逻辑本身按"主母线"分流，主母线概念/信号在对端真实存在。
- **⚠️ 待现场核对**：① yc3 的取值域（预期 `0`/`1` = 东/西母线，与 VQMS `busbar_num` 对齐）与变位写入 `yc_history` 的节奏（阶跃保持 vs 周期）；② 该点在 dump 里未见采集通道映射（`CHUNNEL_YC` 无 yc3），来源待核（可能由对端内部计算或未导出的通道写入）。

| 字段      | 含义                        |
| --------- | --------------------------- |
| `yc_num`  | `3`（见上）                 |
| `yc_time` | 时间戳                      |
| `yc_data` | 主母线号，取值如 `0` / `1` |

## 3. 实时母线电压记录

**来源：`yc_history` 表**。**`yc_num` = 东母 `8` / 西母 `14`（2026-08-18 定，按母线各一点）**：

- **证据链**：`QHeatAvcRtdb.BUSBAR.realVYcNum`——busbar 0（220Kv东母线）= **8**、busbar 1（220Kv西母线）= **14**；`CHUNNEL_YC` 通道映射（yc8 = 1#高压采集、yc14 = 2#高压采集，均 ×0.01）互证；`BUSBAR.PlanVReferenceYcNum`（8/14）与实时电压同点——**对端自己算计划电压也用这两个点**，是最可信的母线电压源。
- **与增量解码的 t₀ 电压（⚠️ 口径注意）**：判定用电压按**本地 realV**（yc8/yc14，按主母线 yc3 选点）；但对端自己的增量绝对值计算（`JS_DATA` js16/17：yc1007 = yc1 + yc1005「220KV减210」、yc1008 = yc1 + yc1006「500KV减500」）用的是**调度转发侧电压 `yc1`**（`CHUNNEL_YC` 转发通道 3/4）。两侧量测有偏差时 V_target 会有差异——联调时用对端现成的 `yc1007`/`yc1008`（= 对端算好的"当前+增量"绝对值）做交叉核对；草稿 §2 的 t₀ 取哪侧，随算法解封收口时定。
- **500kV**：对端 `BUSBAR` 现只有 220kV 东/西两条母线，500kV 无 realV 点——与 §四 缺口一致，待现场补录后按同款字段（`realVYcNum`）查号。

| 字段      | 含义                                |
| --------- | ----------------------------------- |
| `yc_num`  | `8`（东母）/ `14`（西母），见上     |
| `yc_time` | 时间戳                              |
| `yc_data` | 母线电压，如 `234.25`（kV，带小数） |

## 4. 实时总有功功率记录（多台机组相加）

同一节（一.3）：**`yc216` + `yc316`，VQMS 相加**——见上文，此处不重复。

## 5. AVC 闭环控制的全部无功设备（已被 yx501 接口取代，存档备查）

按其最大无功功率发出或吸收能力参与无功电压调节（免考判定依据，详见 [免考条款适用范围.md](政策口径/免考条款适用范围.md)）。

> **架构已定（2026-08-14 Leo 拍板，见 §四 缺口③）**：免考的逐设备复杂运算**全部甩给对端 JS 引擎**，VQMS 只收全厂结论值 **`yx501`**——本节**不再由 VQMS 逐点读取**，点号不另定。对端内部消费的点号存档备查（出自 `QHeatAvcRtdb`，VQMS 不读）：

| 对端内部用途 | 点号（`GENERATOR` 登记） |
|---|---|
| 机组实测无功 | 一号机 `yc217` / 二号机 `yc317`（`qYcNum`） |
| 机组实测有功（P_QLimit 查表用） | `yc216` / `yc316`（`pYcNum`） |
| Q 上/下限随动值 | 一号机 `yc203`/`yc204`、二号机 `yc303`/`yc304`（`QUpLimitYcNum`/`QDownLimitYcNum`） |
| P-Q 极限曲线 | `GENERATOR_P_QLimit` 查表（非点号） |

---

# 三、`his_curve_sv` 字段考察口径（草稿）

> ⚠️ **与现行口径冲突，待 Leo 确认**：下表标注 `high_SV`/`low_SV` 为重点考察、`average_SV`/`plan_SV` 不考察——这与 CLAUDE.md / v3.4 / 外部数据源.md 现行决定（**`average_SV` 是唯一合格判定字段**，`high_SV`/`low_SV` 仅为窗口观测极值、不参与判定）**相反**。若为有意调整，须同步改 CLAUDE.md 等；若为草稿误记，再行清理。

| 字段         | 考察口径（草稿） |
| ------------ | ---------------- |
| `save_time`  | 时间戳           |
| `busbar_num` | 考察主母线号     |
| `high_SV`    | **重点考察数据** |
| `low_SV`     | **重点考察数据** |
| `average_SV` | 不做考察         |
| `plan_SV`    | 不做考察         |

---

# 四、数据缺口核对（对照 [AVC考核核心算法_草稿4_1.md](AVC考核核心算法_草稿4_1.md)）

把核心算法每一步需要的数据，对照上文一~三节现有数据源，结果：调节合格率主判定够用，但投运率 / 免考 / 考核量存在 4 个缺口。

| 算法步骤 | 需要的数据 | 现有数据源 | 结论 |
|---|---|---|---|
| 调节合格·解码目标电压（目标值形态） | `warn_info.warn_info` 文本 | ✅ 有（`warn_type=5`） | 够 |
| 调节合格·解码目标电压（增量形态） | 增量编码 + 当前实时母线电压 | ✅ 编码有；实时电压 = yc_history 母线电压点 | 够 |
| 调节合格·按主母线分组 | 主母线号 | ✅ 有（yc_history 主母线号点） | 够 |
| 调节合格·每分钟 high/low | `his_curve_sv.high_SV`/`low_SV` | ✅ 有 | 够 |
| 调节合格·基准时刻 t₀ | `warn_info.warn_time` | ✅ 有 | 够 |
| 投运率·AVC 投退 | yc_history 投退点（`yc_data` 1/0） | ✅ 有 | 够 |
| **投运率·并网运行状态** | **并网主体并网信号** | ✅ **有**：JS 算的 `yc511`/`yc512`（[JS_并网与母线号.sql](外部DB/JS/JS_并网与母线号.sql)） | **已补** |
| **投运率·退出原因** | **AVC 退出原因（电网 vs 非电网）** | 📨 **接口待对端**：`yc521`/`yc522`（每母线一个，3 态 0/1/2） | **接口已定** |
| **调节合格·免考判定** | 每台 AVC 无功设备实测 Q + 双向极限 (Q_max_发, Q_max_吸) | 📨 **接口待对端**：`yx501`（全厂一个，二态 0/1） | **接口已定** |
| **考核量计算** | 并网主体**额定容量**（千瓦） | ✅ **有**：`GENERATOR.ratingPPower` Σ = 600000 kW（定值） | **已补** |

## 缺口说明

- **缺口① 并网运行信号**（投运率分母）：✅ **已补**。由对端 JS 引擎算好的 `yc511`（正母单元信息）/`yc512`（副母单元信息）提供（见 [JS_并网与母线号.sql](外部DB/JS/JS_并网与母线号.sql)、[JS计算引擎说明.md](外部DB/JS计算引擎说明.md)）。编码 = 带电(1/0)×10 + 并网机组数(0/1/2)；**电厂并网 = `yc511≥10` OR `yc512≥10`**（任一母线带电即并网运行）。该值由对端据 `BUS_GEN_LINK_LOGIC_DEV` 的机组-母线开关拓扑（yx201/202/203、yx301/302/303）周期性求值得出，VQMS 直接读结果即可。
- **缺口② AVC 退出原因**（投运率免责）：📨 **接口已定，待对端实现**。复杂运算（把 9 个 `lock_*_yx_num` + yx3001~3009 闭锁信号按"电网 vs 非电网"归类）**甩给对端 JS 引擎做**，VQMS 只接收**2 个结论值**（分母线，因退出按母线发生）：
  - `yc521` 正母退出原因、`yc522` 副母退出原因
  - 三态编码：`0`=未退出 / `1`=电网原因（免责）/ `2`=非电网原因（扣罚）
  - VQMS 侧判定：`0`→投运分钟；`1`→从分母扣除；`2`→计入惩罚性退出
  - 对端职责：按现场/调度确认的归类规则配 JS（哪类闭锁=电网/非电网）；VQMS 不掺和归类逻辑。
- **缺口③ 设备级无功 / 免考判定**（调节合格率免考）：📨 **接口已定，待对端实现**。免考的逐设备复杂运算（每台机实测 Q vs 随 P 变的 P_QLimit 极限、方向匹配、全部顶满汇总）**全部甩给对端 JS 引擎做**，VQMS 只接收**1 个结论值**（全厂一个）：
  - `yx501` 免考标志：`1`=该时段全部 AVC 闭环无功设备已正确方向顶满（**免考**）；`0`=未顶满（不免考）
  - VQMS 侧判定：`调节不合格 AND yx501=1` → 执行不合格-免考（不计率不计罚）；否则 → 执行不合格-非免考（罚）
  - 对端职责：用 `GENERATOR.qYcNum`(yc217/317) + `pYcNum`(yc216/316) + `GENERATOR_P_QLimit` 查表 + 方向匹配 + 全设备 AND，配成 JS 输出 yx501；VQMS 彻底从"设备清单/能力曲线维护"解脱。
  - 说明：发电机部分对端数据齐全（[GENERATOR](外部DB/QHeatAvcRtdb.md) + P_QLimit）；若 AVC 还控其他无功设备（SVC/电容等），对端算 yx501 时自行纳入 AND，VQMS 只信结论。
- **缺口④ 额定容量**（考核量）：✅ **有数据**，是**定值**。并网主体全厂总额定 = `Σ GENERATOR.ratingPPower` = 300000 + 300000 = **600000 kW**（2 台 300MW 机组）。VQMS 处理方式：给个**初始值**（取自配置库 Σ）后，建配置项**保留 CRUD 的读取、更新**功能（运行人员可在系统里改，如增容改造后更新），不写死代码。考核量公式：`缺额百分点 × 额定容量(万千瓦) × 0.02`。

## 结论

- **调节合格率主判定**（high/low 包络判目标电压）：**数据够用** ✅
- **投运率**：缺口① ✅ **已补**（yc511/yc512）；缺口② 📨 **接口已定**（yc521/yc522，待对端实现）→ 分母可算、免责靠对端结论值
- **免考**：缺口③ 📨 **接口已定**（yx501，待对端实现）→ VQMS 读 1 个结论值即可判，复杂运算全在对端
- **考核量**：缺口④ ✅ **已补**（定值 600000 kW，VQMS 给初始值 + CRUD 读写）

> **架构原则**：缺口②③的复杂运算（闭锁归类、逐设备 Q vs 极限）**甩给对端 JS 引擎**，VQMS 只收固定个数的结论值（②收 2 个、③收 1 个），VQMS 侧零计算。点号 yc521/522、yx501 为建议空号，现场导入若冲突可改，接口契约（取值含义）不变。

