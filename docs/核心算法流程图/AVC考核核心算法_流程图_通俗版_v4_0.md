# VQMS AVC 考核核心算法 —— 流程图 v4.0（通俗版 · 给非开发同事看）

> 这张图用大白话讲两件事：**AVC 装置投运率**（它在不在线）和 **调节合格率**（下达指令后调到目标没）。
>
> ⚠️ **先分清**：这里有两个独立考核维度，不是同一个数。
> - **投运率**：考时间——AVC 装置这个月有没有在线干活，够不够 99%。
> - **调节合格率**：考动作——调度下一道调压指令，AVC 有没有在规定时间内把电压调到位。
>
> 📌 与旧的「电压合格率四道关」流程图（[v3.4 通俗版](backup/核心算法流程图_v3_4_通俗版.md)）不是同一套口径。旧图讲每分钟实际电压 vs 目标窄区间；本图讲 AVC 装置投运率 + 指令调节合格率（附件6 §一、§二）。

---

## 图 A：AVC 投运率是怎么算出来的（时间记账）

> 一句话：这个月里，只要机组并网着、AVC 装置也投着，就记「投运」；退了就记「退出」。退出分两种——电网原因导致的退出**不罚**（扣除），非电网原因的退出**计罚**。最后看投运时间够不够 99%。

```mermaid
graph TD
    classDef s1 fill:#f3e8ff,stroke:#6b21a8,stroke-width:2px,color:#000
    classDef s2 fill:#dbeafe,stroke:#1d4ed8,stroke-width:2px,color:#000
    classDef gate fill:#fef9c3,stroke:#a16207,stroke-width:2px,color:#000
    classDef pass fill:#dcfce7,stroke:#15803d,stroke-width:2px,color:#000
    classDef fail fill:#fecaca,stroke:#b91c1c,stroke-width:2px,color:#000
    classDef skip fill:#f1f5f9,stroke:#64748b,stroke-width:1px,color:#000
    classDef out fill:#fce7f3,stroke:#be185d,stroke-width:2px,color:#000

    Start(["统计周期：一个月"]) --> A1

    subgraph 取数 ["① 取数与对齐"]
        A1["读取 AVC 投退信号<br/>1 = 投入 / 0 = 退出"]:::s1
        A1 --> A2["时间归到每一分钟<br/>秒数 ≥30 进位"]:::s2
        A2 --> A3["读取并网运行信号"]:::s1
    end

    A3 --> G1

    subgraph 逐分钟 ["② 逐分钟判定（这个月里每一分钟都过一遍）"]
        G1{"这一分钟<br/>机组并网了吗？"}:::gate
        G1 -->|"没并网"| S1["这一分钟不考核<br/>（直接跳过）"]:::skip
        G1 -->|"并网了"| G2{"这一分钟<br/>AVC 装置投着吗？"}:::gate
        G2 -->|"投着"| P1["投运分钟 +1<br/>（在线干活）"]:::pass
        G2 -->|"退了"| G3{"退出是<br/>谁的原因？"}:::gate
        G3 -->|"电网原因<br/>（甩锅给电网）"| S2["电网原因退出<br/>不罚（扣除）"]:::skip
        G3 -->|"非电网原因<br/>（自身问题）"| F1["非电网退出<br/>计罚"]:::fail
    end

    P1 --> D1
    S2 --> D1
    F1 --> D1

    subgraph 汇总 ["③ 算投运率"]
        D1["投运率 =<br/>投运分钟 ÷（投运分钟 + 非电网退出分钟）<br/>电网原因退出从分母里扣掉"]:::out
        D1 --> D2{"够 99% 吗？"}:::gate
        D2 -->|"够"| E1["合格"]:::pass
        D2 -->|"不够"| D3["缺几个百分点 → 罚<br/>每缺 1 点 = 额定容量 × 0.02 分/万千瓦"]:::fail
    end

    E1 --> End(["结束：投运率出结果"])
    D3 --> End
```

---

## 图 B：调节合格率是怎么算出来的（两档平行 + 免考）

> 一句话：调度下一道调压指令，解码出「目标电压」→ **快速性档**（短窗口）和**经济性档**（长窗口）**同时、各自独立**地判「这一档里电压有没有够到目标」→ 够到就这一档合格；没够到就再单独判「设备是不是都顶满了还调不动」（满足就**免考**、剔除），否则这一档就**罚**。两档各算各的合格率和罚款，最后相加。
>
> 📌 **关键**：两档是**平行**的两个考核，不是「先短窗、没过再看长窗」。一条指令可能在快速性档被罚（调得慢）、但在经济性档合格（最终还是调到位了）——两个结论互不影响。

```mermaid
graph TD
    classDef s1 fill:#f3e8ff,stroke:#6b21a8,stroke-width:2px,color:#000
    classDef s2 fill:#dbeafe,stroke:#1d4ed8,stroke-width:2px,color:#000
    classDef gate fill:#fef9c3,stroke:#a16207,stroke-width:2px,color:#000
    classDef pass fill:#dcfce7,stroke:#15803d,stroke-width:2px,color:#000
    classDef fail fill:#fecaca,stroke:#b91c1c,stroke-width:2px,color:#000
    classDef skip fill:#f1f5f9,stroke:#64748b,stroke-width:1px,color:#000
    classDef out fill:#fce7f3,stroke:#be185d,stroke-width:2px,color:#000

    Start(["一道 AVC 调压指令"]) --> A1

    subgraph 解码 ["① 算出这道指令的目标电压"]
        A1["把指令翻译成<br/>「要把电压调到多少 kV」"]:::s1
        A1 --> A2{"能算出来吗？"}:::gate
        A2 -->|"翻译失败 / 数据缺失"| S1["这道指令跳过<br/>不计数"]:::skip
        A2 -->|"算得出"| A3["目标电压 V"]:::s2
    end

    A3 --> B1

    subgraph 对齐 ["② 取这一段电压曲线（按分钟）"]
        B1["以下令时刻为起点<br/>取后面每分钟的最高/最低电压"]:::s2
    end

    B1 --> C1
    B1 --> D1

    subgraph 快速窗 ["③ 快速性档（短窗口，约前 5 分钟）—— 考「调得快不快」"]
        C1["把短窗口里每分钟<br/>最高/最低电压并起来<br/>得到一个总范围"]:::s2
        C1 --> C2{"目标电压落进<br/>这个短窗口范围了吗？"}:::gate
        C2 -->|"落进去了"| Q1["✅ 快速性合格<br/>调得快"]:::pass
        C2 -->|"没落进去"| CF1{"免考吗？<br/>设备都顶满了<br/>还是调不动？"}:::gate
        CF1 -->|"是（设备已尽力）"| QF1["快速性免考<br/>剔除：不算不罚"]:::skip
        CF1 -->|"否 / 拿不到数据"| QF2["❌ 快速性不合格，罚"]:::fail
    end

    subgraph 经济窗 ["④ 经济性档（长窗口，约 5~30 分钟）—— 考「最终调到位没」<br/>★ 与 ③ 平行、各自独立，不靠 ③ 的结果 ★"]
        D1["把长窗口里每分钟<br/>最高/最低电压并起来<br/>得到一个总范围"]:::s2
        D1 --> D2{"目标电压落进<br/>这个长窗口范围了吗？"}:::gate
        D2 -->|"落进去了"| Q2["✅ 经济性合格<br/>最终调到位"]:::pass
        D2 -->|"没落进去"| DF1{"免考吗？<br/>设备都顶满了<br/>还是调不动？"}:::gate
        DF1 -->|"是（设备已尽力）"| QE1["经济性免考<br/>剔除：不算不罚"]:::skip
        DF1 -->|"否 / 拿不到数据"| QE2["❌ 经济性不合格，罚"]:::fail
    end

    Q1 --> RF0
    QF1 --> RF0
    QF2 --> RF0
    Q2 --> RE0
    QE1 --> RE0
    QE2 --> RE0

    subgraph 统计 ["⑤ 两档各自算账（互不掺和）"]
        RF0["【快速性】有效 = 总发令 − 快速性免考<br/>快速性合格率 = 快速性合格数 ÷ 有效"]:::out
        RE0["【经济性】有效 = 总发令 − 经济性免考<br/>经济性合格率 = 经济性合格数 ÷ 有效"]:::out
        RF0 --> RF1["快速性罚款 = 缺的百分点 × 机组容量 × 0.02 分/万千瓦"]:::fail
        RE0 --> RE1["经济性罚款 = 缺的百分点 × 机组容量 × 0.02 分/万千瓦"]:::fail
        RF1 --> SUM["总罚款 = 快速性罚款 + 经济性罚款<br/>（两个合格率分开看，不合并成一个数）"]:::out
        RE1 --> SUM
        SUM --> EX["举例：发令 100 次<br/>快速性：免考 3、罚 7 → 缺 7.2 点<br/>经济性：免考 5、罚 5 → 缺 5.3 点<br/>→ 两个档各罚各的，相加得总罚款"]:::out
    end

    EX --> End(["结束：快速性合格率 / 经济性合格率 / 总罚款"])
```

---

## 颜色含义

紫 = 取数 · 蓝 = 对齐 / 算区间 · 黄 = 判定关 · 绿 = 合格 · 红 = 不合格 / 罚 · 灰 = 不计 / 跳过 / 剔除 · 粉 = 汇总出结果。

## 几个还没定死的点（留给业务确认）

- **「退出原因」从哪来**（图 A ②）：系统目前不确定能不能自动分清「电网原因」vs「自身原因」，可能需要人工标注。
- **并网信号从哪来**（图 A ①）：机组并网运行的信号来源待定。
- **长窗口多久算到头**（图 B ④）：「调得慢」的窗口上限（比如 30 分钟）待定；不设上限的话越限拖很久也算合格，不合理。
- **窗口里某分钟没数据**（图 B ③④）：暂定跳过那一分钟，整段全没数据就放弃这道指令。
- **免考能否自动判**（图 B ⑤）：需要设备级无功数据；系统若拿不到，免考留人工。

> 详细技术口径见 [AVC考核核心算法_草稿4_1.md](../AVC考核核心算法_草稿4_1.md) 与 [技术版流程图](AVC考核核心算法_流程图_v4_0.md)。
