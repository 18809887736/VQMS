# VQMS 核心算法：分钟至日表 Rollup (预计算)

该流程图展示了从获取原始数据、时间格式清洗、分桶对齐，到层层过滤判定（主母线归属、门控、数据有效性、合格判定）的完整闭环。

```mermaid
graph TD
    %% 样式定义
    classDef input fill:#f9f,stroke:#333,stroke-width:2px;
    classDef process fill:#bbf,stroke:#333,stroke-width:1px;
    classDef decision fill:#ff9,stroke:#333,stroke-width:1px;
    classDef output fill:#bfb,stroke:#333,stroke-width:2px;
    classDef subProcess fill:#eee,stroke:#333,stroke-width:1px,stroke-dasharray: 5 5;

    Start(["开始: 每日调度"]) --> InputData
    
    subgraph 阶段一: 数据获取与格式清洗
        InputData["输入: stat_date, group_num"]:::input --> FetchData
        FetchData["放宽边界读取当天数据"]:::process
        FetchData --> SQL1[("SQL-1: his_curve_sv\n电压原始数据")]
        FetchData --> SQL2[("SQL-2: yc_history\n遥信与状态数据")]
        
        SQL1 --> FormatCheck
        SQL2 --> FormatCheck
        
        FormatCheck{"① 格式校验\n正则匹配"}:::decision
        FormatCheck -->|"不匹配"| LogDirty["记日志并标缺"]:::process
        FormatCheck -->|"匹配"| ParseTime["② 解析为真实时间 ts"]:::process
        
        ParseTime --> RangeCheck{"③ 精确范围过滤\nts ∈ 当天?"}:::decision
        RangeCheck -->|"否(超出边界)"| Drop["丢弃/归相邻日"]:::process
        RangeCheck -->|"是(当天数据)"| Binning
    end

    subgraph 阶段二: 分桶与时间对齐
        Binning["按分钟就近取整对齐 t"]:::process
        Binning --> VBin["电压桶: keyed by t × busbar_num\navg_SV均值, plan_SV末条"]
        Binning --> SBin["状态桶: keyed by t\n取yc_time ≤ t最近一条"]
    end

    subgraph 阶段三: 逐分钟遍历判定
        VBin --> Loop["遍历每个对齐分钟 t"]
        SBin --> Loop
        
        Loop --> Layer0{"层0: 主母线归属\nmainBusbarNum(t)?"}:::decision
        Layer0 -->|"判定失效/无主母线"| Gap["整组该分钟空档\n不计入任何统计"]:::process
        Layer0 -->|"判定成功"| GetMainBusbar["选取当分钟主母线\nbusbar_num = main"]:::process
        
        GetMainBusbar --> Layer1{"层1: 门控关\nAVC投退 / 远方就地"}:::decision
        Layer1 -->|"门控命中(退出/就地)"| Exclude1["剔除\n记入 excluded_minutes"]:::process
        
        Layer1 -->|"门控未命中"| Layer2{"层2: 数据关\nplan_SV缺值或废值?"}:::decision
        Layer2 -->|"缺值/废值"| Policy{"策略分流\nplan_sv_invalid_policy"}:::decision
        Policy -->|"SKIP"| Exclude2["剔除\n记入 excluded_minutes"]:::process
        Policy -->|"COUNT_UNQUALIFIED"| Unqualified["不合格"]:::process
        Policy -->|"FALLBACK"| Layer3
        
        Layer2 -->|"数据正常"| Layer3
        
        Layer3{"层3: 合格关 (AVC)\n|avg_SV - plan_SV| ≤ 容差?"}:::decision
        Layer3 -->|"满足"| Qualified["合格\n记入 qualified"]:::output
        Layer3 -->|"偏高(超正容差)"| OverHigh["偏高\n记入 over_high"]:::output
        Layer3 -->|"偏低(超负容差)"| OverLow["偏低\n记入 over_low"]:::output
    end
    
    subgraph 阶段四: 日表聚合与持久化
        Qualified --> Accumulate["累加当日统计数据\nmax_SV, min_SV, sum(avg_SV)"]:::process
        OverHigh --> Accumulate
        OverLow --> Accumulate
        Exclude1 --> Accumulate
        Exclude2 --> Accumulate
        Unqualified --> Accumulate
        
        Accumulate --> Upsert[("UPSERT\nvoltage_quality_daily")]:::output
    end
    
    Upsert --> End(["结束该组计算"])
```

## 算法分层说明 (对应 §5.7)

1. **层 0 (主母线归属)**: 在进入合格率计算前，必须先确定该分钟该组的主母线是哪一条。这一步是整个算法的**前置闸门**，避免将副母线的数据误计入考核。
2. **层 1 (门控关)**: 依据站级 YX 信号 (如远方/就地模式，AVC 投入/退出) 决定当分钟是否参与考核。如果不参与，则计入 `excluded_minutes`，不进入总考核时长分母。
3. **层 2 (数据关)**: 对基础数据质量进行控制，对于下发目标电压 (`plan_SV`) 异常或缺失的情况，提供策略配置，通常也会走向剔除 (`SKIP`)。
4. **层 3 (合格关)**: 实际的控制达标率判定，判断偏差是否在配置的容差 `tolerance_v` 内。

### 合格率与对账 (§5.6 / §5.7.1)

- 日：`qualification_rate = qualified / total × 100`，`total = qualified + over_high + over_low`
- `total + excluded = 该母线当天充当主母线的全部分钟` (excluded 只含层 1 与 层 2 剔除，**不含**层 0 跳过与组级空档)
- 月/年：`rate = SUM(qualified) / SUM(total) × 100` (**绝不直接 AVG rate 列**)；`avg_SV` 按 `total` 加权；`excluded = SUM(excluded)`
