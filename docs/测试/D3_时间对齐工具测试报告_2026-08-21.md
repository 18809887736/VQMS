# D3 测试报告 — 时间对齐工具（就近取整到分钟）

> **日期**: 2026-08-21 14:30 首发 / 15:40 评审后补充（§五）
> **基准文档**: `docs/项目规划_v5_0.md` §13 时间原则（L662）/ §12.1 D3 完成标准（L624）/ 取整责任边界（L155）/ CLAUDE.md「Source-data 硬约束」/ `docs/测试/VQMS_测试方案_v5_0.md` §2.1 / §4.3
> **测试性质**: 纯单元测试（无数据库依赖，不需 10.0.0.9 连接）
> **Commit**: `7e91849`（工具+示例测试）/ `cdea748`（属性测试补充）

---

## 一、执行摘要

| 指标 | 数值 |
|---|---|
| 测试类 | `MinuteRounderTest`（示例锚点）/ `MinuteRounderPropertyTest`（jqwik 属性，`cdea748` 补充） |
| 用例总数 | 16 示例 + 5 属性（每属性 1000 随机样本 + 6 边缘样本） |
| 通过 | **16 + 5 全通过** |
| 失败 | 0 |
| 错误 | 0 |
| 跳过 | 0 |
| 执行耗时 | 0.058s + 0.614s |
| **结论** | ✅ **全部通过** |

运行命令：

```bash
JAVA_HOME=/c/environment/jdk-17.0.20+8 mvn -pl ruoyi-vqms -am test \
  -Dtest=MinuteRounderTest -Dsurefire.failIfNoSpecifiedTests=false
```

> 环境注意：本机默认 JDK 为 1.8（Corretto），编译目标是 17，须显式指 `JAVA_HOME=/c/environment/jdk-17.0.20+8`。

---

## 二、交付清单

| 文件 | 类型 | 说明 |
|---|---|---|
| [MinuteRounder.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/MinuteRounder.java) | 工具类 | `parse` / `round` / `parseAndRound` 三方法 |
| [MinuteRounderTest.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/MinuteRounderTest.java) | 单元测试 | 16 用例，`assert_描述` 命名风格与 D1/D2 一致 |
| [SaveTimeFilter.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/SaveTimeFilter.java) | 既有收紧 | 正则 `(\.\d+)?` → `(\.\d{1,9})?`，与 parse 口径对齐 |

### API 契约

| 方法 | 入参 | 出参 | 语义 |
|---|---|---|---|
| `parse(String)` | 时间原文（`uuuu-MM-dd[空格或T]HH:mm:ss[.1~9位]`） | `LocalDateTime` / null | 北京时间墙钟解释（无 UTC 中转）；格式非法/空返回 null，坏行由调用方跳过+记日志 |
| `round(LocalDateTime)` | 非空时间 | `LocalDateTime` | 秒≥30 进位（跨分/时/日/月/年自然进位），<30 舍去，毫秒不参与判定；幂等 |
| `parseAndRound(String)` | 时间原文 | `LocalDateTime` / null | parse + round 一步；D4 ③ 闸门与搁置轨 judge 窗口对齐的推荐入口 |

### 规约符合性映射（v5.0）

| 规约条款 | 实现位置 |
|---|---|
| 秒≥30 进位 / <30 舍去（§13 L662） | `round()`：`truncatedTo(MINUTES)` + `second>=30 ? +1min` |
| 禁 floor/截断/按原始秒分组 | 同上——进位语义本身排除截断 |
| 幂等（双重取整无害，L155） | 已整到分钟 → second=0 <30 → 不变 |
| reader 返回原始时间戳、对齐由调用方经 D3 完成（L155） | 工具独立于 reader，`SourceReader` 未改动 |
| his_curve_sv / yc_history 共用（D3 完成标准 L624） | 单一工具按原文格式解析，两表时间列同构 |
| Asia/Shanghai 不做 UTC 中转 | `LocalDateTime` 无时区数学 |

---

## 三、断言清单与结果

### 取整语义（9 例）

| # | 测试方法 | 输入 → 预期 | 结果 |
|---|---|---|---|
| 1 | `assert_秒30整_进位` | 12:34:30.000 → 12:35 | ✅ |
| 2 | `assert_秒59带毫秒_进位` | 12:34:59.999 → 12:35 | ✅ |
| 3 | `assert_秒29带大毫秒_舍去` | 12:34:29.999999999 → 12:34 | ✅ |
| 4 | `assert_秒0整_舍去不变` | 12:34:00.0 → 12:34 | ✅ |
| 5 | `assert_跨分边界_59分30秒进到整点` | 12:59:30.123 → 13:00 | ✅ |
| 6 | `assert_跨小时跨日边界` | 08-21 23:59:30.500 → 08-22 00:00 | ✅ |
| 7 | `assert_跨月跨年边界` | 2026-12-31 23:59:30 → 2027-01-01 00:00 | ✅ |
| 8 | `assert_闰年2月末边界` | 2028-02-28 23:59:59.999 → 02-29；2026-02-28 → 03-01（非闰） | ✅ |
| 9 | `assert_幂等_双重取整不变` | round(round(x)) == round(x) | ✅ |

### 原文解析（7 例）

| # | 测试方法 | 覆盖 | 结果 |
|---|---|---|---|
| 10 | `assert_parse_空格分隔带毫秒` | `2026-08-21 12:34:56.789`（真实 save_time 形态） | ✅ |
| 11 | `assert_parse_T分隔无毫秒_首尾空白容忍` | `  2026-08-21T12:34:56  ` | ✅ |
| 12 | `assert_parse_非法原文返回null` | null/空白/缺时间/无分隔符/月13/时25/垃圾/10位小数 → 全 null | ✅ |
| 13 | `assert_parse_不可能日期与24点_拒绝且不钳位改值` | 2027-02-29 / 04-31 / 02-30 / 24:00:00 → 全 null（含 parseAndRound 路径） | ✅ |
| 14 | `assert_parse_尾随裸小数点_与正则契约一致拒绝` | `…12:00:00.` → null | ✅ |
| 15 | `assert_parse_真闰日合法` | 2028-02-29 12:00:00 正常解析（防 STRICT 误伤） | ✅ |
| 16 | `assert_parseAndRound_一步` | `12:34:29.999`→12:34、`12:34:30`→12:35、垃圾→null | ✅ |

---

## 四、对抗验证记录（3 视角 workflow，2026-08-21）

实现后经 3 并行评审代理对抗验证（规约符合性 / 解析器边界对抗 / 调用方适配），结论 **PASS / FAIL / PASS**，抓出 1 个 blocker + 3 个 minor/nit，全部当日修复：

| 严重度 | 发现 | 处置 |
|---|---|---|
| **blocker** | 默认 `ResolverStyle.SMART` 把 `2027-02-29` 静默钳位成 02-28、`04-31`→04-30、`02-30`→02-28、`24:00:00`→次日 00:00——脏 `save_time` 带错值过关且不落坏行日志，比丢行更糟（正是 D4 闸门要拦的反面） | ✅ `uuuu-MM-dd` + `ResolverStyle.STRICT`，四类输入全拒；真闰日 2028-02-29 不受影响（用例 15）；补用例 13 |
| minor | 两层判定不一致：≥10 位小数正则收（`\d+` 无上限）、parse 拒（max 9） | ✅ `SaveTimeFilter` 正则收紧 `\d{1,9}`，口径统一 |
| minor | `round(null)` NPE 与 parse/parseAndRound 的 null 语义不对称 | ✅ javadoc 声明非空契约 + 指引组合路径走 `parseAndRound` |
| nit | 尾随裸小数点（`…12:00:00.`）正则拒、parse 收 | ✅ parse 显式拒绝 `endsWith(".")`，与正则完全对齐（补用例 14） |
| nit | D4 ① 需 time→string 反向格式化，本工具不提供 | 不处置——D4 自行加一行 formatter 即可，非 D3 缺陷 |

验证方法注记：解析器对抗代理在 JDK 17 下逐字复制 `buildParser()` 实测复现四类钳位输入，并实测修复版对真实数据全形态（空格/T 分隔、无毫秒、`.5`/`.099`/`.11`/`.123456789` 变宽毫秒）解析结果与原版一致——修复后才回写仓库。

---

## 五、偏离说明与属性测试补充（2026-08-21 评审后，`cdea748`）

### 5.1 偏离声明（Leo 2026-08-21 评审指出）

测试方案 §2.1/§4.3 定 D3 为 **TDD 起手式**：jqwik 属性断言先写、边界示例作锚点补充。首发交付（`7e91849`）只有示例式 JUnit 用例，未引入 jqwik，且报告未声明偏离。**属执行疏漏，非有意改策略**——首发时未对照 §4.3 的属性清单逐条落，其中「单调性」不变量在首发用例中完全缺失（幂等/归零仅由示例间接覆盖）。

### 5.2 补充内容

依赖（`ruoyi-vqms/pom.xml`，test scope）：`net.jqwik:jqwik:1.10.1` + `net.jqwik:jqwik-time:1.10.1`（spring-boot BOM 不管理 jqwik，版本显式钉死）。

[MinuteRounderPropertyTest.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/MinuteRounderPropertyTest.java)，生成域 1600~2999 年任意时间，每属性 1000 随机样本：

| 属性 | 断言 | 来源 |
|---|---|---|
| `prop_幂等` | `round(round(t)) = round(t)` | §4.3 属性① |
| `prop_单调` | `t₁ ≤ t₂ ⇒ round(t₁) ≤ round(t₂)`（t₂ = t₁ + 0~86399s） | §4.3 属性②——**首发缺失，本次补上** |
| `prop_秒纳秒归零` | 取整结果 second=0 且 nano=0 | §4.3 属性③ |
| `prop_全秒空间_当且仅当秒大于等于30进位` | 0~59 秒 × 任意纳秒全空间：进位 ⟺ 秒≥30 | Leo 评审要求 |
| `prop_原文往返_解析取整等价` | 任意时间 → 原文格式 → `parseAndRound` ≡ `round` | 自加——STRICT 解析器全空间不丢不改的随机化证据 |

### 5.3 结果

5 属性 × (1000 随机 + 6 边缘) 样本全部通过，0 失败；与 16 示例锚点（29.999/30.000/30.5、跨分/时/日/月/年、闰年双向）互补，D3 测试形态现与测试方案 §4.3 对齐。

---

## 六、已知限制与后续工作

1. **`round(null)` 抛 NPE（有契约声明）**：`round` 设计为纯函数、入参须非空；脏原文一律先过 `parse`/`parseAndRound`（null 拦截）。D4 ③ 与搁置轨 judge 窗口对齐按此使用。
2. **D4 三步闸门待落地**：① 放宽边界字符串粗筛（需 D4 自备 time→string formatter）② 正则校验（`SaveTimeFilter` 已就位）③ 解析+取整+Java 侧精确过滤（`parseAndRound` 已备好）。取整最大偏移 ±1 分钟与 ① 的 ±1 分钟放宽幅度恰好匹配（评审确认）。
3. **坏行跳过日志**：`parse` 返回 null 即坏行信号，记日志责任在调用方（与 D1 reader 的 `logBeforeAfter` 模式一致），工具自身不落日志。
4. **真实数据毫秒位数不固定已覆盖**：样例库出现 `.099`/`.103`/`.11`（2~3 位），`appendFraction(0..9)` 变宽解析全部正确（对抗验证实测）。
5. **pre-commit hook 只跑 L0（测试方案 §6 批次表脚注）**：条件「首个 Java L0 测试存在即挂上」自本报告起成立——待挂 git hook（仅跑 `MinuteRounder*` 级别纯单测，排除 IT），随 D4 交付一并落地。

---

## 七、相关文件

| 类型 | 路径 |
|---|---|
| 工具类 | `ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/MinuteRounder.java` |
| 单元测试（示例锚点） | `ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/MinuteRounderTest.java` |
| 属性测试（jqwik） | `ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/MinuteRounderPropertyTest.java` |
| 依赖变更 | `ruoyi-vqms/pom.xml`（+jqwik 1.10.1 / +jqwik-time 1.10.1，test scope） |
| 格式校验（D1 既有，本次收紧） | `ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/SaveTimeFilter.java` |
| 规约依据 | `docs/项目规划_v5_0.md` §13 / §12.1 D3 行 / L155 时间契约 / `docs/测试/VQMS_测试方案_v5_0.md` §2.1 / §4.3 |
| Surefire 报告 | `ruoyi-vqms/target/surefire-reports/TEST-com.ruoyi.vqms.source.reader.MinuteRounder*.xml` |

---

*报告生成于 2026-08-21 14:30，15:40 评审后补充属性测试章节，由 Claude Code 自动生成。*
