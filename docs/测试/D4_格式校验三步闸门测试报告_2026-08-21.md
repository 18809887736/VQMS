# D4 测试报告 — 格式校验三步闸门

> **日期**: 2026-08-21 16:00 首发 / 16:15 评审修复后终版
> **基准文档**: `docs/项目规划_v5_0.md` §12.1 D4 行（L625）+ D4 三步闸门内联细节（L632）/ §5 时间契约（L155）/ `docs/测试/VQMS_测试方案_v5_0.md` §2.1 / §4.4 / CLAUDE.md「Source-data 硬约束」
> **测试环境**: L0 纯单测（本机）+ L1 直连 10.0.0.9 `qheatavchisdb`（凭证走环境变量 `VQMS_AVC_TEST_USER/PASSWORD`，不入库）
> **Commit**: `8c7ae2d`（闸门 + pre-commit 检查点）/ `30992fb`（对抗验证修复）

---

## 一、执行摘要

| 指标 | 数值 |
|---|---|
| L0 锚点 | `SaveTimeGateTest` 13 用例 ✅ 全过 |
| L0 属性 | `SaveTimeGatePropertyTest` 4 属性 × 1000 样本 ✅ 全过 |
| L1 场景 | `D4GateScenarioIT` 4 用例（真实库）✅ 全过 |
| 回归 | D1 两 IT（6+4）、D3 两类（16+5）✅ 全过 |
| 合计 | **52 例，0 失败** |
| TDD | ✅ 严格红绿：stub 先行 11 锚点全红（UnsupportedOperationException）→ 实现转绿 |
| pre-commit | ✅ L0 检查点已挂（`.githooks/pre-commit` + `core.hooksPath`），`8c7ae2d` 起每次提交实跑验证 |

运行命令：

```bash
# L0（pre-commit 同款）
JAVA_HOME=<JDK17> mvn -pl ruoyi-vqms -am test
# IT（需 10.0.0.9 可达 + 凭证环境变量）
VQMS_AVC_TEST_USER=... VQMS_AVC_TEST_PASSWORD=... mvn -pl ruoyi-vqms -am test \
  -Dtest='D4GateScenarioIT,ManifestScenarioReadIT,Mysql57SourceReaderIT' -Dsurefire.failIfNoSpecifiedTests=false
```

---

## 二、交付清单

| 文件 | 角色 |
|---|---|
| [SaveTimeGate.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/SaveTimeGate.java) | 闸门：`expandBounds`（①）/ `filter`（②+③），表无关泛型 |
| [Mysql57SourceReader.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/Mysql57SourceReader.java) | 三读方法接入：expand → mapper 粗筛 → filter 精滤；返回原始时间戳 |
| [SaveTimeFilter.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/SaveTimeFilter.java) | ② 正则，收窄为 canonical 契约（见 §五.1） |
| [SaveTimeGateTest.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/SaveTimeGateTest.java) | 13 锚点：三步各自独立断言 |
| [SaveTimeGatePropertyTest.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/SaveTimeGatePropertyTest.java) | 4 属性（§4.4 两条 + 粗筛不变量 + 恰好保留） |
| [D4GateScenarioIT.java](RuoYi-Vue-springboot3/ruoyi-vqms/src/test/java/com/ruoyi/vqms/source/reader/D4GateScenarioIT.java) | 真实库三表零误杀（warn/curve/yc） |
| [.githooks/pre-commit](.githooks/pre-commit) | §6 交付检查点：ruoyi-vqms Java/XML 改动触发 L0；强制 JDK17（ambient JAVA_HOME=JDK8 不可信，`VQMS_JDK17` 可覆盖） |

---

## 三、断言清单与结果

### L0 锚点（13）

| 分组 | 用例 | 断言 |
|---|---|---|
| ① | `expand_区间前后各扩1分钟` / `expand_跨日跨年边界` | ±1 分钟、秒精度字符串、跨日/年进位正确 |
| ① | `expand_非法边界快速失败` / `expand_倒置区间快速失败` | 非法串与倒置区间抛 IAE（caller bug fail-fast，评审修复） |
| ② | `坏行拦截_好行不受影响` | 垃圾串/不可能日期/尾裸点混批：坏行跳过、好行保留、不抛 |
| ② | `全坏行返回空且不抛` / `空列表平安通过` | 含 null 元素不 NPE |
| ② | `T分隔与空白衬垫_按坏行处理` | canonical 契约显式化（评审修复） |
| ③ | `边界分钟不丢行_秒30进位纳入` | `09:59:30.123` 取整 10:00 ∈ 目标 → 保留 |
| ③ | `边界分钟_秒29舍去剔除` | `09:59:29.999` → 09:59 ∉ 目标 → 剔除 |
| ③ | `区间两端闭含` / `区间尾部秒30进位越界剔除` | 闭区间语义 + `10:05:30`→10:06 越界剔除 |
| ③ | `原始时间戳原样保留` | 返回原文实例，不取整改写（§5 契约） |

### L0 属性（4 × 1000 样本，测试方案 §4.4 两条全覆盖）

| 属性 | 不变量 |
|---|---|
| `prop_粗筛窗口包含所有目标行` | 凡取整后落目标区间的行，原文必在放宽边界内（① 边界分钟不丢行的生成器证明；row 生成收窄至窗口 ±2 分钟，评审修复空转） |
| `prop_合法格式恒通过` | 变宽毫秒 0~9 位 × 窗口环绕构造，合法行永不误杀 |
| `prop_非法格式恒拦截且不抛` | 四类腐化（换非法字符/删字符/截断/尾部垃圾）经 `parse()==null` 过滤后恒拦截 |
| `prop_过滤恰好保留目标区间内的行` | 保留 ⟺ 取整后落闭区间（不多不少） |

### L1 场景（4，真实库 qheatavchisdb）

| 用例 | 断言 |
|---|---|
| `warn_20条指令全量过闸门_条数不丢` | 闸门组合后指令数仍 == 20（manifest 承诺）；粗筛 ≥ 精滤 |
| `curve_闸门零误杀_不丢行且增留行必属区间` | 双母线全区间：直读行全保留；增留行（如 `09:59:57.100` 类非整分行）取整必落区间——**实证直读 BETWEEN 边界丢行缺陷** |
| `curve_单分钟窗口_精滤语义精确` | 单分钟窗口内所有行取整恰为该分钟 |
| `yc_闸门零误杀_不丢直读行` | 第三表（U01–U07 投运率数据）零误杀收口（评审修复，§4.4「26 场景全量过闸门」三表全覆盖） |

---

## 四、TDD 红绿记录

1. **红**：stub `SaveTimeGate`（两方法抛 UnsupportedOperationException）+ 11 锚点先行 → 全红（10 errors + 1 wrong-exception）
2. **绿**：实现 `expandBounds`/`filter` → 11 锚点转绿（期间修一处测试自身缺陷：`List.of` 不容 null 元素）
3. 属性类随后落地，抓出生成器两处缺陷（见 §五.2）并修复

---

## 五、对抗验证记录（3 视角 workflow，2026-08-21）

结论 **PASS×3**，10 findings，5 项当日修复、5 项声明处置：

### 5.1 已修复（`30992fb`）

| 发现 | 严重度 | 修复 |
|---|---|---|
| **①②③ 格式契约不一致**：正则/解析器宽容 T 分隔与空白衬垫，但 ① SQL 字典序对它们结构性丢弃——`2026-08-21T09:59:45.100` 行 SQL 层无声消失、不进坏行日志（D4 要杀的「边界丢行」同构漏洞） | minor×2 | `SaveTimeFilter` 收窄 canonical 契约（空格分隔、无首尾空白、小数 1~9 位）——T/衬垫行从「SQL 静默丢」变「② 显式坏行记日志」，三步承诺「② 认合法 = ③ 可见」闭合；`MinuteRounder` 作为独立工具保留宽容（D3 契约），口径差异 javadoc 声明 |
| yc_history 闸门路径无集成覆盖，§4.4「26 场景全量过闸门」只实证两表 | minor | IT 增第四用例 |
| `expandBounds`/`filter` 不校验 start ≤ end，倒置区间静默空——考核数据源会被下游当「0 条真值」消费 | nit（升级修复） | `requireWindow` 倒置即抛 IAE，与非法边界同口径 fail-fast + 锚点 |
| 属性 ①/④ 近乎空转：全域随机 row 落窗概率 ≈2e-6，in-window 分支千次试验零命中 | minor | row 生成收窄至窗口 ±2 分钟偏移 |
| 三 IT 硬编码测试库 root 口令兜底（D1 遗留模式扩散，违 CLAUDE.md Security） | minor | 兜底删除，环境变量必填；**⚠️ 历史提交仍含旧值，待口令轮换**（见 §七.3） |

### 5.2 已声明不修（理由）

| 发现 | 理由 |
|---|---|
| `Mysql57SourceReader` 三方法接线无直接测试（IT 均绕过 reader 类） | reader 经 `@DataSource(SLAVE)` 路由需 RuoYi Spring 上下文，单测代价高；接线错误会被 D5/D8 真实消费方立即暴露。**随 D5 消费方出现补直调 IT** |
| 「记日志」行为无断言（ListAppender） | 日志是可观测性辅助非正确性契约；坏行拦截本身已断言。若后续需要日志 SLO 再补 |
| `corruptedTimes` 以 `parse()==null` 过滤存在 oracle 循环 + `ThreadLocalRandom` 破坏收缩 | 具体反例（not-a-time/不可能日期/尾裸点）已由锚点独立兜住；属性价值重心在「不抛异常」。已知局限，如实登记 |
| 属性 toRaw 未生成 T/衬垫形态 | 随 canonical 收窄，此类形态已是坏行——`T分隔与空白衬垫_按坏行处理` 锚点确定性覆盖 |

---

## 六、已知限制与后续工作

1. **测试库口令轮换待办**：`syth7777` 已入 git 历史（D1 起 3 文件，本次止血但未清史）。建议与 D5 部署前一起轮换（`ALTER USER`），旧值作废即无害。
2. **save_time 真实毫秒形态**：样例库以 3 位毫秒 + 少量 2 位（`.11`）出现；canonical 契约按 1~9 位容变宽。若现场出现 T 分隔（当前未见过），闸门将按坏行记日志跳过——属可接受行为（源系统格式漂移应显式暴露而非静默吞）。
3. **pre-commit 绕过面**：`git commit --no-verify` 可跳过检查点（git 机制使然）；CI/部署侧门禁后补。
4. **hook 机器绑定**：`JAVA_HOME` 兜底硬编码本机 JDK17 路径，`VQMS_JDK17` 环境变量可覆盖；跨机开发时需设置。

---

## 七、相关文件

| 类型 | 路径 |
|---|---|
| 闸门/接线 | `ruoyi-vqms/src/main/java/com/ruoyi/vqms/source/reader/SaveTimeGate.java`、`Mysql57SourceReader.java`、`SaveTimeFilter.java` |
| 测试 | 同包 `SaveTimeGateTest.java`、`SaveTimeGatePropertyTest.java`、`D4GateScenarioIT.java` |
| 检查点 | `.githooks/pre-commit`（启用：`git config core.hooksPath .githooks`） |
| 证据（原始 XML） | `docs/测试/证据/D4_SaveTimeGateTest_surefire_2026-08-21.xml` 等 3 份（字节原样，修复后终版 run） |
| 规约依据 | v5.0 L625/L632/L155、测试方案 §2.1/§4.4 |

---

*报告生成于 2026-08-21 16:15，由 Claude Code 自动生成。*
