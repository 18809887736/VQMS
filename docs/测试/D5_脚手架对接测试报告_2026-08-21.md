# D5 测试报告 — RuoYi 脚手架对接（控制器 + 菜单/perms + 前端契约对齐）

> **日期**: 2026-08-21 17:00
> **基准文档**: `docs/项目规划_v5_0.md` §10.1 端点清单 / §10.2 分页导出限量 / §9.3 前端阶段 / §12.1 D5 行；`docs/测试/VQMS_测试方案_v5_0.md` §2.1（D5 不 TDD，验收断言）/ §4.6
> **测试环境**: L0 本机 + L1 直连 `10.0.0.9:13306/ry_vqms`（root，密码走 `MYSQL_ROOT_PASSWORD` 环境变量必填）
> **Commit**: `85a1a82`（对接主体）/ `2e03eec`（对抗验证吸收）

---

## 一、执行摘要

| 指标 | 数值 |
|---|---|
| L0 | `D5PermsConsistencyTest` 3 用例 ✅ |
| L1 控制器 | `D5ControllerIT` 6 用例（standalone MockMvc + 真实 Service + SourceReader 桩）✅ |
| L1 菜单 | `D5MenuIT` 4 用例（真实库应用 `vqms_menu.sql`）✅ |
| 回归 | D2 `D2BusinessLogicIT` 7/7 ✅（`selectList(busbarNum)` 签名变更后） |
| 合计 | **13 例全绿 + 回归 7/7** |
| pre-commit | 两次提交均实跑 L0（vqms+admin 两模块）通过 |

运行配方（复现）：

```bash
cd RuoYi-Vue-springboot3
JAVA_HOME=<JDK17> mvn -pl ruoyi-admin -am test -Dtest='D5*' -Dsurefire.failIfNoSpecifiedTests=false   # L0 部分无需凭证
MYSQL_ROOT_PASSWORD=<...> 同上 -Dtest='D5ControllerIT,D5MenuIT'                                        # L1 需主库凭证
MYSQL_ROOT_PASSWORD=<...> mvn -pl ruoyi-vqms -am test -Dtest=D2BusinessLogicIT -Dsurefire.failIfNoSpecifiedTests=false
```

凭证矩阵：管理侧（D2/D5 三 IT）= `MYSQL_ROOT_PASSWORD` @ `13306/ry_vqms`；外部源侧（D1/D4 两 IT）= `VQMS_AVC_TEST_USER/PASSWORD` @ `3306/qheatavchisdb`。

---

## 二、交付清单

| 层 | 内容 |
|---|---|
| 控制器（`ruoyi-admin` `web.controller.vqms`，5 个） | threshold 完整 CRUD+export（真实 D2 Service，逻辑 FK 校验生效）；vqms_busbar/list 下拉（行带 vGrade）；curve/list 真实数据（经 D1 reader + D4 闸门，31 天/500 行限量）；stats/{daily,monthly,yearly} + avc/{runtime,regulation} 搁置轨空壳（list 空表 200、export 显式报搁置轨） |
| 菜单/权限（`sql/vqms_menu.sql`） | avc perms 冒号段对齐 §10.1；增补 `vqms:vqms_busbar:list` 五处 F（2037~2041）+ role 2 全量绑定 |
| 前端对齐 | 5 个 api URL（stats//avc/ 斜杠式）、5 页导出 download URL、curve 参数名 beginTime→startTime、threshold 页字段对齐 D2 契约、avc 两页导出按钮 perms、删除 5 个死详情函数、4 页旧路径注释改正 |
| 域对象 | `VqmsBusbar`/`VqmsBusbarGroup` 补 `@JsonProperty("vGrade")`（Jackson 默认序列化 `vgrade` 与前端契约不符——IT 抓出）；`VqmsBusbarThreshold` 补 `@Excel` |
| Service/Mapper | threshold `selectList(busbarNum)` 过滤参数（controller 母线编号筛选生效） |

---

## 三、验收断言明细（测试方案 §4.6）

**L0 `D5PermsConsistencyTest`（3）**——四方防漂移：

| 用例 | 断言 |
|---|---|
| 权威清单覆盖 | §10.1 全部 18 个 perms 与 5 控制器 `@PreAuthorize` 反射提取值**双向相等**（多出/缺失均红） |
| 菜单 SQL 一致 | 控制器 perms ⊆ `vqms_menu.sql` perms |
| 前端不漂移 | 7 页 `index.vue` 内 `vqms:*` perms ⊆ §10.1 清单（对抗验证吸收后新增） |

**L1 `D5ControllerIT`（6）**：

| 用例 | 断言 |
|---|---|
| threshold_增查改删闭环 | list 前置空 → add → list=1 → getInfo 字段回读 → edit → remove，全程 `{code:200}`，行带 `thresholdId` 契约 |
| threshold_非法FK被Service层拒绝 | busbarNum=999999 → `{code:500}`（D2 逻辑 FK 校验经控制器路径生效） |
| busbar_下拉含vGrade | rows[0].`vGrade` 存在（@JsonProperty 修复的直接断言） |
| curve_分页与数据契约 | total=3、pageSize=2 取 2 行、highSV/lowSV/saveTime 字段回读 |
| curve_范围超31天被拒 | §10.2 限量落地，`{code:500}` |
| 搁置轨端点_空表与显式拒绝 | 5 个 list 端点 `{code:200,total:0,rows:[]}` |

**L1 `D5MenuIT`（4）**：7 个页面菜单（C）落库且 perms/component/status 正确；component 路径在 `RuoYi-Vue3/src/views/` **真实存在**（10 处 F+7 处 C 全校验）；11 个按钮 perms 落库（busbar=5 处）；**2000~2099 全部菜单绑定角色**（不存在孤儿菜单——对抗验证吸收后新增）。

---

## 四、对抗验证记录（3 视角，2026-08-21）

结论 **PASS / FAIL / FAIL**，18 findings。**三个 blocker 全在前端契约错位**——后端与测试全绿掩盖了页面对不上后端的事实，恰好证明「全绿≠前端调通」：

### 4.1 已修复（`2e03eec`）

| 发现 | 严重度 | 修复 |
|---|---|---|
| threshold 页消费 `row.id/lowLimit/highLimit`，后端契约是 `thresholdId/criterionType/toleranceV/effectiveFrom/effectiveTo`——列表三列恒空、改删必 404、新增必败（表单缺 effectiveFrom 非空列） | **blocker** | 页面表格+表单+校验规则全量对齐 D2 契约 |
| curve 页发 `beginTime`，控制器要 `startTime`——每次查询必进 catch 走 mock 兜底，真实端点永远打不到 | **blocker** | 参数名对齐 |
| 5 页导出 `download('vqms/{daily,...}/export')` 与后端 `/vqms/stats/*/export`、`/vqms/avc/*/export` 不匹配——全部 404，推翻「显式报搁置轨而非 404」设计 | **blocker** | 5 处 URL 对齐（threshold 原本就对） |
| 2037~2041 母线 F 授权未进 role 2 授权块——非 admin 五页母线下拉将 403 | minor | SQL 补 5 行 + MenuIT 增孤儿菜单断言 |
| 前端 perms 无防漂移护栏（手工对齐） | minor | L0 增前端扫描用例 |
| 5 个 api 文件死详情函数指向不存在端点 | nit | 删除 |
| 4 页 busbar 注释写旧路径 `/vqms/busbar` | nit | 注释改为 `/vqms/vqms_busbar/list` |

### 4.2 声明不修（如实登记，转后续里程碑）

| 项 | 处置 |
|---|---|
| **perms「生效」= 结构保证非行为验证**：standalone MockMvc 无 Security 链，`@PreAuthorize` 未被求值，401/403 拒绝路径零覆盖。当前依据 = RuoYi 全局 `@EnableMethodSecurity`（已核 SecurityConfig）+ L0 注解完备 + L1 菜单落库三重传导 | **D6 L3 冒烟补一条 403 断言**（webAppContext+springSecurity 或真实登录） |
| **前端「调通」= 契约静态对齐 + 后端 IT，无浏览器端到端**：L3 前端冒烟排在测试方案里程碑 2 | D6 随真实数据接入一并冒烟 |
| §10.2 导出 10 万行上限未落地（唯一真实导出=threshold 管理表，量级极小）；pageSize 500 仅落 curve | 随 S2/S3 统计导出解封一并落地 |
| threshold 页 vGrade 筛选控件静默失效（需 join busbar） | D6 补（列表行也不带 vGrade，join 后一并解决） |
| SLAVE 数据源未接线（application-druid.yml 出厂值，DruidConfig 空 catch 静默回落 MASTER）——真部署 curve 运行期 500 而非启动期报错；`@DataSource` AOP 路径零测试覆盖 | 部署 L5 验证承接；.env 驱动 slave 配置随部署落地（改原生 DruidConfig 违反「不修改原生模块」，不动） |
| curve 页图例承诺 average_SV 系列——源数据废值不映射（CLAUDE.md 硬约束），接真后无源 | D6 改页面文案去 average 系列 |
| daily 页 `busbarNum === '0'` 严格比较，接真（Long）后恒 false | D6 统一类型 |
| 测试方案 §3 库名/变量名与实际漂移（`ry_vqms_test` 每轮重置 vs 实际 `ry_vqms` 共享+行级自清理；示例变量名不符） | **待 Leo 拍板**：按实况勘误 or 建独立测试库 |

---

## 五、已知限制与后续工作

1. **口令轮换待办扩大**：D5 又出库一处——D2 IT 主库 root 密码兜底（历史值在 git 历史）。现共两枚待轮换：主库 root（`13306`）+ 测试库 root（`3306`，D1 起）。
2. **`D5MenuIT` 不得对部署主库运行**：其幂等逻辑会 `delete sys_menu/sys_role_menu where 2000~2099` 再重放——`ry_vqms` 转生产库后此 IT 必须改打独立测试库（与 §3 漂移项同批解决）。
3. **busbarNum 可空**：curve/list 不传母线读双母线全量（31 天约 8.9 万行）——前端级联总会带值，暂不强制。

---

## 六、相关文件

| 类型 | 路径 |
|---|---|
| 控制器 | `ruoyi-admin/src/main/java/com/ruoyi/web/controller/vqms/Vqms{Curve,Stats,Avc,Threshold,Busbar}Controller.java` |
| 测试 | 同包 `D5{PermsConsistencyTest,ControllerIT,MenuIT}.java` |
| 菜单 | `sql/vqms_menu.sql`（2026-08-21 D5 修订 + 2037~2041） |
| 证据 | `docs/测试/证据/D5_*.xml` 3 份（终版 run，6+4+3） |
| 规约 | v5.0 §9.3/§10.1/§10.2/§12.1、测试方案 §2.1/§4.6 |

---

*报告生成于 2026-08-21 17:00，由 Claude Code 自动生成。*
