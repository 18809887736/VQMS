# D6 测试报告 — 前端阶段 3（真实数据接入，无 mock）+ L3 真栈冒烟

> **日期**: 2026-08-22 09:40
> **基准文档**: `docs/测试/VQMS_测试方案_v5_0.md` §4.9（D6 条目）/ `docs/项目规划_v5_0.md` §9.2 / §9.3 / §10.2
> **测试环境**: L3 真栈 = 本机 backend（`druid,local` profile → 10.0.0.9 栈：13306 master / 3306 SLAVE / 16379 redis）+ vite dev + 浏览器（Claude Browser）；L0/L1 既有矩阵回归
> **Commit**: `377baa3`（D6 主体）/ `41936ee`（对抗验证吸收）

---

## 一、执行摘要

| 指标 | 数值 |
|---|---|
| L3 冒烟 | **7/7 页通过**（登录/菜单 + curve/daily/monthly/yearly/avc-runtime/avc-regulation/threshold） |
| 回归 | **74/74 全绿**（D1~D5 全矩阵 + D6 新增 avc 反向断言 1 例） |
| 无 mock 核查 | ✅ 前端 vqms 代码零 mock/零硬编码数据（对抗验证 no-mock 视角 PASS） |
| 对抗验证 | PASS/FAIL/PASS，12 findings：1 blocker + 6 minor 当日修复，其余声明 |

## 二、L3 冒烟清单与结果（测试方案 §4.9「人工冒烟清单」）

| # | 项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 登录（VQMS 品牌、无验证码）→ getRouters 加载 VQMS 菜单 | ✅ | 会话记录 + network |
| 2 | **curve 页真实曲线**：时间范围 2026-03-15 10:00~11:00 + 母线 0 → ECharts 渲染 high_SV/low_SV 双线，33 点（含 `09:59:57.100` 边界行经 D4 闸门正确归入 10:00 窗） | ✅ | 截图入库 `证据/D6_曲线页真实数据冒烟_2026-08-22.jpg` + API 响应 |
| 3 | 母线下拉走后端 `/vqms/vqms_busbar/list`（5 页；「220kV 东母线 (0)」来自 API 非本地种子） | ✅ | network + DOM |
| 4 | daily/monthly/yearly：API 空态「暂无数据」+ 电压等级列存在 + 筛选可用 | ✅ | DOM 断言 |
| 5 | avc-runtime/regulation：**厂级口径无母线/电压等级筛选**（§4.9 反向断言，另有 L0 静态断言守护） | ✅ | DOM（筛选区空） |
| 6 | **「独立指标」强制标注**（§9.2 avc-regulation 验收项）：el-alert 前端内置、无条件渲染，文案与考核单价条款自洽 | ✅ | DOM 断言 |
| 7 | threshold 页：种子行 + vGrade 列（dict-tag「220kV」）+ 容差 1.000 拍板值 + **页面级增删闭环**（新增成功→删除→种子恢复原状）+ 编辑回填显示母线名 | ✅ | DOM + SQL 核对 |
| 8 | 匿名访问 `/vqms/curve/list` → 401（业务码） | ✅ | curl |
| 9 | **SLAVE 数据源真实路由实证**（D5 声明的接线零覆盖盲区闭合） | ✅ | 33 行真实数据返回 |

> 403（有权登录但无对应 perm）未测——需专用无权角色，登记待办（§五.4）。

## 三、交付明细

**前端**：5 页母线下拉切 API（类型归一 String 对齐字典）；curve 页去 mock（真实 rows 渲染、average_SV 废值系列移除、y 轴 scale 自适应使 222~224kV 波动可见、ECharts 单例 + resize 监听卸载移除）；threshold 页 vGrade 筛选/列/join 落地 + 表单母线下拉 + 编辑回填归一；avc 两页独立指标标注；daily/monthly/yearly busbarNum 类型统一。

**后端**：`VqmsBusbarThresholdMapper` join `vqms_busbar` 输出 vGrade（列名 `t.` 限定防歧义）+ vGrade 过滤参数；`application-local.yml`（纯 env 占位、可入库；root 仅本地 dev 注记，部署须 vqms_app §11）；vite 代理 8080→7000 修正（本机 dev 直连）。

**测试**：`D5PermsConsistencyTest` 增 avc 两页无电压等级筛选反向断言（§4.9，fail-closed）。

## 四、对抗验证记录（3 视角，2026-08-22）

结论 **PASS / FAIL / PASS**，12 findings：

### 4.1 已修复（`41936ee`）

| 发现 | 严重度 | 修复 |
|---|---|---|
| monthly/yearly `busbarNum === '0'` 严格比较未随 daily 同步修——后端 Long→number 序列化下主母线恒错标「副母线」，stats 解封首日必现 | **blocker** | String() 归一 |
| threshold 编辑回填 busbarNum number vs el-option string——下拉显示裸编号 | minor | 回填 String 归一（复验：显示「220kV 东母线 (0)」✓） |
| curve ECharts 每查询重复 init + resize 监听无界累积（离页后仍触发） | minor | 单例 init + 卸载移除（复验：两次查询 canvas 仍 1 ✓） |
| curve UI 文案「消费模式随 D6 定稿」——内部里程碑术语泄漏进产品文案且已失约 | minor | 中性表述 + 行动指引 |
| `application-local.yml` master 用 root 无账号约束注记 | minor | 注记「仅本地 dev；部署须 vqms_app」 |
| D5 技术债锚点「随 D6 定」已过期 | minor | D5 报告 §五.3 锚点更新（见 4.2） |

### 4.2 声明与登记

| 项 | 处置 |
|---|---|
| **curve 消费模式 = 临时口径，非定稿**：单页 pageSize=500 + 截断提示；未做聚合/分窗，PageHelper total 仍触发 COUNT 全扫（D5 点名成本，临时接受）。**D5 技术债不关闭**——且截断提示只需 hasMore 信号不需精确 total，原「total vs LIMIT 下推」两难已被化解：收口路径 = SQL LIMIT 501 探测 + hasMore，锚点改挂**真实数据回放（测试方案 §5.1）或统计轨解封，先到者** | D5 报告 §五.3 已更新注记 |
| curve x 轴显示原始毫秒时间戳（如 `09:59:57.100`），D3 取整在显示链路未做——当前无害（有序、单母线 1 行/分钟），逐分钟聚合展示上线时在 controller 出参统一取整 | 登记 |
| avc-runtime 标注提及「罚款」而该页暂无罚款列（随统计上线出现）——无条件展示比 §9.2「同屏即带」更严格，不算违规 | 已注记 |
| §9.2 通俗版标注措辞为前端自拟合理解读，通俗版定稿时回核 | 待定清单既有项 |
| avc 反向断言整文件 contains 扫描，注释提及 vGrade 也会红 | fail-closed 有意，保持 |

## 五、已知限制与后续工作

1. **403 断言未测**：匿名 401 已验；403 需专用无权角色——随 D7 或下次冒烟补。
2. **消费模式债**（见 §4.2）：LIMIT 501 + hasMore 收口，锚点已写死。
3. **L3 自动化**：§4.9 定位「可选增强」，阶段 3 图表交互铺开后 Vitest/Playwright 转「应做」——触发条件已近，D7+ 排期。
4. **两枚口令轮换**待办延续（主库 @13306 + 测试库 @3306）。
5. **测试方案 §3 库名漂移**拍板项延续（本报告口径同 D5）。

## 六、相关文件

| 类型 | 路径 |
|---|---|
| 前端 | `RuoYi-Vue3/src/api/vqms/busbar.js`（新）+ 7 页 `index.vue` + `vite.config.js` |
| 后端 | `VqmsBusbarThresholdMapper.xml`（join vGrade）/ domain / service / `VqmsThresholdController` / `application-local.yml` |
| 测试 | `D5PermsConsistencyTest`（+avc 反向断言）；回归 74/74 证据沿用各里程碑 `docs/测试/证据/` |
| L3 证据 | `docs/测试/证据/D6_曲线页真实数据冒烟_2026-08-22.jpg` |
| 冒烟配置 | `.claude/launch.json`（vite 命令式） |

---

*报告生成于 2026-08-22 09:40，由 Claude Code 自动生成。*
