# D7 测试报告 — 判定整定参数（CRUD + 值域/锁定双层校验 + Redis 缓存）

> **日期**: 2026-08-22 10:15
> **基准文档**: `docs/项目规划_v5_0.md` §6.2.5 / §12.1 D7 完成标准 / §10.1；`docs/测试/VQMS_测试方案_v5_0.md` §4.6 D7 条目（两段断言 + CHECK 分层候选）
> **测试环境**: L1 直连 `ry_vqms` @13306（`MYSQL_ROOT_PASSWORD`）+ 真实 Redis @16379（`VQMS_REDIS_PASSWORD`，IT 内手工 Lettuce + FastJson2 序列化对齐真部署）+ 真栈冒烟
> **Commit**: `b316401`（主体）/ `50b96e3`（对抗验证吸收）

---

## 一、执行摘要

| 指标 | 数值 |
|---|---|
| L1 | `D7JudgeParamIT` **9/9** ✅ |
| 回归 | D5 三件 15/15 + D2 7/7 ✅ |
| 真栈冒烟 | 403 行为断言（D6 遗留闭环）+ PUT→Redis 即时新值 + 锁定行友好拒绝 + 页面锁定态 ✅ |
| 对抗验证 | FAIL/PASS/FAIL，13 findings：1 blocker + 5 minor 修复，其余声明 |
| 完成标准 | ✅ `/vqms/judgeParam` CRUD 可用；✅ 值域校验拦截（Service + DB CHECK 双层）；✅ 改值后缓存刷新下次读取即生效 |

## 二、值域/锁定双层防线设计（测试方案 §4.6 分层候选落地）

按 §2.1「约束优先于测试逻辑」下沉 MySQL 8.4 CHECK（`sql/vqms.sql` + 增量迁移 `sql/migrations/2026-08-22_d7_judge_param_checks.sql` 双入库）：

- **`ck_value_range`**：行本地值域（min/max 任一 NULL 则不设限——自定义行允许开放值域）
- **`ck_locked_rows`**（蕴含式）：非该键恒真，锁定键钉值+值域列——`t_econ=5` 写死、分档阈值 1/5 政策值、`t_fast` 只钉值域列 [1,4] 不钉值（可整定）
- 跨行 `t_fast < t_econ` 由「t_econ 钉 5 ∧ t_fast∈[1,4]」两行本地约束传导等价保证（CHECK 行本地写不了跨行）
- Service 层保留友好报错；**已知边界**：CHECK 不拦 DELETE，必需行删除仅 Service 层拦

## 三、断言清单（D7JudgeParamIT 9 用例）

| 用例 | 断言 |
|---|---|
| 种子四行在 | t_fast=4 / t_econ=5 / 阈值 1·5；行数 ≥4（自定义行允许） |
| Service 层拒绝非法 t_fast | 0/5/6 → IAE（值域 + 跨行） |
| Service 层拒绝锁定行修改与必需删除 | t_econ/阈值改值、t_fast 删除 → IAE |
| **绕过 Service 直改 DB 被 CHECK 拒** | mapper 直 update t_econ=6 → ERROR 3819（§4.6 DB 段断言） |
| **缓存即时生效** | 改 t_fast 4↔3 → `getInt` 立即新值 + Redis 键存在（完成标准核心） |
| controller CRUD 契约 | list 4 行 / 非法 PUT → code 500 友好拒 |
| **自定义键增删回路** | insert d7_it_probe=7 → getInt=7 → delete（blocker 回归锁） |
| **重复键友好拒绝** | insert t_fast → IAE「参数键已存在」非裸 DuplicateKeyException |
| **绕过 Service 置 NULL 值域被 CHECK 拒** | JDBC 原生 `update set value_min=null` → ERROR 3819（`<=>` 语义回归锁） |

## 四、真栈冒烟（L3）

| 项 | 结果 |
|---|---|
| **403 行为断言**（D6 遗留待办闭环）：无权测试用户（专用无菜单角色）登录 → `/vqms/judgeParam/list` → `{code:403}`；admin → 200+4 行 | ✅（测试用户已清理） |
| PUT t_fast=3 → **Redis 键即时变 3**（缓存刷新端到端） | ✅ |
| PUT t_econ=6 → 端到端友好拒绝 | ✅ |
| judgeparam 页面：无缝拼接提示 + 4 行种子 + 锁定行 tag/编辑禁用 | ✅ |

## 五、对抗验证记录（3 视角，2026-08-22）

结论 **FAIL / PASS / FAIL**，13 findings。

### 5.1 已修复（`50b96e3`）

| 发现 | 严重度 | 修复 |
|---|---|---|
| **`ck_locked_rows` 初版误写键白名单**（OR 链要求谓词 TRUE）——任何新 param_key 的 INSERT 四支全 false 被拒：Add 端到端死路、「自定义行可删」成不可达死代码、堵死 v4.0 A6 扩展路（乙档阈值加行）。初版 IT 6 用例无一走 insert，全绿掩盖 | **blocker**（两视角独立命中） | 重写为蕴含式；IT 补 insert→getInt→delete 回路作回归锁。修复过程中又实测抓到 **MySQL 三值逻辑旁路**：普通 `=` 遇 NULL 求值 NULL，`FALSE OR NULL ≠ FALSE` 放行——值域列置 NULL 绕过锁定（复现过 t_fast=99 得逞一瞬，已修数据）——比较全部改 NULL 安全等值 `<=>`，JDBC 原生旁路用例锁定 |
| 手工 ALTER 无入库留存（commit message 之外无处可查） | minor | `sql/migrations/2026-08-22_d7_judge_param_checks.sql` 入库（含非幂等注记）；vqms.sql 同步 |
| 缓存无 TTL：写 DB 后覆盖缓存前崩溃 → 陈旧值永不收敛；update rows==0 + 并发 delete → 已删键复活缓存 | minor | 写穿 + 24h TTL 兜底收敛；rows==0 → evict + 抛「参数不存在」 |
| javadoc「写后 evict」与实现（覆盖）不符 | minor | 措辞对齐「写后覆盖（带 TTL）」，与 §6.2.5 字面 CacheEvict 的差异在本报告声明：行为契约「改值后下次读取即新值」等价且省一次回源，并发乱序永久脏读风险由 TTL 封顶 24h |
| 重复键 insert 裸抛 DuplicateKeyException（MySQL 报文直达用户） | minor | insert 前 `countByKey` 查重友好报错 |
| getInt 不看 status（停用行照常供值，语义未定义） | nit | 停用行显式抛「判定参数已停用」 |
| warmup 日志记常数而非实际行数 | nit | 实际 size |
| AfterAll 无条件把共享库 t_fast 归一为 4——现场合法整定 1/2 会被测试静默覆写 | minor | 只归一 {3,4}，越界保留 |
| D2 断言 judge_param ==4 且不清理该表（与 add 解封后成跨模块脆弱耦合）；D2 上下文被新 Service（依赖 RedisCache）加载失败 | minor | 断言 ≥4；TestConfig 排除 `VqmsJudgeParamService` |

### 5.2 声明不修

| 项 | 理由 |
|---|---|
| create_by/update_by 全链路不写（表审计列恒空） | 操作审计走 `sys_oper_log`（@Log 已挂）足够；行级列属 RuoYi 生成器残留 |
| 业务校验用 IAE 而非 RuoYi 惯例 ServiceException（合法拒绝打 ERROR 堆栈日志噪音） | 行为正确；全局换异常类型属跨里程碑重构，随 D8/D9 顺手统一 |
| 迁移脚本非幂等（MySQL 8.4 无 DROP CHECK IF EXISTS） | 注记已写明跳过规则；一年一两发的迁移脚本不值得引存储过程 |

## 六、相关文件

| 类型 | 路径 |
|---|---|
| Service/Controller | `VqmsJudgeParamService.java`（新）/ `VqmsJudgeParamController.java`（新） |
| DDL | `sql/vqms.sql`（CHECK）+ `sql/migrations/2026-08-22_d7_judge_param_checks.sql`（增量） |
| 前端 | `api/vqms/judgeParam.js` + `views/vqms/judgeparam/index.vue` + 菜单 2042-2045 |
| 测试 | `D7JudgeParamIT`（9）+ `D5PermsConsistencyTest`/`D5MenuIT` 扩展 + D2 适配 |
| 证据 | `docs/测试/证据/D7_JudgeParamIT_surefire_2026-08-22.xml` |

---

*报告生成于 2026-08-22 10:15，由 Claude Code 自动生成。*
