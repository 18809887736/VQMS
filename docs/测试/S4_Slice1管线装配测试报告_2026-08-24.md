# S4 测试报告 — Slice 1：RegulationPipeline 装配体（调节合格率管线）

- **日期**：2026-08-24
- **口径基准**：v5.0 §8.8.2（三阶段装配体，本身无算术只有编排与路由）；§8.8.4 S4；§12.2 S4（第一版按日期区间幂等重算）
- **层级**：L0 编排路由 mock 全测（真 judge）+ 配置缺口 DDL 补缺
- **结果**：✅ 新增 6 用例全绿；全模块回归 202/202（vqms 198 + admin 4）；⚠️ 26 场景管线集成冒烟随 IT 凭据批

## 一、交付物

| 项 | 内容 |
|---|---|
| `statistics/RegulationPipeline` | 三阶段装配体：ledger 指令流 → 门控 → judge → 免考后置 / Undecodable→INVALID 占位 → 写 `vqms_regulation_cmd`（uk 幂等 upsert，重算即覆盖）；曲线窗口批量预取（区间两端放宽 t_econ 分钟）；主母线解析走 yc4001 指示点阶跃保持 + 组兜底（§6.2.2 首个消费方）；参数经 D7 缓存服务读取（t_fast/t_econ 运行时生效） |
| 配置补缺（DDL+种子+迁移） | ① `vqms_busbar.realtime_yc_num` 列（t₀ 实时电压点位载体；种子 bn0/bn1=4002 合成口径，真实候选 yc8/yc14 待核对）；② `vqms_yc_point_map` 补种 501 免考旗（gate_enabled=0——免考应用信号非门控） |
| `VqmsRegulationCmd` + Mapper | 明细表写入面（upsertBatch ON DUPLICATE KEY UPDATE，S2 DDL 的消费方就位） |
| `YxSignalReader.heldDecimalValue` | 接口加十进制读取变体（实时电压 234.25 kV 不能被 Integer 截断；开关量仍走 heldValue），impl 抽公共行选取 |

## 二、默认口径声明（待 Leo 确认，改路由各一行）

| # | 口径 | 默认实现 |
|---|---|---|
| ① | stub 护栏（§8.8.5 两选项） | **拒写**：isStub() 时整批不落正式统计表 + WARN（PipelineResult.stubBlocked=-1 哨兵），比「STUB 落库+前端标注」彻底防假数字 |
| ② | 门控拦截指令分母归属 | **不进分母**、gateSkipped 计数披露（若拍板「进分母」，改路由为落 INVALID 行即可） |
| ③ | 免考旗缺失处置 | **从严按 0 计罚**（对齐 S3 矛盾态从严先例）——⚠ UNVERIFIED-口径：yx501 读取失败属第三类失效分轨（随 #3/#5），选套拍板时一并复核 |
| ④ | 策略处置 disposition | 本切片恒 NULL（选套前「只记不判」）；S5 选套后接 DataUnavailabilityPolicy.evaluate |

## 三、用例清单（6）

stub 拒写零交互 / 正常判定落账断言全列（含快照与 disposition=NULL）/ PENALIZED+yx501=1→EXEMPTED / Undecodable 占位带归因 / 门控拦截披露不落账 / 脏时间跳过。

## 四、证据与待办

| 文件 | 内容 |
|---|---|
| `docs/测试/证据/S4_Slice1_全模块回归198_2026-08-24.log` | `-pl ruoyi-vqms,ruoyi-admin -am test`：198+4 全绿 BUILD SUCCESS |

**Slice 2/3 待做**：投运率日记账作业（信号逐分钟分类→计数→写回）；Quartz sys_job 接线 + 三级 rollup 提升 mapper + 幂等重算编排。26 场景管线冒烟随 IT 凭据批跑。
