# S4 测试报告 — Slice 2：投运率日记账（RuntimePipeline）

- **日期**：2026-08-24
- **口径基准**：正式 v1_0 §1.3~§1.5（时间记账）；测试方案 §5.0 组件矩阵；§12.2 S4 幂等重算
- **层级**：L0 编排 mock 全测（真分类器/真统计纯函数）
- **结果**：✅ 新增 6 用例全绿；全模块回归 208/208（vqms 204 + admin 4）

## 一、交付物

| 项 | 内容 |
|---|---|
| `statistics/RuntimePipeline` | 单日重算编排：五信号整日预取（回看前一日，跨零点保持）→ 内存阶跃保持 → 逐分钟 `RuntimeMinuteClassifier` → `RuntimeMinuteCounts` → `RuntimeStatistics.summarize` → 写 `vqms_runtime_daily`（同日覆盖 upsert） |
| `VqmsRuntimeDaily` + Mapper | S3 日记账写入面（upsert ON DUPLICATE KEY UPDATE） |
| `YxSignalReaderImpl.pickLatest` 提为 public | 批量作业复用行选取语义（接口不变） |

**零并网基数语义**：rate/shortfall/penalty 存 NULL（区别于真 0%，沿 DDL 注释口径）；容量缺失 → 率照算、罚款 NULL。

## 二、默认组合口径声明（待 Leo 确认/随真实点号核对复核）

| 口径 | 默认实现 |
|---|---|
| 并网 | max(yc511, yc512) ≥ 10——任一母线并网即厂级并网（points.yaml 值语义） |
| 退出原因 | max(yc521, yc522)——任一侧非电网证据(2)即从严计罚、两侧均电网(1)才免责；缺失按 0（矛盾态从严由分类器承接） |
| AVC 投退缺失 | 从严按 0（退） |
| 容量 | busbar_group 各组和（决策⑤ 厂级口径） |

点号常量集中于 RuntimePipeline 一处（3009 撞号警示，真实现场核对后一处改）。

## 三、用例清单（6）

全投运日 100% 零罚款 / **混合退出手算向量**（1320 投运 + 120 非电网退出 → rate 91.667%、shortfall 7.333 点、罚款恰 4.4 分 = 7.333×30万千瓦×0.02）/ 零并网日率列 NULL / 容量缺失罚款 NULL 率照算 / 两母线原因组合从严 / 并网 ≥10 边界恰 10 即并网。

过程记录：首轮手算向量错两处（avc 信号整日为退；缺额误用 100% 基准而非政策阈值 99%）→ 测试当场抓出 → 修正期望后 208/208。产品代码零改动。

## 四、证据与待办

| 文件 | 内容 |
|---|---|
| `docs/测试/证据/S4_Slice2_全模块回归208_2026-08-24.log` | `-pl ruoyi-vqms,ruoyi-admin -am test`：204+4 全绿 BUILD SUCCESS |

**Slice 3 待做**：Quartz sys_job 接线（调节区间重算 / 投运率单日重算两个任务方法）+ 三级 rollup SQL 从 IT 提升 mapper + 月/年作业。
