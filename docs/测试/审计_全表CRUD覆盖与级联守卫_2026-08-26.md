# VQMS 全表 UI CRUD 覆盖与删除级联守卫审计报告（2026-08-26）

> **方法**：三路对抗审计（每路含自我反驳）——①14 表 UI CRUD 覆盖矩阵 ②删除路径级联守卫枚举 ③规划语义核验（v5.0 §6.2/§10.1/§12 对照）。全部结论带 文件:行号 证据（审计工作流 wf_2defdfee-e9e，324K tokens）。
> **判定口径**：管理/配置类表必须有 UI 维护路径；统计/流水类无 CRUD 属设计（管线幂等写、纠错走区间重算）。

## 一、UI CRUD 覆盖矩阵（14 表）

| 表 | 判定 | 说明 |
|---|---|---|
| vqms_busbar | ✅ COVERED | 2026-08-26 c1245a8 补齐（页面/端点/菜单/写路径四项俱全） |
| **vqms_busbar_group** | ❌ **MISSING_UI（最优先）** | **Service+守卫+IT 全部建好却是死代码**——D2 实现了 insert/update/delete（含 main_indicator_yc_num FK 校验、删除前 busbar 引用 RESTRICT 守卫，v5.0 §6.2:173 规划强制项），但无 controller/菜单/页面，生产调用方为零。运行时被两条管线消费：resolveMainBusbar（判定窗口挂哪条母线）+ 罚款容量 rated_capacity_kw 求和——**指示点/兜底配错=判定静默挂错母线；容量配错=罚款静默算错**。既定维护事件（500kV 指示点补录、4001 换真号）目前只能手工 SQL。修法=补 controller+菜单+页面三件套复用现成 Service，边际成本极低 |
| vqms_busbar_threshold | ✅ COVERED | 阈值管理页（CRUD+导出+生效区间自动闭合） |
| vqms_judge_param | ✅ COVERED | 判定参数页（D7：CHECK 双层+锁定键+写穿缓存） |
| vqms_policy_param | ✅ COVERED | 策略参数页（apply/applyFreeform 专用写端点，逐行不开放属设计） |
| **vqms_yc_point_map** | ❌ **MISSING_UI（有前置拍板项）** | 全仓最欠账：mapper 全套含**零调用死方法 deleteByYcNum**（半成品痕迹），无 service/controller/UI。gate_enabled 置 1、3009 换号等既定维护事件只能裸改 DB 且无守卫无留痕。⚠️ **前置拍板**：运行时点号真相源在 Java 常量（投运率五信号 3009/511/512/521/522 + 免考旗 501 均不经本表）——**先定"点号真相源归表还是归代码"，再决定 UI 形态** |
| vqms_command_ledger | ✅ BY_DESIGN | 只增摘录底账（§6.2.6 Leo 拍板）；但见 §三.1 抓取编排缺口 |
| 六张统计表 | ✅ BY_DESIGN | 管线幂等 upsert + rollup；月度有读端点，**日/年粒度用户不可见属文档化过渡态**（等真实回放拍板页面语义） |

## 二、删除级联守卫矩阵

| 引用边 | 守卫 | 说明 |
|---|---|---|
| busbar ← threshold.busbar_num | ✅ RESTRICT（刚补） | Service 硬拦 + UI thresholdCount 预检双保险 |
| **busbar ← busbar_group.default_main_busbar_num** | ❌ **MISSING_GUARD（规划 FK 清单漏网）** | 删 busbar 0 后 group 0 的兜底号悬空 → resolveMainBusbar 兜底路径**裸返回**（指示点路径有 contains 校验、兜底路径没有——防御不对称）→ 空窗口整档判 INVALID **静默劣化统计**。修法：照抄 threshold RESTRICT 模式 + 兜底路径补成员校验作纵深防御 |
| busbar_group ← busbar.group_num | ✅ 守卫在 | 但组删除端点不存在——守卫为不可达代码（随 group UI 一并接线）；同一规则现存两份代码，接线时二留一 |
| threshold 删除下游 | ✅ 无引用方 | 条件：tolerance_v 若重获判定角色须重审（Open follow-ups 在案） |
| judge_param 删除 | ✅ 锁定键保护 | 小瑕疵：批量删循环无事务边界（RuoYi 通病，部分应用后整批报错） |
| policy_param | ✅ 无删除路径 | 清空=「只记不判」合法态（D9 设计本意） |
| ledger/统计表 | ✅ 应无删、确实无 | 纠错走区间重算 |

## 三、审计外新发现（按严重度）

1. **🔴 指令流水账抓取编排未接线**：D8 报告明言「抓取编排随 S4 Quartz」，但 S4 Slice3 的 VqmsStatsTask 只有三入口（recomputeYesterday/Range/RuntimeDay），**未调 ingest()**——流水账生产零触发，目前只能 IT 灌数。真实数据回放前必须补（sys_job 种子 + ingest 调用）
2. **🟡 两处过时文案**（新部署即误导）：sql/vqms_menu.sql:104 策略参数菜单备注仍写「甲乙丙丁，选套值留空待政策拍板」（四候选已退役）；sql/vqms.sql:239 仍写 vqms_policy_param「无 CRUD UI」
3. **🟡 规划 §6.2.1 滞后**：「统计按 busbar_num 落」与落地的厂级 DDL（date-keyed 无 busbar 列）矛盾——需勘误（不升版本号）
4. **🟡 max_staleness_minutes 死列**：group 表该列 domain/mapper 映射齐全但 resolveMainBusbar 完全不消费——规划 §6.2.2 定义了语义却无实现（漏实现或未声明预留，须拍板）
5. **🟡 年级 rollup 有写无读**：regulation_yearly/runtime_yearly 持续产出但无任何读端点（读侧仅 monthly）——与"日/年页面语义等真实回放拍板"同批
6. ⚪ 批量删除无事务边界、守卫规则双份代码（随 group UI 接线一并收敛）

## 四、建议行动清单（优先序）

| # | 动作 | 成本 | 触发条件 |
|---|---|---|---|
| 1 | 补 **default_main_busbar_num 反向守卫**（照抄 threshold RESTRICT 模式 + 兜底路径成员校验） | ~30 行 | 建议立即（静默劣化形态） |
| 2 | 补 **vqms_busbar_group 三件套**（controller+菜单+页面，复用死代码 Service；顺带收敛双份守卫、组下拉进母线表单） | 半天 | 建议立即（500kV 补录/换号在即） |
| 3 | 补 **ledger ingest 进 VqmsStatsTask** + sys_job 种子 | ~20 行 | 真实回放开工前必须 |
| 4 | 两处过时文案勘误 + 规划 §6.2.1 勘误 | 10 分钟 | 随手 |
| 5 | **yc_point_map 真相源拍板**（归表/归代码）→ 再定 CRUD 形态 | 拍板项 | 点号核对事件前 |
| 6 | 日/年读端点与页面语义 | 立项 | 真实回放后 |

---

> 审计执行：Claude Code（ox-alpha 会话），三路对抗工作流（wf_2defdfee-e9e）；本报告为审计合成，修复实施另行排期。
