# 执行记录：vqms_v_grade 字典补齐 + 菜单/配置核查（ry_vqms）

**日期**：2026-08-20（下午，紧随 [vqms_tables部署_2026-08-20.md](vqms_tables部署_2026-08-20.md)）
**来源**：`sql/vqms.sql` 第五节（部署定制）/ 第六节（VQMS 字典）+ `sql/vqms_menu.sql`
**操作人**：Claude（Leo 指令："把 ②③ 补进 10.0.0.9"）
**状态**：✅ 完成——核查发现 ③ 菜单与 ⑤ 验证码配置**早已在库**，实际仅补 ② 字典 4 行

---

## 背景

VQMS 库完整初始化 = 三类：① 6 张业务表（上午已部署）；② 字典 `vqms_v_grade`（`sys_dict_type`/`sys_dict_data` 数据行）；③ 菜单/权限（`sys_menu` 21 行）。`sql/vqms.sql` 为全量脚本（含与 ① 重复的建表段，全文件直跑会撞已存在表），只能抽段执行。

## 第 1 步：先核查，不盲跑（避免主键冲突）

用临时 mysql 客户端容器（`root@'%'` TCP 路线，同上午）查 `ry_vqms` 现状：

| 项 | 核查结果 | 结论 |
|---|---|---|
| ③ `sys_menu` 2000~2060 | **21 行全在**（2000 合格率统计 / 2010 曲线查询 / 2020 AVC 考核 / 2030 系统配置 四目录 + 7 页面 + 导出/阈值按钮，perms 齐全）——早前部署已入（dump 的 AUTO_INCREMENT=2037 即此） | **跳过不执行**（`vqms_menu.sql` 为裸 insert 固定主键，重跑必撞 duplicate key） |
| ⑤ `sys.account.captchaEnabled` | `false`（已关闭） | 已生效，跳过 |
| ② 字典 `vqms_v_grade` | `sys_dict_type` / `sys_dict_data` **均无行** | **缺失，本次唯一要补的** |

## 第 2 步：补字典（幂等段，整段直跑）

抽取 `sql/vqms.sql` L229~237（第六节全部：delete×2 + insert dict_type 1 行 + dict_data 3 行，自带幂等），本地 sed 截段 → stdin 经 ssh 直灌临时客户端容器执行（不在服务器落临时文件）。

## 第 3 步：验证 + Redis 缓存收尾

- 落库验证：`sys_dict_type` 1 行（电压等级）+ `sys_dict_data` 3 行（0=500kV/danger、1=220kV/primary、2=66kV及以下/info），中文完好 ✅
- Redis：`KEYS sys_dict:vqms_v_grade` 无 key → 无脏缓存，下次前端 `useDict` 直接读库，**未做任何删除/FLUSH**（避免误伤 token 等运行数据）。

## 结果汇总

| 类 | 本次动作 | 库内终态 |
|---|---|---|
| ② 字典 | 补插 4 行 | ✅ 1 type + 3 data |
| ③ 菜单/权限 | 核查确认已存在，未动 | ✅ 21 行（2000~2036） |
| ⑤ 验证码关闭 | 核查确认已生效，未动 | ✅ false |

**至此 `ry_vqms` 三类初始化数据全部就位**（6 表 + 种子 / 字典 / 菜单），D5（脚手架对接）与前端电压等级筛选的库侧前置已齐。

## 待办

- 无（本文件相关的库侧工作收口）。
- 提醒：菜单/权限行与仓库 `sql/vqms_menu.sql` 内容为同批生成（阶段 2），如日后该文件更新需同步库内（当前一致，未逐字段 diff 目录树结构）。
