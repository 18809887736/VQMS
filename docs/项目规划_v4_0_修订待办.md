# 项目规划 v4.0 —— 事后修订待办

> v4.0 已于 **2026-08-14** 提交，本文档不动已提交版，只记录事后核对发现的待修订项。
> 待修订原因详见同会话核实结论（对照 `docs/项目规划_v4_0.md` 原文 + `docs/AVC考核核心算法_草稿.md` §2）。
> 修订时按本清单逐条对照执行；**断言 10 已核实为伪问题，不修**（见 D 节）。

---

## A. 核心必改（4 项）

### A1. §8.2 stub 改确定性
- **位置**：§8.2（约第 319 行），`StubRegulationJudge` 描述。
- **现状**：原文「返回全 `QUALIFIED` 或**随机三态**」。
- **改为**：确定性 stub——按输入 hash 出确定结论（或固定全 QUALIFIED），并在返回里带 `stub=true` 标记。
- **原因**：随机返回让测试/集成不可复现，且会污染任何落库或缓存。注：后端 `ruoyi-vqms` 尚未落地，stub 还没真正运行，故当前是「潜在风险」而非已发生事故；改确定性仍是标准好习惯。

### A2. §8 RegulationJudge 重构为三阶段管线（最核心 · 论据已修正）
- **位置**：§8.2 接口契约（约第 305–321 行）。
- **现状**：接口 `judge(AvcCommand cmd, List<MinuteCurve> curveByMinute, JudgeParams params)` 输出 `VERDICT ∈ {QUALIFIED, PENALIZED, EXEMPT}`；§8.2（第 321 行）宣称「接口契约稳定……不破坏接口」。但**免考与门控数据在接口输入里没有通道**——算法定型要塞这两类数据时签名必然改，与「不动调用方」自相矛盾。
- **改为（三阶段管线）**：
  1. **门控前置过滤**（调用方做）：读 `yc_history` 的 AVC投退/远方就地 yx 点，排除 AVC 退出/就地模式时段的指令。**不进 judge**。
  2. **RegulationJudge 包络判定**：**从返回值移除 `EXEMPT`**，只出 `{QUALIFIED, PENALIZED}`。输入自洽，真正实现「只换实现、不动调用方」。
  3. **免考后置应用**（非判定）：**读对端 `yx501`**（全厂一个，0/1），PENALIZED + yx501=1 → 标记免考、剔除该档分母。免考判定（Q vs 极限）是对端义务，不进 VQMS judge。
- **原因（⚠️ 2026-08-14 论据修正）**：门控=前置作用域、免考=后置覆盖（草稿 §2.6 包络失败后才判），架构上不同。**原论据「免考判不了、EXEMPT 不可达」已证伪**——免考数据对端 GENERATOR 库齐全（`qYcNum`=yc217/317 逐机 Q + `GENERATOR_P_QLimit` 双向极限曲线），`docs/数据源头（草稿）.md` §4 缺口③ 已定「对端 JS 算 `yx501`、VQMS 读结论值」架构。**Leo 2026-08-14 拍板：免考判定归对端、VQMS 不背判定义务**——故 `EXEMPT` 不该是 judge（VQMS 判定接口）的输出，它是后置读 yx501 的**应用结果**，不是判定产物。
- **附带（finding A，解码归属）**：`AvcCommand` 应携带 `warn_info` 原始字段 + t₀ 实时母线电压，**解码留在 judge 实现内部**，不要让调用方预算 `V_target`（否则草稿 §2.8「循环码」位定义等解码变更会迫使调用方改动，违反「不动调用方」）。

### A3. §8.5 解码失败处理（加独立计数 + 合规确认）
- **位置**：§8.5 表格（约第 344 行），「目标值/增量解码失败 | 跳过 + 记日志，不计入发令次数」。该行在 §8.5 已标「暂定」。
- **改为**：跳过判定 + 记日志 + **单独计数 `undecodable_count` 并上报**；标注「暂定项，需与政策口径/电厂考核惯例确认」。
- **原因**：调节合格率分母=发令次数，解码失败就剔除分母=系统性抬高合格率，有合规风险。记日志留了运维痕迹但修不了分母抬高；审计看的是统计数字本身。

### A4. §12.1 D4 内联 + §10 补 API 清单
- **D4（约第 436 行）**：唯一依据「v3.4 §5.7.3 ①-a」在 v4.0 全文未复述 → **把该闸门细节（放宽边界 + 正则校验 + 解析后精确过滤）内联进 v4.0 §12.1 或新增小节**，使「无需回查 v3.4」名副其实。（D3 顺手去掉冗余的「v3.4 §5.5」引用——§13 已完整复述时间对齐规则，D3 矛盾较弱。）
- **§10（约第 395–405 行）**：现只讲前缀剥离，无端点清单 → 补 7 个前端页面所需的 `/vqms/*` 端点清单、分页/导出约定、`@PreAuthorize` perms 字符串（如 `vqms:curve:list`）。

### A5. 逻辑 FK 写路径校验（确定轨 D1~D6 开工时补）
- **位置**：§12.1 D1~D6 确定轨，`busbar`/`busbar_group`/`yc_point_map` 的维护写路径。
- **现状**：三表之间无物理外键（跨库 + RuoYi 风格，可接受），全靠应用层保证一致性。
- **改为**：关键写路径加存在性校验——维护 `busbar` 时校验 `group_num` 在 `busbar_group` 存在；维护 `yc_point_map` 时校验 `busbar_id` 存在；删除 `busbar_group` 时校验无引用。
- **原因**：无 DB 层约束 → 错漏不会报错，只会在统计结果里悄悄出错。

### A6. 判定整定参数 CRUD 方案（自建表 `vqms_judge_param`）
- **背景**：`t_fast` / `t_econ` / 分档阈值是现场可整定的判定参数，需「初始值 + 运行时 CRUD 读写 + 改后即时生效」（对应会议清单 #1/#2）。Leo 2026-08-14 拍板走自建表。
- **存储**：新建 VQMS 自管表 `vqms_judge_param`，与 `busbar_threshold` 并列。**不用** RuoYi `sys_config`（要值域校验 / 结构化可读 / 归属清晰 / 操作日志，键值对给不了）。
- **DDL（utf8mb4）**：
  `param_id BIGINT PK AUTO_INCREMENT`、`param_key VARCHAR(64) NOT NULL UNIQUE`、`param_value INT NOT NULL`（分钟数）、`name VARCHAR(64) NOT NULL`、`description VARCHAR(255)`、`value_min INT`、`value_max INT`、`status CHAR(1) DEFAULT '0'`、`create_by/create_time`、`update_by/update_time`、`remark`。
- **初始值与值域**（Leo 2026-08-14 定）：
  - `t_fast`：范围 **[1,5)**（整数 1–4），自由整定；默认值待定（建议 **4**）。快速性档窗口 = [1, `t_fast`]。
  - `t_econ` = **5**（**写死、固定，不再可整定**）。经济性档窗口 = [`t_fast`+1, 5]。
  - `tier_threshold_fast` = **1**、`tier_threshold_econ` = **5**（附件6 政策值，锁）。
  - **指令间隔 = 5 分钟**（Leo 确认：现场 AVC 实际就是 5 分钟一条）→ 判定窗口 = 间隔 = 5 分钟：**指令重叠不发生（#4 解决）、`T_econ` 无上限隐患（#1 解决）**。
- **CRUD**：RuoYi 代码生成 `/vqms/judgeParam` + Vue 页面，挂菜单「VQMS 管理 → 判定参数」，perms `vqms:judgeParam:list/add/edit/remove`，操作日志自动。
- **读写生效**：判定时读表 + **Redis 缓存** `vqms:judgeParam:{key}`；CRUD 改完 `CacheEvict` 刷新、下次判定读新值；启动灌缓存。
- **值域校验**（Service 层）：`t_fast` ∈ [1,5) 整数、且 < `t_econ`(=5)；`t_econ` 锁定 5；分档阈值 1/5 锁定。
- **归属**：确定轨实施（不依赖算法定稿），归 `ruoyi-vqms`。

### A7. 缺数据策略 —— 四套候选方案（待 Leo 会上选）
- **背景**：判定窗口内某分钟无 `his_curve_sv`（采集漏 / `save_time` 脏写）怎么处理；对应会议清单 #3。草稿 §2.8 暂定「跳过缺失 + 整窗全缺剔除」，但有误判与合规隐患。
- **隐患**（数据例证）：① 跳过缺失分钟若恰好丢极值，综合区间变窄 → 误判（如 V_target=233.5，缺 low=233 那分钟后区间下界抬到 234，合格翻不合格）；② 剔除分母 = 抬高合格率，不单独计数则审计无痕（与 #5 解码失败同类）。
- **四套候选**（每套含 部分缺 / 整窗缺 / 留痕 三口径一致处理）：

| 策略 | 部分缺 | 整窗缺 | 留痕 | 立场 |
|---|---|---|---|---|
| 甲（草稿暂定） | 跳过，用剩余 | 剔除分母 | 仅日志 | 宽松 |
| **乙（推荐）** | **阈值剔除（可用 <50% 弃，≥50% 用剩余）** | **剔除分母** | **单独计数 `partial_missing_count` / `missing_window_count`** | **稳健** |
| 丙 | 有缺失即判不合格 | 计不合格 | 日志 | 严 |
| 丁 | 跳过 + 完整度标记 | 挂起人工后审 | 标记 + 计数 | 透明 |

- **推荐乙**：务实折中（缺多了弃、缺一点用剩余、全部留痕，与 #5 一致）。代价：没彻底消除「缺极值误判」。要彻底干净 → **乙 + 丁标记**（阈值剔除 + 单独计数 + 剩余判定带完整度标记），工程量最大。
- **待 Leo 定**：选哪套；若选乙，阈值（建议 50%，可整定）+ 是否叠加丁的完整度标记。

---

## B. 流程图联动（呼应 A3）

- **图B 解码失败节点 `S1` 措辞同步**（A3 落地时一并改）：
  - `docs/核心算法流程图/AVC考核核心算法_流程图.md` 第 84 行
  - `docs/核心算法流程图/AVC考核核心算法_流程图_通俗版.md` 第 85 行
  - `docs/核心算法流程图/_render/figB_调节合格率.mmd` 第 15 行
  - 现「跳过+记日志 / 不计发令次数」→ 改「跳过+记日志 + 单独计数 `undecodable_count`（暂定，需合规确认）」。纯措辞，流向不变。
- **门控前置框**：暂不画。门控点未补录（§14-7，`gate_enabled=0` 空转），现在画等于画空转框；等门控点补录后再在图B 入口补「门控前置过滤」节点。
- **图A 投运率、免考判关节点（CF1/DF1）**：**不动**。免考在图里已是「包络失败→再判免考」的后置覆盖，与 A2 三阶段一致；A2 是代码接口切分，算法流向层面无差异。

---

## C. 增强类（评估认可，优先级较低，非必改）

- **§9.4 状态陈旧**：阶段 1/2 已落地，但 §9.4（约第 386–391 行）仍列「品牌去除 6 项确认」「页面清单是否齐全」为待确认 → 标注「阶段 1/2 已落地，以下确认项转 review/微调」。
- **§5 只读无强制**：§5 只用 `@DataSource(SLAVE)` 路由约定，不防误写 → §11 账号分离补「外部源连接用 `GRANT SELECT`-only 只读账号」。
- **非功能约束缺失**：`his_curve_sv` 无索引随历史变慢（§14-10）→ 补查询预算/分页上限/导出限量；补**数据备份恢复**要求（外部源是原始数据唯一真相源，正文只字未提，尤为关键）。
- **测试对接**：v4.0 已引用 `tools/avc-data-gen/`（§9.3/§9.4），但未引用 `docs/测试方案.md`；D1~D6 表格无「完成标准/验收」列 → 补验收标准。
- **`tolerance_v` 占位标注（§6.2.4）**：表已建但字段角色在新判定口径下悬空（CLAUDE.md 已标 open follow-up）。建议在该列注释更明确标注「占位/角色待定，**禁用旧 `|average_SV−plan_SV|≤tolerance_v` 口径**」，避免后续误读旧模型。
- **待定项跟踪机制（§8.5 / §14）**：§8.5 有 6–7 项待定（缺数据策略、`T_econ` 上限、循环码含义等）、§14 风险 14 项，均如实转述草稿但缺跟踪。建议每项配负责人 + 目标解决时间 + 验收标准（具体由 Leo 指派），否则易成长期挂起的 TODO。

---

## D. 已核实无需改（留档）

- **断言 10（伪问题）**：§6.2.2 `main_indicator_yc_num=3008`（主母线号**指示点**，`point_type='busbar_id'`）与 §14-7「门控点未补录」（远方就地总/AVC投退两个 yx 点）是**不同字段，不矛盾**；`warn_type=5` 在 §8.1、草稿第 86 行三处一致，§8.2 注释仅省略未重复，不构成不一致。**不修。**

---

## E. 安全（独立于 v4.0 修订，建议尽快处理）

- **凭据泄露提级（§14.12 / `docs/tmp.md`）**：`docs/tmp.md` 含明文 DB 凭证（MySQL root 密码、主机）；CLAUDE.md Security 节已记、v4.0 §14.12 也列了，但与「循环码含义待验证」等技术细节并列在同一风险表，优先级被稀释。
- **动作**：① 从 git 历史擦除该密码；② 轮换该密码；③ 此后不再往受跟踪文件写秘密。
- **时效**：**不受「v4.0 已提交、先不改」约束**——独立安全操作，越早越好。git 历史重写属破坏性操作（force push 影响共享历史），**需 Leo 明确授权后执行，不主动操作**。
