# VQMS 项目规划（电压质量管理系统）

> 运行环境: Linux · Docker + Docker Compose · PostgreSQL · Spring Boot 微服务 · Vue 3 Web
> 数据源: [his_curve_sv.md](his_curve_sv.md)（外部 MySQL `10.0.0.35 / qheatavchisdb.his_curve_sv`）
> 创建日期: 2026-07-31

---

## 1. 项目概述

VQMS（Voltage Quality Management System）面向变电站母线电压质量的采集、统计与可视化。系统从外部 AVC 盒子 MySQL 库读取母线电压曲线（`his_curve_sv`），按 **日 / 月 / 年** 三个粒度统计电压质量，核心指标包括：

- **电压合格率** — 电压处于 `[low_SV, high_SV]` 区间的时间占比
- **超上限率** — `average_SV > high_SV` 的时间占比
- **超下限率** — `average_SV < low_SV` 的时间占比
- **超限时间** — 超上限 + 超下限的累计时长

提供 Web 看板，支持母线 `0 / 1` 切换、曲线查看、统计报表与 Excel 导出。

### 1.1 存储职责划分（重要）

| 存储 | 职责 | 存什么 |
|------|------|--------|
| **外部 MySQL**（`10.0.0.35`） | 业务数据源（**只读**） | `his_curve_sv` 原始电压曲线 |
| **PostgreSQL** | 管理库 + 派生统计（**唯一持久库**） | 用户/密码/角色/配置/日志 + 日/月/年统计结果 + 预计算游标 |
| ~~Redis~~ | **不引入**（最简架构） | — |

> **关键原则**：
> - PostgreSQL **不存储原始业务数据**——原始电压曲线始终只在外部 MySQL（唯一真实来源）。
> - PostgreSQL 存储的是**可重算的派生统计**与管理数据，全持久化，重启不丢、无需预热。
> - **不使用 Redis**：JWT 无状态鉴权，统计直接查 PG（本量级 PG 毫秒级足够），组件最少。

### 1.2 源数据特征（关键约束）

| 项 | 说明 | 影响 |
|----|------|------|
| `save_time` | `varchar(255)`，非 datetime，带毫秒 | 查询/计算时按 `Asia/Shanghai` 解释为时间戳 |
| 无主键 / 无索引 | 源表无任何约束 | 预计算与查询需按 `(save_time, busbar_num)` 去重、排序 |
| 双写 | 每分钟母线 0 和 1 各一条 | 统计按 `busbar_num` 分组 |
| `plan_SV` | 原始遥测编码（如 `10245`），非真实电压 | 需 `ori_code` 表解码，**当前该表为空**，作为待办占位 |
| 数据量 | 约 2,880 行/天（2 条/分钟） | 体量小，单次预计算很轻 |

---

## 2. 技术栈

| 层 | 选型 | 备注 |
|----|------|------|
| 后端微服务 | **Java 17 + Spring Boot 3.x** | Spring Cloud Gateway + OpenFeign + JWT |
| 数据库 | **PostgreSQL 15** | 管理 + 派生统计（唯一持久库） |
| 鉴权 | **JWT（无状态）** | 登录签发，网关校验签名；不依赖会话存储 |
| 外部数据源 | MySQL 5.7（`10.0.0.35`） | 业务数据唯一来源，**只读** |
| 前端 | **Vue 3 + Element Plus + ECharts** | Vite 构建，SPA |
| Excel 导出 | **Alibaba EasyExcel**（基于 Apache POI） | 流式写 xlsx，低内存，适合大范围明细导出 |
| 反向代理 | Nginx | 托管前端静态资源 + API 路由 |
| 部署 | **Docker + Docker Compose** | 单机编排，Linux 主机 |

---

## 3. 系统架构

```
                          ┌──────────────┐
                          │   浏览器      │  Vue 3 + Element Plus + ECharts
                          └──────┬───────┘
                                 │ HTTP
                          ┌──────▼───────┐
                          │    Nginx     │  :80  静态资源 + /api 反代
                          └──────┬───────┘
                                 │ /api/**
                          ┌──────▼─────────┐
                          │ gateway-service │  路由 / CORS / JWT 鉴权
                          └──────┬─────────┘
                                 │
                          ┌──────▼─────────┐
                          │ web-api-service │  查询 + 用户/配置/日志 + 统计读写
                          └──┬──────────┘
              缺统计→Feign重算 │          │ 读写管理/统计
   ┌────────────────┬─────────┘          │
   │                │          ┌─────────▼─────────┐
   │                │          │    PostgreSQL     │
   ┌────────────────▼──────┐   │  管理 + 统计 + 游标 │  ← data-ingestion 也写入
   │  statistics-service    │   │   (唯一持久库)     │
   │  按需重算 → 写 PG       │   └───────────────────┘
   └────────────┬──────────┘            ▲
                │ 只读业务               │ 写统计(预计算)
                │              ┌─────────┴──────────┐
                │              │ data-ingestion-    │
                │              │ service            │
                │              │ 定时读MySQL→算→写PG │
                │              └─────────┬──────────┘
                │                        │ 只读
                └────────────┬───────────┘
                       ┌──────▼──────────────┐
                       │ 外部 MySQL (AVC 盒子) │ 10.0.0.35 / his_curve_sv
                       │ 业务数据唯一来源(只读) │
                       └─────────────────────┘
```

**数据流**：
- **预计算路径**：`data-ingestion-service` 定时读外部 MySQL → 计算日/月/年统计 → UPSERT 进 PG 统计表。
- **查询路径**：前端 → `web-api-service` → 直接查 PG 统计表；若该时段未计算（行缺失）→ Feign 调 `statistics-service` 实时重算、写入 PG 后返回。
- **管理路径**：用户/配置/日志由 `web-api-service` 读写 PG。
- **鉴权**：登录签发 JWT（无状态），`gateway-service` 校验签名与过期。

---

## 4. 微服务划分

共 4 个 Spring Boot 服务 + 1 个网关，均为独立 Docker 容器：

| 服务 | 端口 | 职责 | 关键依赖 |
|------|------|------|----------|
| **gateway-service** | 8080 | API 网关：路由转发、CORS、JWT 无状态鉴权 | web-api |
| **data-ingestion-service** | 8081 | **预计算调度**：定时读外部 MySQL → 计算统计 → UPSERT 进 PG | MySQL(外部)、PG |
| **statistics-service** | 8082 | **按需重算**：行缺失时读 MySQL → 算 → 写 PG；提供手动重算接口 | MySQL(外部)、PG |
| **web-api-service** | 8083 | 面向前端查询（曲线读 MySQL、统计读 PG）+ 用户/配置/日志管理（读写 PG）+ Excel 导出 | MySQL、PG、statistics(Feign) |

> 注：`statistics-service`（按需）与 `data-ingestion-service`（定时预热）职责相关，可合并为单个"统计服务"以降低复杂度；本规划按"定时预热与按需计算分离"拆分，便于独立调度与隔离批量读对查询的影响。

---

## 5. 数据库设计（PostgreSQL —— 唯一持久库）

### 5.1 管理表

```sql
-- 用户表
CREATE TABLE sys_user (
    id            BIGSERIAL PRIMARY KEY,
    username      VARCHAR(64)  NOT NULL UNIQUE,
    password_hash VARCHAR(128) NOT NULL,          -- BCrypt 散列
    display_name  VARCHAR(64),
    enabled       BOOLEAN DEFAULT TRUE,
    created_at    TIMESTAMPTZ DEFAULT now(),
    updated_at    TIMESTAMPTZ DEFAULT now()
);

-- 角色表
CREATE TABLE sys_role (
    id   BIGSERIAL PRIMARY KEY,
    code VARCHAR(32) NOT NULL UNIQUE,             -- ADMIN / OPERATOR / VIEWER
    name VARCHAR(64)
);

-- 用户-角色
CREATE TABLE sys_user_role (
    user_id BIGINT REFERENCES sys_user(id) ON DELETE CASCADE,
    role_id BIGINT REFERENCES sys_role(id),
    PRIMARY KEY (user_id, role_id)
);

-- 系统配置（外部 MySQL 连接、采集频率、电压上下限默认值、JWT 密钥引用等）
CREATE TABLE sys_config (
    key        VARCHAR(64) PRIMARY KEY,
    value      TEXT,
    remark     VARCHAR(255),
    updated_at TIMESTAMPTZ DEFAULT now()
);

-- 操作日志（审计）
CREATE TABLE sys_operation_log (
    id         BIGSERIAL PRIMARY KEY,
    username   VARCHAR(64),
    action     VARCHAR(64),
    params     TEXT,
    ip         VARCHAR(64),
    created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_oplog_time ON sys_operation_log (created_at DESC);
```

### 5.2 统计表（日 / 月 / 年，派生数据）

派生聚合，可随时从外部 MySQL 重算重建。三张表结构相近，仅时间维度不同：

```sql
-- 每日统计
CREATE TABLE voltage_quality_daily (
    id                  BIGSERIAL PRIMARY KEY,
    stat_date           DATE     NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,              -- 采样总分钟数
    qualified_minutes   INTEGER,              -- 合格分钟数  (low <= avg <= high)
    over_high_minutes   INTEGER,              -- 超上限分钟数(avg > high)
    over_low_minutes    INTEGER,              -- 超下限分钟数(avg < low)
    over_limit_minutes  INTEGER,              -- 超限总时长(分) = over_high + over_low
    qualification_rate  NUMERIC(5,2),         -- 合格率 %
    over_high_rate      NUMERIC(5,2),         -- 超上限率 %
    over_low_rate       NUMERIC(5,2),         -- 超下限率 %
    max_SV              NUMERIC(10,0),        -- 日最大电压
    min_SV              NUMERIC(10,0),        -- 日最小电压
    avg_SV              NUMERIC(12,2),        -- 日平均电压
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_date, busbar_num)
);
CREATE INDEX idx_vqd_busbar_date ON voltage_quality_daily (busbar_num, stat_date);

-- 每月统计
CREATE TABLE voltage_quality_monthly (
    id                  BIGSERIAL PRIMARY KEY,
    stat_year           SMALLINT NOT NULL,
    stat_month          SMALLINT NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,
    qualified_minutes   INTEGER,
    over_high_minutes   INTEGER,
    over_low_minutes    INTEGER,
    over_limit_minutes  INTEGER,
    qualification_rate  NUMERIC(5,2),
    over_high_rate      NUMERIC(5,2),
    over_low_rate       NUMERIC(5,2),
    max_SV              NUMERIC(10,0),
    min_SV              NUMERIC(10,0),
    avg_SV              NUMERIC(12,2),
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_year, stat_month, busbar_num)
);

-- 每年统计
CREATE TABLE voltage_quality_yearly (
    id                  BIGSERIAL PRIMARY KEY,
    stat_year           SMALLINT NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,
    qualified_minutes   INTEGER,
    over_high_minutes   INTEGER,
    over_low_minutes    INTEGER,
    over_limit_minutes  INTEGER,
    qualification_rate  NUMERIC(5,2),
    over_high_rate      NUMERIC(5,2),
    over_low_rate       NUMERIC(5,2),
    max_SV              NUMERIC(10,0),
    min_SV              NUMERIC(10,0),
    avg_SV              NUMERIC(12,2),
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_year, busbar_num)
);

-- 预计算游标（记录各母线最后已计算日期，便于断点续算/全量回填）
CREATE TABLE precompute_cursor (
    busbar_num  SMALLINT PRIMARY KEY,
    last_date   DATE,
    updated_at  TIMESTAMPTZ DEFAULT now()
);
```

月度统计由 `voltage_quality_daily` 用 SQL `INSERT ... SELECT ... GROUP BY` 汇总得出，年度统计由 `voltage_quality_monthly` 汇总得出（逐级聚合，避免回扫 MySQL 全量原始数据）。

### 5.3 外部 MySQL —— 业务数据源（只读，不复制）

业务数据不迁入本系统，始终直接读外部 AVC 盒子库的 `his_curve_sv`（结构见 [his_curve_sv.md](his_curve_sv.md)）。所有服务对其只读。

---

## 6. 电压质量统计算法（核心逻辑）

### 6.1 单分钟判定

对外部 MySQL `his_curve_sv` 每条记录（每分钟），用 `average_SV`（实测电压）对照 `[low_SV, high_SV]` 区间：

| 条件 | 分类 |
|------|------|
| `low_SV ≤ average_SV ≤ high_SV` | 合格 |
| `average_SV > high_SV` | 超上限 |
| `average_SV < low_SV` | 超下限 |

> 边界值（等于上下限）计为合格。样例数据 `high=low=avg=234` 属于退化的全合格稳态，符合该规则。

### 6.2 日统计聚合

对某日某母线的所有分钟记录（读 MySQL，按 `(save_time,busbar_num)` 去重）：

```
total_minutes      = COUNT(*)
qualified_minutes  = COUNT(avg <= high AND avg >= low)
over_high_minutes  = COUNT(avg > high)
over_low_minutes   = COUNT(avg < low)
over_limit_minutes = over_high_minutes + over_low_minutes

qualification_rate = qualified_minutes / total_minutes * 100
over_high_rate     = over_high_minutes / total_minutes * 100
over_low_rate      = over_low_minutes  / total_minutes * 100
max_SV / min_SV / avg_SV = MAX/MIN/AVG(average_SV)
```

结果 `UPSERT` 进 `voltage_quality_daily`（按 `(stat_date, busbar_num)` 冲突覆盖）。超限时间展示时分钟可换算为 `小时:分钟`（如 125 分 → 2h05m）。

### 6.3 月 / 年统计聚合（SQL 逐级汇总）

用 SQL 直接从下一级统计表聚合（不可用 rate 直接平均）：

```sql
-- 月度（由日表汇总）
INSERT INTO voltage_quality_monthly (stat_year, stat_month, busbar_num,
    total_minutes, qualified_minutes, over_high_minutes, over_low_minutes, over_limit_minutes,
    qualification_rate, over_high_rate, over_low_rate, max_SV, min_SV, avg_SV)
SELECT EXTRACT(YEAR FROM stat_date), EXTRACT(MONTH FROM stat_date), busbar_num,
       SUM(total_minutes), SUM(qualified_minutes), SUM(over_high_minutes),
       SUM(over_low_minutes), SUM(over_high_minutes)+SUM(over_low_minutes),
       SUM(qualified_minutes)::numeric / NULLIF(SUM(total_minutes),0) * 100,
       SUM(over_high_minutes)::numeric / NULLIF(SUM(total_minutes),0) * 100,
       SUM(over_low_minutes)::numeric  / NULLIF(SUM(total_minutes),0) * 100,
       MAX(max_SV), MIN(min_SV),
       SUM(avg_SV * total_minutes) / NULLIF(SUM(total_minutes),0)
FROM voltage_quality_daily
WHERE stat_date >= :month_start AND stat_date < :next_month_start
GROUP BY 1,2,3
ON CONFLICT (stat_year, stat_month, busbar_num) DO UPDATE SET ...;
```

年度统计同理，由 `voltage_quality_monthly` 汇总到 `voltage_quality_yearly`。

### 6.4 调度与查询策略

| 任务 | 执行方 | 频率 | 范围 |
|------|--------|------|------|
| 当日日统计预热 | data-ingestion | 每小时 | 重算当天各母线 → `voltage_quality_daily` |
| 历史日补算 | data-ingestion | 每晚 01:00 | 重算昨日 |
| 月统计汇总 | data-ingestion | 每晚 01:30 | 当月各母线 → `voltage_quality_monthly` |
| 年统计汇总 | data-ingestion | 每晚 02:00 | 当年各母线 → `voltage_quality_yearly` |
| 缺数据按需重算 | statistics | 按需 | 查询时段行缺失时 → 算 → 写 PG → 返回 |
| 手动重算 | statistics | 触发 | 按日期范围 |

查询时 `web-api-service` 先查 PG 统计表；行缺失则经 Feign 调 `statistics-service` 实时重算并写入后返回，对前端透明。

---

## 7. API 设计（`gateway-service` → 各服务）

统一前缀 `/api/v1`，返回 `{ code, msg, data }`。除登录外均需携带 JWT。

| 方法 | 路径 | 说明 | 服务 |
|------|------|------|------|
| POST | `/api/v1/auth/login` | 登录（校验 PG 用户/密码，签发 JWT） | web-api |
| GET | `/api/v1/users` | 用户列表（管理） | web-api |
| POST | `/api/v1/users` | 新建用户 | web-api |
| GET | `/api/v1/curve` | 原始曲线 `?busbar=0&start=&end=`（读 MySQL） | web-api |
| GET | `/api/v1/stats/daily` | 日统计 `?busbar=0&date=2026-07-31`（读 PG，缺则重算） | web-api |
| GET | `/api/v1/stats/daily/range` | 日统计区间（月历）`?busbar=0&from=&to=` | web-api |
| GET | `/api/v1/stats/monthly` | 月统计 `?busbar=0&year=2026&month=7` | web-api |
| GET | `/api/v1/stats/yearly` | 年统计 `?busbar=0&year=2026` | web-api |
| GET | `/api/v1/export/curve` | 导出原始曲线明细 xlsx `?busbar=0&start=&end=` | web-api |
| GET | `/api/v1/export/stats/daily` | 导出日统计 xlsx `?busbar=0&from=&to=` | web-api |
| GET | `/api/v1/export/stats/monthly` | 导出月统计 xlsx `?busbar=0&year=2026` | web-api |
| GET | `/api/v1/export/stats/yearly` | 导出年统计 xlsx `?busbar=0&year=2026` | web-api |
| POST | `/api/v1/stats/recompute` | 手动重算 `?from=&to=` | statistics |
| GET | `/api/v1/ingestion/status` | 预计算状态/游标 | data-ingestion |

**Excel 导出说明**：
- 导出接口返回 xlsx 二进制流（`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`），前端以 blob 触发下载，响应头带 `Content-Disposition: attachment; filename=...`。
- 数据来源：曲线明细读外部 MySQL，统计读 PG；缺统计时先按需重算。
- 由 EasyExcel 流式写入；单次导出限区间/行数（如 ≤1 年、≤50 万行），超限提示缩小范围。
- 导出列：曲线明细（时间、母线、上限/下限/平均/计划电压）；日/月/年统计（时间维度、各时长、各率、极值电压）。

---

## 8. 前端规划（Vue 3 + Element Plus + ECharts）

### 8.1 页面

1. **登录** — 用户名/密码，获取 JWT 后存于前端（无状态）。
2. **电压曲线** — ECharts 折线图，叠加 `high_SV/low_SV` 上下限带，母线切换、时间范围选择。
3. **日统计** — 当日合格率/超限率卡片 + 24h 曲线 + 分钟明细表。
4. **月统计** — 月历热力图（按日合格率着色）+ 月度汇总卡片 + 日列表表格。
5. **年统计** — 12 个月柱状图 + 年度汇总卡片 + 月列表表格。
6. **系统管理**（可选） — 用户/角色/配置维护。

> 曲线页与日/月/年统计页均提供 **"导出 Excel"** 按钮，按当前筛选条件（母线、时间范围）调用 `/api/v1/export/*` 下载 xlsx；明细表区也支持就地导出当前表格。

### 8.2 目录（`frontend/vqms-web`）

```
vqms-web/
├── src/
│   ├── api/          # axios 封装（带 JWT），对接 /api/v1
│   ├── views/        # Login / Curve / Daily / Monthly / Yearly / Admin
│   ├── components/   # StatCard / RateGauge / BusbarSwitch
│   ├── router/  store/
│   └── App.vue  main.ts
├── Dockerfile        # 多阶段：node 构建 → nginx 托管
└── vite.config.ts
```

---

## 9. 目录结构（仓库整体）

```
VQMS/
├── docker-compose.yml          # 全栈编排
├── .env                        # 连接串/端口/外部 MySQL/JWT 密钥
├── 项目规划.md                  # 本文档
├── CLAUDE.md  README.md  his_curve_sv.md  his_curve_tables.md
├── infra/
│   ├── postgres/init.sql       # 建表脚本：管理表 + 统计表 + 游标 + 初始管理员
│   └── nginx/nginx.conf        # 前端托管 + /api 反代到 gateway
├── services/
│   ├── gateway-service/        # Spring Cloud Gateway（路由/JWT 鉴权）
│   ├── data-ingestion-service/ # 预计算调度：MySQL→PG
│   ├── statistics-service/     # 按需重算：MySQL→PG
│   └── web-api-service/        # 查询 API + 管理 + 导出（PG）
└── frontend/
    └── vqms-web/               # Vue 3 SPA
```

> 无 Redis 组件、无本地原始业务数据——原始曲线始终读外部 MySQL，统计与管理全在 PostgreSQL。

---

## 10. Docker Compose 部署

服务清单（均纳入同一 `docker-compose.yml`）：

| 容器 | 镜像/构建 | 端口 | 依赖 |
|------|-----------|------|------|
| `postgres` | postgres:15 | 5432 | — （管理 + 统计，唯一持久库） |
| `gateway-service` | 本地构建 | 8080 | web-api |
| `data-ingestion-service` | 本地构建 | 8081 | postgres |
| `statistics-service` | 本地构建 | 8082 | postgres |
| `web-api-service` | 本地构建 | 8083 | postgres |
| `nginx` | nginx:stable | 80 → 前端 + /api | gateway |

关键配置点：
- `.env` 管理外部 MySQL 连接（`MYSQL_HOST=10.0.0.35` / 库 / 账号）、PG 密码、服务端口、JWT 密钥与过期时间。
- `data-ingestion-service` 与 `statistics-service` 通过 Docker `extra_hosts` 或 host 网络访问外部 AVC 盒子 MySQL（取决于网络可达性）。
- PG 使用 named volume 持久化；`infra/postgres/init.sql` 挂载到 `/docker-entrypoint-initdb.d/` 首次初始化（建管理表 + 统计表 + 游标 + 初始管理员账号）。
- Nginx 单一入口：`/` 托管前端 SPA，`/api/` 反代到 `gateway-service:8080`。

---

## 11. 开发里程碑

| 阶段 | 内容 | 产出 |
|------|------|------|
| **M1 基础设施** | docker-compose、PG 建表（管理+统计）、Nginx 骨架 | 容器可启、库表就绪 |
| **M2 预计算调度** | data-ingestion：定时读 MySQL→算→写 PG 统计表 + 月/年 SQL 汇总 | PG 有预计算统计 |
| **M3 按需重算** | statistics：缺数据重算 + 手动重算接口 | 查询不缺数 |
| **M4 查询 API + 鉴权 + 导出** | web-api：曲线/统计查询 + Excel 导出 + 用户/配置/日志（PG）+ JWT 签发/校验 | API + 登录可用 |
| **M5 前端看板** | Vue 登录/曲线/日/月/年 + 导出按钮 + 管理页 | Web 可用 |
| **M6 联调优化** | 端到端、边界用例、日志监控 | 可交付 |

---

## 12. 待解决问题

1. **`plan_SV` 解码**：源字段为原始遥测编码（`10245`），需 `ori_code` 映射表解码，当前该表为空。先按原值读取/展示，预留解码逻辑（读侧计算，无需落库），待映射表填充后启用。
2. **稳态数据验证**：现有样例 `high=low=avg`，统计恒为 100% 合格；待采集到含波动/越限的真实数据后校验算法分支。
3. **时区**：源 `save_time` 含毫秒但无时区，统一按 Asia/Shanghai 解释。
4. **JWT 无状态的吊销**：无会话存储意味着令牌签发后在到期前难以主动吊销；缓解策略——短有效期（如 2h）+ 滚动刷新，或在 `sys_config` 维护轻量黑名单（按需，极致简化可不做）。
5. **全量回填入口**：PG 统计虽持久，仍需提供"按日期范围全量重算"（首启、修正历史、`plan_SV` 解码启用后重建）。
6. **外部 MySQL 可用性**：AVC 盒子库不可达时——已计算统计仍可从 PG 正常返回；仅曲线查询与未计算时段受影响，返回明确错误。
7. **大范围导出性能**：全年曲线明细导出（~百万行）需流式写入 + 区间/行数上限；必要时改为异步生成后提供下载链接。
