# VQMS 项目规划 v2（电压质量管理系统 · 单体架构版）

> 运行环境: Linux · Docker + Docker Compose · PostgreSQL · Spring Boot 单体应用 · Vue 3 Web
> 数据源: [his_curve_sv.md](his_curve_sv.md)（外部 MySQL `10.0.0.35 / qheatavchisdb.his_curve_sv`）
> 创建日期: 2026-07-31
> 说明: 本版本相较 v1（`项目规划.md`）取消微服务拆分，改为单体应用；数据模型、统计算法、存储职责划分与 v1 保持一致。

---

## 1. 项目概述

VQMS（Voltage Quality Management System）面向变电站母线电压质量的采集、统计与可视化。系统从外部 AVC 盒子 MySQL 库读取母线电压曲线（`his_curve_sv`），按 **日 / 月 / 年** 三个粒度统计电压质量，核心指标包括：

- **电压合格率** — 电压处于 `[low_SV, high_SV]` 区间的时间占比
- **超上限率** — `average_SV > high_SV` 的时间占比
- **超下限率** — `average_SV < low_SV` 的时间占比
- **超限时间** — 超上限 + 超下限的累计时长

提供 Web 看板，支持母线 `0 / 1` 切换、曲线查看、统计报表与 Excel 导出。

### 1.1 为什么改为单体架构

实际数据规模：单站点、2 条母线，约 2,880 行/天，源库最大表约 1.2 万行，属于低并发内部监控工具。v1 规划中拆分的 4 个 Spring Boot 微服务 + 网关，其价值（独立扩缩容、故障隔离、多团队并行开发）在此规模下基本无法体现，反而带来：

- `data-ingestion` 与 `statistics` 职责本就高度重叠（v1 文档也承认可合并）
- 查询链路多出网关路由 + Feign 远程调用两次网络往返，仅为处理"统计行缺失"这种低频分支
- 4 个 JVM 容器 + 网关 + PG + Nginx，部署与联调成本明显高于收益

因此改为**单一 Spring Boot 应用**，内部按包（package）划分模块边界，保留清晰的职责分层，但去掉跨进程调用；未来若真的出现需要独立伸缩的模块（例如接入多站点后统计计算成为瓶颈），再从对应包拆出为独立服务，成本可控。

### 1.2 存储职责划分（不变）

| 存储 | 职责 | 存什么 |
|------|------|--------|
| **外部 MySQL**（`10.0.0.35`） | 业务数据源（**只读**） | `his_curve_sv` 原始电压曲线 |
| **PostgreSQL** | 管理库 + 派生统计（**唯一持久库**） | 用户/密码/角色/配置/日志 + 日/月/年统计结果 + 预计算游标 |
| ~~Redis~~ | **不引入**（最简架构） | — |

> **关键原则**（与 v1 一致）：
> - PostgreSQL **不存储原始业务数据**——原始电压曲线始终只在外部 MySQL（唯一真实来源）。
> - PostgreSQL 存储的是**可重算的派生统计**与管理数据，全持久化，重启不丢、无需预热。
> - **不使用 Redis**：JWT 无状态鉴权，统计直接查 PG（本量级 PG 毫秒级足够），组件最少。

### 1.3 源数据特征（关键约束，不变）

| 项 | 说明 | 影响 |
|----|------|------|
| `save_time` | `varchar(255)`，非 datetime，带毫秒 | 查询/计算时按 `Asia/Shanghai` 解释为时间戳；拉取时先做格式校验，异常行记录日志跳过而非整批失败 |
| 无主键 / 无索引 | 源表无任何约束 | 预计算与查询需按 `(save_time, busbar_num)` 去重、排序 |
| 双写 | 每分钟母线 0 和 1 各一条 | 统计按 `busbar_num` 分组 |
| `plan_SV` | 原始遥测编码（如 `10245`），非真实电压 | 需 `ori_code` 表解码，**当前该表为空**，作为待办占位 |
| 数据量 | 约 2,880 行/天（2 条/分钟） | 体量小，单次预计算很轻，无需分片/流式处理 |

---

## 2. 技术栈

| 层 | 选型 | 备注 |
|----|------|------|
| 后端 | **Java 17 + Spring Boot 3.x（单体）** | Spring MVC + Spring Security（JWT）+ Spring Scheduler |
| 数据库 | **PostgreSQL 15** | 管理 + 派生统计（唯一持久库） |
| 鉴权 | **JWT（无状态）** | 登录签发，Spring Security Filter 校验签名 |
| 外部数据源 | MySQL 5.7（`10.0.0.35`） | 业务数据唯一来源，**只读**，用第二个 `DataSource` 接入 |
| 前端 | **Vue 3 + Element Plus + ECharts** | Vite 构建，SPA |
| Excel 导出 | **Alibaba EasyExcel**（基于 Apache POI） | 流式写 xlsx，低内存 |
| 反向代理 | Nginx | 托管前端静态资源 + `/api` 反代到后端 |
| 部署 | **Docker + Docker Compose** | 3 个容器：postgres / backend / nginx |

---

## 3. 系统架构

```
                    ┌──────────────┐
                    │   浏览器      │  Vue 3 + Element Plus + ECharts
                    └──────┬───────┘
                           │ HTTP
                    ┌──────▼───────┐
                    │    Nginx     │  :8080  静态资源 + /api 反代
                    └──────┬───────┘
                           │ /api/**
                    ┌──────▼──────────────────────────┐
                    │   vqms-backend（单体 Spring Boot）│  :7000
                    │  ┌────────────────────────────┐ │
                    │  │ web (Controller层)          │ │  查询/管理/导出 API + JWT 鉴权
                    │  ├────────────────────────────┤ │
                    │  │ statistics (Service层)      │ │  日/月/年统计算法，供 web 与 scheduler 复用
                    │  ├────────────────────────────┤ │
                    │  │ ingestion (Scheduler层)     │ │  定时任务：读 MySQL → 调 statistics → 写 PG
                    │  ├────────────────────────────┤ │
                    │  │ common (数据源/实体/工具)     │ │  双数据源配置、DO/DTO、异常处理
                    │  └────────────────────────────┘ │
                    └──────┬──────────────────┬───────┘
                    只读     │                  │ 读写
                    ┌────────▼───────┐  ┌───────▼─────────┐
                    │  外部 MySQL     │  │   PostgreSQL     │
                    │  (AVC 盒子)     │  │ 管理+统计+游标   │
                    │  10.0.0.35     │  │ (唯一持久库)      │
                    │  his_curve_sv  │  │                  │
                    └────────────────┘  └──────────────────┘
```

**数据流**：
- **预计算路径**：内置 `@Scheduled` 定时任务（ingestion 模块）读外部 MySQL → 调用 statistics 模块计算日/月/年统计 → UPSERT 进 PG 统计表。
- **查询路径**：前端 → Controller → 直接查 PG 统计表；若该时段未计算（行缺失）→ **进程内直接调用** statistics 模块的方法实时重算、写入 PG 后返回（不再经过网络调用）。
- **管理路径**：用户/配置/日志由 Controller 读写 PG。
- **鉴权**：登录签发 JWT（无状态），Spring Security Filter 校验签名与过期，不依赖会话存储。

模块之间用 Java 接口（interface）解耦，比如 `StatisticsService` 被 Controller 和 Scheduler 共同依赖，方便日后若某个模块确需拆出，只需把该接口的实现改成远程调用（Feign/HTTP），业务代码改动很小。

---

## 4. 后端模块划分（同一 Spring Boot 工程内的包结构）

```
com.vqms
├── VqmsApplication.java
├── common/
│   ├── config/          # 双数据源（MySQL只读 + PG）、Jackson、CORS、JWT 配置
│   ├── entity/          # PG 表对应的 JPA/MyBatis 实体
│   ├── dto/             # 接口出入参
│   └── exception/       # 全局异常处理
├── source/               # 外部 MySQL 只读访问层
│   ├── HisCurveSvMapper.java
│   └── HisCurveSvRepository.java
├── statistics/           # 统计核心逻辑（被 web 和 ingestion 共同依赖）
│   ├── StatisticsService.java       # 接口
│   ├── StatisticsServiceImpl.java   # 单分钟判定 + 日/月/年聚合算法
│   └── VoltageQualityRepository.java
├── ingestion/            # 定时预计算调度
│   └── PrecomputeScheduler.java     # @Scheduled 任务，调用 StatisticsService
├── web/                  # 对外 API
│   ├── AuthController.java
│   ├── CurveController.java
│   ├── StatsController.java
│   ├── ExportController.java
│   └── AdminController.java         # 用户/角色/配置/日志
└── security/
    ├── JwtTokenProvider.java
    └── JwtAuthFilter.java
```

> 与 v1 的对应关系：v1 的 `gateway-service` 能力（路由/CORS/鉴权）下沉为 Spring Security + CORS 配置；`data-ingestion-service` → `ingestion` 包；`statistics-service` → `statistics` 包；`web-api-service` → `web` 包。**统计算法、数据模型完全复用 v1 的设计，未作改动。**

---

## 5. 数据库设计（PostgreSQL —— 唯一持久库，与 v1 完全一致）

### 5.1 管理表

```sql
-- 用户表
CREATE TABLE sys_user (
    id            BIGSERIAL PRIMARY KEY,
    username      VARCHAR(64)  NOT NULL UNIQUE,
    password_hash VARCHAR(128) NOT NULL,          -- BCrypt 散列
    display_name  VARCHAR(64),
    enabled       BOOLEAN DEFAULT TRUE,
    created_at    TIMESTAMPTZ DEFAULT now(),
    updated_at    TIMESTAMPTZ DEFAULT now()
);

-- 角色表
CREATE TABLE sys_role (
    id   BIGSERIAL PRIMARY KEY,
    code VARCHAR(32) NOT NULL UNIQUE,             -- ADMIN / OPERATOR / VIEWER
    name VARCHAR(64)
);

-- 用户-角色
CREATE TABLE sys_user_role (
    user_id BIGINT REFERENCES sys_user(id) ON DELETE CASCADE,
    role_id BIGINT REFERENCES sys_role(id),
    PRIMARY KEY (user_id, role_id)
);

-- 系统配置（外部 MySQL 连接、采集频率、电压上下限默认值、JWT 密钥引用等）
CREATE TABLE sys_config (
    key        VARCHAR(64) PRIMARY KEY,
    value      TEXT,
    remark     VARCHAR(255),
    updated_at TIMESTAMPTZ DEFAULT now()
);

-- 操作日志（审计）
CREATE TABLE sys_operation_log (
    id         BIGSERIAL PRIMARY KEY,
    username   VARCHAR(64),
    action     VARCHAR(64),
    params     TEXT,
    ip         VARCHAR(64),
    created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_oplog_time ON sys_operation_log (created_at DESC);
```

### 5.2 统计表（日 / 月 / 年，派生数据）

```sql
-- 每日统计
CREATE TABLE voltage_quality_daily (
    id                  BIGSERIAL PRIMARY KEY,
    stat_date           DATE     NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,
    qualified_minutes   INTEGER,
    over_high_minutes   INTEGER,
    over_low_minutes    INTEGER,
    over_limit_minutes  INTEGER,
    qualification_rate  NUMERIC(5,2),
    over_high_rate      NUMERIC(5,2),
    over_low_rate       NUMERIC(5,2),
    max_SV              NUMERIC(10,0),
    min_SV              NUMERIC(10,0),
    avg_SV              NUMERIC(12,2),
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_date, busbar_num)
);
CREATE INDEX idx_vqd_busbar_date ON voltage_quality_daily (busbar_num, stat_date);

-- 每月统计
CREATE TABLE voltage_quality_monthly (
    id                  BIGSERIAL PRIMARY KEY,
    stat_year           SMALLINT NOT NULL,
    stat_month          SMALLINT NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,
    qualified_minutes   INTEGER,
    over_high_minutes   INTEGER,
    over_low_minutes    INTEGER,
    over_limit_minutes  INTEGER,
    qualification_rate  NUMERIC(5,2),
    over_high_rate      NUMERIC(5,2),
    over_low_rate       NUMERIC(5,2),
    max_SV              NUMERIC(10,0),
    min_SV              NUMERIC(10,0),
    avg_SV              NUMERIC(12,2),
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_year, stat_month, busbar_num)
);

-- 每年统计
CREATE TABLE voltage_quality_yearly (
    id                  BIGSERIAL PRIMARY KEY,
    stat_year           SMALLINT NOT NULL,
    busbar_num          SMALLINT NOT NULL,
    total_minutes       INTEGER,
    qualified_minutes   INTEGER,
    over_high_minutes   INTEGER,
    over_low_minutes    INTEGER,
    over_limit_minutes  INTEGER,
    qualification_rate  NUMERIC(5,2),
    over_high_rate      NUMERIC(5,2),
    over_low_rate       NUMERIC(5,2),
    max_SV              NUMERIC(10,0),
    min_SV              NUMERIC(10,0),
    avg_SV              NUMERIC(12,2),
    computed_at         TIMESTAMPTZ DEFAULT now(),
    UNIQUE (stat_year, busbar_num)
);

-- 预计算游标（记录各母线最后已计算日期，便于断点续算/全量回填）
CREATE TABLE precompute_cursor (
    busbar_num  SMALLINT PRIMARY KEY,
    last_date   DATE,
    updated_at  TIMESTAMPTZ DEFAULT now()
);
```

月度统计由 `voltage_quality_daily` 汇总得出，年度统计由 `voltage_quality_monthly` 汇总得出（逐级聚合，避免回扫 MySQL 全量原始数据）。

### 5.3 外部 MySQL —— 业务数据源（只读，不复制）

业务数据不迁入本系统，始终直接读外部 AVC 盒子库的 `his_curve_sv`（结构见 [his_curve_sv.md](his_curve_sv.md)）。后端对其只读，通过独立只读 `DataSource` 接入，防止误写。

---

## 6. 电压质量统计算法（核心逻辑，与 v1 完全一致）

### 6.1 单分钟判定

对外部 MySQL `his_curve_sv` 每条记录（每分钟），用 `average_SV`（实测电压）对照 `[low_SV, high_SV]` 区间：

| 条件 | 分类 |
|------|------|
| `low_SV ≤ average_SV ≤ high_SV` | 合格 |
| `average_SV > high_SV` | 超上限 |
| `average_SV < low_SV` | 超下限 |

> 边界值（等于上下限）计为合格。样例数据 `high=low=avg=234` 属于退化的全合格稳态，符合该规则。

### 6.2 日统计聚合

```
total_minutes      = COUNT(*)
qualified_minutes  = COUNT(avg <= high AND avg >= low)
over_high_minutes  = COUNT(avg > high)
over_low_minutes   = COUNT(avg < low)
over_limit_minutes = over_high_minutes + over_low_minutes

qualification_rate = qualified_minutes / total_minutes * 100
over_high_rate     = over_high_minutes / total_minutes * 100
over_low_rate      = over_low_minutes  / total_minutes * 100
max_SV / min_SV / avg_SV = MAX/MIN/AVG(average_SV)
```

拉取源数据时按 `(save_time, busbar_num)` 去重排序；`save_time` 格式异常的行记录警告日志并跳过，不影响当次计算整体成功。结果 `UPSERT` 进 `voltage_quality_daily`。超限时间展示时分钟可换算为 `小时:分钟`（如 125 分 → 2h05m）。

### 6.3 月 / 年统计聚合（SQL 逐级汇总，加权平均，不可直接对 rate 取平均）

```sql
-- 月度（由日表汇总）
INSERT INTO voltage_quality_monthly (stat_year, stat_month, busbar_num,
    total_minutes, qualified_minutes, over_high_minutes, over_low_minutes, over_limit_minutes,
    qualification_rate, over_high_rate, over_low_rate, max_SV, min_SV, avg_SV)
SELECT EXTRACT(YEAR FROM stat_date), EXTRACT(MONTH FROM stat_date), busbar_num,
       SUM(total_minutes), SUM(qualified_minutes), SUM(over_high_minutes),
       SUM(over_low_minutes), SUM(over_high_minutes)+SUM(over_low_minutes),
       SUM(qualified_minutes)::numeric / NULLIF(SUM(total_minutes),0) * 100,
       SUM(over_high_minutes)::numeric / NULLIF(SUM(total_minutes),0) * 100,
       SUM(over_low_minutes)::numeric  / NULLIF(SUM(total_minutes),0) * 100,
       MAX(max_SV), MIN(min_SV),
       SUM(avg_SV * total_minutes) / NULLIF(SUM(total_minutes),0)
FROM voltage_quality_daily
WHERE stat_date >= :month_start AND stat_date < :next_month_start
GROUP BY 1,2,3
ON CONFLICT (stat_year, stat_month, busbar_num) DO UPDATE SET ...;
```

年度统计同理，由 `voltage_quality_monthly` 汇总到 `voltage_quality_yearly`。

### 6.4 调度与查询策略

| 任务 | 执行方式 | 频率 | 范围 |
|------|--------|------|------|
| 当日日统计预热 | `PrecomputeScheduler` (`@Scheduled`) | 每小时 | 重算当天各母线 → `voltage_quality_daily` |
| 历史日补算 | `PrecomputeScheduler` | 每晚 01:00 | 重算昨日 |
| 月统计汇总 | `PrecomputeScheduler` | 每晚 01:30 | 当月各母线 → `voltage_quality_monthly` |
| 年统计汇总 | `PrecomputeScheduler` | 每晚 02:00 | 当年各母线 → `voltage_quality_yearly` |
| 缺数据按需重算 | `StatsController` → 直接调用 `StatisticsService`（进程内方法调用，无网络开销） | 按需 | 查询时段行缺失时 → 算 → 写 PG → 返回 |
| 手动重算 | `StatsController` 暴露 `/recompute` 接口 → 调用 `StatisticsService` | 触发 | 按日期范围 |

查询时 Controller 先查 PG 统计表；行缺失则**直接调用同进程内的 `StatisticsService` 方法**实时重算并写入后返回，对前端透明，无需跨服务网络往返。

---

## 7. API 设计

统一前缀 `/api/v1`，返回 `{ code, msg, data }`。除登录外均需携带 JWT。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/auth/login` | 登录（校验 PG 用户/密码，签发 JWT） |
| GET | `/api/v1/users` | 用户列表（管理） |
| POST | `/api/v1/users` | 新建用户 |
| GET | `/api/v1/curve` | 原始曲线 `?busbar=0&start=&end=`（读 MySQL） |
| GET | `/api/v1/stats/daily` | 日统计 `?busbar=0&date=2026-07-31`（读 PG，缺则重算） |
| GET | `/api/v1/stats/daily/range` | 日统计区间（月历）`?busbar=0&from=&to=` |
| GET | `/api/v1/stats/monthly` | 月统计 `?busbar=0&year=2026&month=7` |
| GET | `/api/v1/stats/yearly` | 年统计 `?busbar=0&year=2026` |
| GET | `/api/v1/export/curve` | 导出原始曲线明细 xlsx `?busbar=0&start=&end=` |
| GET | `/api/v1/export/stats/daily` | 导出日统计 xlsx `?busbar=0&from=&to=` |
| GET | `/api/v1/export/stats/monthly` | 导出月统计 xlsx `?busbar=0&year=2026` |
| GET | `/api/v1/export/stats/yearly` | 导出年统计 xlsx `?busbar=0&year=2026` |
| POST | `/api/v1/stats/recompute` | 手动重算 `?from=&to=` |
| GET | `/api/v1/ingestion/status` | 预计算状态/游标 |

**Excel 导出说明**：
- 导出接口返回 xlsx 二进制流，前端以 blob 触发下载，响应头带 `Content-Disposition: attachment; filename=...`。
- 数据来源：曲线明细读外部 MySQL，统计读 PG；缺统计时先按需重算。
- 由 EasyExcel 流式写入；单次导出限区间/行数（如 ≤1 年、≤50 万行），超限提示缩小范围；后续如有需要可升级为异步生成+下载链接。
- 导出列：曲线明细（时间、母线、上限/下限/平均/计划电压）；日/月/年统计（时间维度、各时长、各率、极值电压）。

---

## 8. 前端规划（Vue 3 + Element Plus + ECharts，与 v1 一致）

### 8.1 页面

1. **登录** — 用户名/密码，获取 JWT 后存于前端（无状态）。
2. **电压曲线** — ECharts 折线图，叠加 `high_SV/low_SV` 上下限带，母线切换、时间范围选择。
3. **日统计** — 当日合格率/超限率卡片 + 24h 曲线 + 分钟明细表。
4. **月统计** — 月历热力图（按日合格率着色）+ 月度汇总卡片 + 日列表表格。
5. **年统计** — 12 个月柱状图 + 年度汇总卡片 + 月列表表格。
6. **系统管理**（可选） — 用户/角色/配置维护。

> 曲线页与日/月/年统计页均提供 **"导出 Excel"** 按钮，按当前筛选条件（母线、时间范围）调用 `/api/v1/export/*` 下载 xlsx；明细表区也支持就地导出当前表格。

### 8.2 目录（`frontend/vqms-web`）

```
vqms-web/
├── src/
│   ├── api/          # axios 封装（带 JWT），对接 /api/v1
│   ├── views/        # Login / Curve / Daily / Monthly / Yearly / Admin
│   ├── components/   # StatCard / RateGauge / BusbarSwitch
│   ├── router/  store/
│   └── App.vue  main.ts
├── Dockerfile        # 多阶段：node 构建 → nginx 托管
└── vite.config.ts
```

---

## 9. 目录结构（仓库整体）

```
VQMS/
├── docker-compose.yml          # 全栈编排（仅 3 个服务容器）
├── .env                        # 连接串/端口/外部 MySQL/JWT 密钥
├── 项目规划.md  项目规划_v2.md   # 规划文档（v1 微服务版 / v2 单体版）
├── CLAUDE.md  README.md  his_curve_sv.md  his_curve_tables.md
├── infra/
│   ├── postgres/init.sql       # 建表脚本：管理表 + 统计表 + 游标 + 初始管理员
│   └── nginx/nginx.conf        # 前端托管 + /api 反代到 backend
├── backend/                    # 单体 Spring Boot 应用（见第 4 节包结构）
│   └── src/main/java/com/vqms/...
└── frontend/
    └── vqms-web/               # Vue 3 SPA
```

> 相比 v1：去掉 `services/` 下 4 个独立微服务目录，改为单一 `backend/` 工程；仓库结构更扁平，`docker-compose.yml` 只需编排 `postgres` / `backend` / `nginx` 三个容器。

---

## 10. Docker Compose 部署

| 容器 | 镜像/构建 | 端口 | 依赖 |
|------|-----------|------|------|
| `postgres` | postgres:15 | 15432:5432 | — （管理 + 统计，唯一持久库） |
| `backend` | 本地构建（`backend/Dockerfile`） | 7000:7000 | postgres |
| `nginx` | nginx:stable | 8080:80 → 前端 + /api | backend |

关键配置点：
- **端口映射**（host:container）：`postgres` `15432:5432`、`backend` `7000:7000`、`nginx` `8080:80`。容器内沿用镜像默认监听端口（PG `5432`、Nginx `80`），仅 host 侧改端口避让冲突；`backend` 自身监听 `7000`，Nginx 反代目标为 `backend:7000`。
- `.env` 管理外部 MySQL 连接（`MYSQL_HOST=10.0.0.35` / 库 / 账号，**不要提交到 Git**，仓库中曾在 `tmp.md` 明文出现过一次账号密码，上线前需清理历史记录并轮换该密码）、PG 密码、JWT 密钥与过期时间。
- `backend` 通过 Docker `extra_hosts` 或 host 网络访问外部 AVC 盒子 MySQL（取决于网络可达性）。
- PG 使用 named volume 持久化；`infra/postgres/init.sql` 挂载到 `/docker-entrypoint-initdb.d/` 首次初始化（建管理表 + 统计表 + 游标 + 初始管理员账号）。
- Nginx 单一入口：`/` 托管前端 SPA，`/api/` 反代到 `backend:7000`。

相比 v1，容器数从 6 个（postgres + 4 微服务 + nginx）降到 3 个，`docker-compose up` 后联调路径也从"网关→服务发现→Feign"简化为"Nginx→单体后端"，故障定位更直接。

---

## 11. 开发里程碑

| 阶段 | 内容 | 产出 |
|------|------|------|
| **M1 基础设施** | docker-compose（3 容器）、PG 建表（管理+统计）、Nginx 骨架、后端工程骨架（含双数据源配置） | 容器可启、库表就绪 |
| **M2 统计核心 + 预计算调度** | `statistics` 模块：单分钟判定 + 日/月/年聚合算法；`ingestion` 模块：`@Scheduled` 定时读 MySQL→算→写 PG | PG 有预计算统计，算法可单测覆盖 |
| **M3 查询 API + 按需重算** | `web` 模块：曲线/统计查询接口，缺数据时进程内直接调用 `statistics` 重算 + 手动重算接口 | 查询不缺数 |
| **M4 鉴权 + 管理 + 导出** | JWT 登录/校验 + 用户/配置/日志管理（PG）+ EasyExcel 导出 | API + 登录可用 |
| **M5 前端看板** | Vue 登录/曲线/日/月/年 + 导出按钮 + 管理页 | Web 可用 |
| **M6 联调优化** | 端到端、边界用例（波动/越限数据）、日志监控 | 可交付 |

> 相比 v1 的 6 阶段，M2/M3 合并了原先分属两个微服务的统计逻辑，减少了"先写统计服务、再写调度服务、最后联调两者网络调用"的中间环节，预计能提前进入前端联调。

---

## 12. 待解决问题（与 v1 一致，未因架构调整而变化）

1. **`plan_SV` 解码**：源字段为原始遥测编码（`10245`），需 `ori_code` 映射表解码，当前该表为空。先按原值读取/展示，预留解码逻辑（读侧计算，无需落库），待映射表填充后启用。
2. **稳态数据验证**：现有样例 `high=low=avg`，统计恒为 100% 合格；待采集到含波动/越限的真实数据后校验算法分支。
3. **时区**：源 `save_time` 含毫秒但无时区，统一按 Asia/Shanghai 解释。
4. **JWT 无状态的吊销**：短有效期（如 2h）+ 滚动刷新；对内部工具而言足够，不引入黑名单机制。
5. **全量回填入口**：PG 统计虽持久，仍需提供"按日期范围全量重算"接口（首启、修正历史、`plan_SV` 解码启用后重建）。
6. **外部 MySQL 可用性**：AVC 盒子库不可达时——已计算统计仍可从 PG 正常返回；仅曲线查询与未计算时段受影响，返回明确错误。
7. **大范围导出性能**：全年曲线明细导出（~百万行）需流式写入 + 区间/行数上限；必要时改为异步生成后提供下载链接。
8. **凭据管理**：`tmp.md` 中曾明文出现数据库账号密码，实施前需清理 Git 历史中的敏感信息并轮换密码，正式配置统一走 `.env`（不入库）。

---

## 13. 与 v1（微服务版）对比小结

| 维度 | v1 微服务版 | v2 单体版（本文档） |
|------|------------|---------------------|
| 服务/容器数 | 4 微服务 + 网关 + PG + Nginx = 6 | 1 后端 + PG + Nginx = 3 |
| 统计逻辑复用方式 | Feign 远程调用 | 进程内接口调用 |
| 数据模型 / 统计算法 | — | 完全一致，未改动 |
| 适用场景 | 多站点接入、需要独立扩缩容/多团队并行 | 当前单站点低并发场景 |
| 演进路径 | 已是拆分终态 | 若未来规模增长，可从对应包（如 `statistics`）拆出为独立服务 |
