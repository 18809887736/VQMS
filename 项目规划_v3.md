# VQMS 项目规划 v3（基于 RuoYi-Vue 前后端分离底座）

> 运行环境: Linux · Docker + Docker Compose · MySQL/PostgreSQL + Redis · Spring Boot 3 单体(RuoYi-Vue) · Vue 3 Web
> 数据源: [his_curve_sv.md](his_curve_sv.md)（外部数据源 `10.0.0.9 / qheatavchisdb.his_curve_sv`，当前 MySQL 5.7，访问层按可替换设计）
> 底座: [RuoYi-Vue-springboot3](RuoYi-Vue-springboot3)（后端 v3.9.2 / SB 3.5.14）+ [RuoYi-Vue3](RuoYi-Vue3)（前端 Vue3+Element Plus+Vite）
> 创建日期: 2026-08-01
> 说明: 本版本在 [项目规划_v2.md](项目规划_v2.md) 基础上，**不再从零搭建工程**，而是引入若依（RuoYi-Vue 前后端分离版）作为应用底座，在其上叠加 VQMS 电压质量业务。领域模型、统计算法、存储职责划分与 v2 一致；变更的是工程载体与若干技术选型。

---

## 1. 项目概述

### 1.1 为什么有 v3（v2 → v3 的定位变化）

v2 规划"从零写一个 Spring Boot 单体"，鉴权、用户/角色/菜单、日志、定时任务、Excel 导出、前端后台壳子全部自建。这些是**每个管理系统都要重复造的轮子**，与"电压质量统计"这一核心价值无关。v3 直接用若依（RuoYi-Vue 前后端分离版）做底座，把这些通用能力全部复用，**把精力聚焦在 `source`(外部只读) / `statistics`(合格率算法) / `ingestion`(预计算调度) 三个业务包上**。

> ⚠️ 重要：若依有三个易混的仓库。本项目的底座是 **RuoYi-Vue（前后端分离版，Spring Security + JWT + Redis）**，**不是** `y_project/RuoYi` 单体版（Shiro + Thymeleaf，无 Vue）。前端用官方配套的 **RuoYi-Vue3**（Vue3 + Element Plus + Vite），与该后端原生配对。

### 1.2 v2 → v3 关键变更总览

| 维度 | v2（从零搭建） | v3（基于 RuoYi-Vue） | 影响 |
|------|---------------|----------------------|------|
| 鉴权 | 自建 JWT | **复用 RuoYi 的 Spring Security + JWT** | 一致，省工作量 |
| 缓存 | **无 Redis** | **引入 Redis**（RuoYi 硬依赖：token/验证码/限流/字典缓存） | ⚠️ 推翻 v2"无 Redis"决策；容器 3→4 |
| 管理库 | PostgreSQL（唯一持久库） | **见 §1.4 DB 选型**（推荐 MySQL，RuoYi 原生；或保留 PG） | ⚠️ 需拍板 |
| 工程结构 | `com.vqms` 自建 6 包 | **RuoYi 6 模块 + 新增 `ruoyi-vqms` 业务模块** | 升级友好 |
| 通用后台 | 自建 | **复用 RuoYi**（用户/角色/菜单/部门/字典/参数/日志/代码生成/Druid 监控/Swagger） | 大幅省工 |
| 定时任务 | 裸 `@Scheduled` | **复用 RuoYi `ruoyi-quartz`**（DB 存任务 + 页面管理） | 更强 |
| Excel 导出 | EasyExcel | **复用 RuoYi POI 工具**（`@Excel` 注解）；EasyExcel 可选替换 | 省工 |
| 前端壳 | 自建 | **复用 RuoYi-Vue3**（登录/布局/菜单/axios+JWT/用户中心） | 大幅省工 |
| 容器数 | 3（postgres/backend/nginx） | **4**（db + redis + backend + nginx） | +1 |
| 业务核心 | source/statistics/ingestion 自建 | **同样自建**（RuoYi 没有） | 不变 |

> **复用 vs 自建的边界**：若依解决"通用管理系统"，**解决不了"电压质量"**。`source`/`statistics`/`ingestion` 三个包 + 前端曲线/日/月/年看板，无论哪个版本都得自己写。

### 1.3 存储职责划分

| 存储 | 职责 | 存什么 |
|------|------|--------|
| **主库**（MySQL 或 PG，见 §1.4） | 管理库 + 派生统计（唯一持久库） | RuoYi 的 `sys_*`（用户/角色/菜单/字典/参数/日志）+ VQMS 的 `voltage_quality_*` 统计表 + 预计算游标 |
| **Redis** | 缓存/会话辅助（RuoYi 引入） | 登录 token、验证码、限流、字典/参数缓存 |
| **外部数据源**（`10.0.0.9`，现 MySQL 5.7） | 业务数据源（**只读**） | `his_curve_sv` 原始电压曲线 |

> **存储切分铁律（与 v2 一致）**：原始电压曲线**绝不复制进主库**；主库只存"可重算的派生统计"与管理数据。外部数据源是原始数据唯一真实来源。

### 1.4 ⚠️ 决策点：主库选 MySQL 还是 PostgreSQL

这是 v3 唯一需要你拍板的重大选型（v2 定的是 PostgreSQL）。两者都可行，差异在"省事"与"延续 v2":

| 选项 | 优点 | 代价 | 建议 |
|------|------|------|------|
| **A. MySQL（推荐）** | RuoYi 原生，零迁移；init 脚本/MyBatis XML/PageHelper/社区资料全按 MySQL；与外部源同引擎，运维单一 | 推翻 v2 的 PG 选型 | **新项目求快选这个** |
| **B. PostgreSQL** | 延续 v2；PG 在统计/窗口函数上更强 | 需迁移 RuoYi（方言、分页、`ry_*.sql` 建表脚本、少量 XML SQL）；社区有 PG fork 但会与官方版本漂移 | 想保持 v2 一致性才选 |

> 本文以下章节**以 MySQL 为主库**为默认描述（容器名 `mysql`、DDL 用 MySQL 语法）。若选 PG，把 `mysql` 容器换 `postgres:15`、DDL 改 PG 语法（见 v2 §5）、端口沿用 `15432:5432`，其余不变。**请在确认后告知，我会据此校正全文。**

### 1.5 源数据特征（关键约束，与 v2 完全一致，此处从略）

见 [项目规划_v2.md](项目规划_v2.md) §1.3。要点：`save_time` 是 `varchar(255)` 非 datetime（按 Asia/Shanghai 解释，异常行跳过）；源表无主键无索引（按 `(save_time, busbar_num)` 去重排序）；每分钟双写母线 0/1（按 `busbar_num` 分组）；`plan_SV` 是原始遥测编码（`ori_code` 表空，暂占位）；约 2,880 行/天。

---

## 2. 技术栈（实测自底座 pom.xml / package.json）

### 2.1 后端（RuoYi-Vue-springboot3）

| 层 | 选型 | 版本 |
|----|------|------|
| 语言/JDK | Java | 17 |
| 框架 | Spring Boot | 3.5.14 |
| 鉴权 | **Spring Security + JWT**（jjwt） | RuoYi 自带 |
| 缓存 | **Redis**（spring-boot-starter-data-redis） | RuoYi 自带 |
| ORM | MyBatis（mybatis-spring-boot-starter） | 3.0.5 |
| 分页 | PageHelper | 2.1.1 |
| 连接池 | Alibaba Druid | 1.2.28 |
| JSON | fastjson2 | 2.0.62（已是 v2，安全） |
| Excel | Apache POI | 4.1.2（偏旧，可升 5.x） |
| 代码生成 | Velocity | 2.3 |
| 定时任务 | Quartz（ruoyi-quartz 模块） | RuoYi 自带 |
| API 文档 | springdoc-openapi | 2.8.17 |
| 系统监控 | oshi-core | 7.3.0 |

### 2.2 前端（RuoYi-Vue3）

| 层 | 选型 | 版本 |
|----|------|------|
| 框架 | Vue | 3.5.26 |
| UI | Element Plus | 2.13.1 |
| 构建 | Vite | 6.4.1 |
| 语言 | JavaScript（非 TS） | — |
| 图表 | ECharts（VQMS 自行引入） | 待定 |
| HTTP | axios（封装带 JWT，前缀 `/dev-api`） | RuoYi 自带 |

---

## 3. 系统架构

```
                    ┌──────────────┐
                    │   浏览器      │  RuoYi-Vue3 (Vue3 + Element Plus + ECharts)
                    └──────┬───────┘
                           │ HTTP
                    ┌──────▼───────┐
                    │    Nginx     │  :8080  静态资源 + /api 反代
                    └──────┬───────┘
                           │ /dev-api/**
                    ┌──────▼──────────────────────────────────────┐
                    │   vqms-backend（RuoYi-Vue 单体 Spring Boot）  │  :7000
                    │  ┌────────────────────────────────────────┐  │
                    │  │ ruoyi-vqms  ★VQMS 新增业务模块          │  │
                    │  │   ├─ source/      外部数据源只读(可换方言)│  │
                    │  │   ├─ statistics/  合格率算法(日/月/年)    │  │
                    │  │   └─ ingestion/  Quartz 预计算任务       │  │
                    │  ├────────────────────────────────────────┤  │
                    │  │ ruoyi-framework  安全/缓存/多数据源/AOP   │  │  RuoYi 原生
                    │  │ ruoyi-system     用户/角色/菜单/字典/日志 │  │  （复用）
                    │  │ ruoyi-quartz     定时任务                │  │
                    │  │ ruoyi-generator  代码生成                │  │
                    │  │ ruoyi-common     工具/实体/异常           │  │
                    │  │ ruoyi-admin      启动+Controller         │  │
                    │  └────────────────────────────────────────┘  │
                    └──────┬──────────────┬──────────────┬─────────┘
                      只读  │          读写 │         读写  │
                    ┌───────▼──────┐  ┌────▼─────┐  ┌─────▼─────┐
                    │ 外部数据源    │  │  主库     │  │  Redis    │
                    │ 现MySQL5.7   │  │ MySQL/PG │  │ token/缓存│
                    │ 10.0.0.9     │  │ 管理+统计 │  │           │
                    │ his_curve_sv │  │ (唯一持久)│  │           │
                    └──────────────┘  └──────────┘  └───────────┘
```

**数据流**（与 v2 一致，仅承载在 RuoYi 之上）：
- **预计算**：Quartz 任务（`ruoyi-vqms/ingestion`）读外部数据源 → 调 `statistics` 算日/月/年 → UPSERT 进主库统计表。
- **查询**：前端 → Controller → 查主库统计表；行缺失则进程内调 `statistics` 重算后返回。
- **管理**：用户/配置/日志走 RuoYi 自带 Controller + 主库 `sys_*`。
- **鉴权**：RuoYi 登录签发 JWT，Spring Security Filter 校验。

---

## 4. RuoYi 提供 vs VQMS 自建（核心映射）

### 4.1 后端模块结构（RuoYi 原生 6 模块 + 新增 `ruoyi-vqms`）

```
RuoYi-Vue-springboot3/
├── ruoyi-admin/        # 启动类 + Controller（VQMS 的 Controller 也放这里的 com.ruoyi.web.controller.vqms）
├── ruoyi-framework/    # 安全/缓存/多数据源/AOP —— 直接复用，不改
├── ruoyi-system/       # 用户/角色/菜单/字典/日志 —— 直接复用 sys_* 表
├── ruoyi-quartz/       # 定时任务 —— VQMS 预计算任务挂这里
├── ruoyi-generator/    # 代码生成 —— 可用于生成统计表 CRUD
├── ruoyi-common/       # 工具/实体/异常 —— 直接复用
└── ruoyi-vqms/   ★新增 # VQMS 业务模块（com.ruoyi.vqms）
    ├── source/         # HisCurveSvReader 接口 + MySQL5.7 实现（可扩 MySQL8/SQLite/PG）
    ├── statistics/     # 单分钟判定 + 日/月/年加权聚合算法
    ├── ingestion/      # Quartz 预计算 Job（调 statistics → 写主库）
    └── domain|mapper/  # voltage_quality_* 实体 + Mapper
```

> **升级友好原则**：尽量不动 RuoYi 原生 6 个模块的文件；业务全集中在 `ruoyi-vqms`。将来若依发版，合并冲突最小。

### 4.2 复用 / 自建 清单

| 能力 | 来源 | 动作 |
|------|------|------|
| 登录 / JWT 签发校验 | RuoYi | 直接用 |
| 用户/角色/菜单/部门/字典/参数管理 | RuoYi `sys_*` | 直接用 |
| 操作日志 / 登录日志 | RuoYi | 直接用 |
| 定时任务调度（含页面） | RuoYi `ruoyi-quartz` | 注册 VQMS 的预计算 Job |
| Excel 导入导出 | RuoYi POI 工具 | 用 `@Excel`；或换 EasyExcel |
| 代码生成 / Druid 监控 / Swagger | RuoYi | 直接用 |
| 多数据源（`@DataSource` 注解路由） | RuoYi framework | **承载外部源只读 DataSource** |
| 前端登录/布局/菜单/用户中心 | RuoYi-Vue3 | 直接用 |
| **外部数据源只读访问 + 方言可替换** | **自建**（`source`） | 新建 |
| **电压合格率统计算法** | **自建**（`statistics`） | 新建 |
| **预计算调度逻辑** | **自建**（`ingestion`，挂 Quartz） | 新建 |
| **曲线/日/月/年看板（ECharts）** | **自建**（前端 `views/vqms/`） | 新建 |

---

## 5. 数据库设计

### 5.1 管理表（直接用 RuoYi 自带 `sys_*`）

RuoYi 的 `sql/` 目录已提供建表脚本（`ry_2025xxxx.sql` 等）：`sys_user / sys_role / sys_menu / sys_dept / sys_dict_* / sys_config / sys_oper_log / sys_logininfor / sys_job`(Quartz) / `sys_user_role` …。**VQMS 不重建这些表，直接复用**。初始管理员账号也由 RuoYi 脚本创建。

### 5.2 统计表（VQMS 新增，派生数据，DDL 同 v2 §5.2）

新增三张统计表 + 一张游标表（建在主库，独立于 `sys_*`）：

| 表 | 粒度 | 唯一键 |
|----|------|--------|
| `voltage_quality_daily` | 日 × 母线 | `(stat_date, busbar_num)` |
| `voltage_quality_monthly` | 月 × 母线 | `(stat_year, stat_month, busbar_num)` |
| `voltage_quality_yearly` | 年 × 母线 | `(stat_year, busbar_num)` |
| `precompute_cursor` | 母线 | `busbar_num` |

字段与 v2 §5.2 完全一致（total/qualified/over_high/over_low/over_limit _minutes、三个 rate、max/min/avg_SV、computed_at）。

> **DDL 按所选主库**：MySQL 用 `BIGINT AUTO_INCREMENT`、`DATETIME`、`DECIMAL(5,2)`、`INSERT...ON DUPLICATE KEY UPDATE`；PostgreSQL 用 `BIGSERIAL`、`TIMESTAMPTZ`、`NUMERIC`、`ON CONFLICT ... DO UPDATE`（完整 PG DDL 见 v2 §5.2）。完整建表脚本在落地阶段生成（可借 RuoYi 代码生成器产出雏形再改）。

### 5.3 外部数据源（只读，不复制，DB 可替换 —— 与 v2 §5.3 一致）

业务数据不迁入主库，始终直接读外部源库 `his_curve_sv`（结构见 [his_curve_sv.md](his_curve_sv.md)）。通过 `ruoyi-vqms/source` 的 `HisCurveSvReader` 接口接入，只读防误写。**DB 兼容性设计**：当前 MySQL 5.7（`10.0.0.9`），以后可切 MySQL 8 / SQLite / PostgreSQL——

- 接口（`HisCurveSvReader`）返回 JDBC 无关领域对象；每方言一个实现（现 `Mysql57CurveReader`），按 `source.type` 选 Bean。
- `save_time` 为 `varchar`、跨方言日期解析语法不同（`STR_TO_DATE`/`strftime`/`to_timestamp`），解析封在实现内或 SELECT 原始字符串在 Java 侧统一解析。
- 配置走 `.env` + `sys_config`：`source.type=mysql57|mysql8|sqlite|postgres`、`source.driver/url`、账号；切换只改配置 + 换 Bean。
- 只用通用 `SELECT ... WHERE save_time BETWEEN ? AND ? AND busbar_num=?`，无 DB 专有写法。
- **承载方式**：复用 RuoYi 的多数据源机制（`@DataSource(Slave)` 注解 + `AbstractRoutingDataSource`），把外部源配为只读 `slave` DataSource，与主库 `master` 隔离。

---

## 6. 电压质量统计算法（核心逻辑，与 v2 完全一致）

### 6.1 单分钟判定

对外部数据源 `his_curve_sv` 每条记录，用 `average_SV` 对照 `[low_SV, high_SV]`：

| 条件 | 分类 |
|------|------|
| `low_SV ≤ average_SV ≤ high_SV` | 合格 |
| `average_SV > high_SV` | 超上限 |
| `average_SV < low_SV` | 超下限 |

边界值（等于上下限）计为合格。

### 6.2 日统计聚合

```
total/qualified/over_high/over_low _minutes = 各自 COUNT
over_limit_minutes = over_high + over_low
qualification_rate = qualified / total * 100   （其余两率同理）
max_SV/min_SV/avg_SV = MAX/MIN/AVG(average_SV)
```

源数据按 `(save_time, busbar_num)` 去重排序；`save_time` 异常行记日志跳过。结果 UPSERT 进 `voltage_quality_daily`。

### 6.3 月 / 年聚合（逐级汇总，加权平均，**不可对 rate 直接取平均**）

月度由日表、年度由月表用 `INSERT...SELECT...GROUP BY` 汇总，**按分钟数加权**：

```
qualification_rate = SUM(qualified_minutes)/SUM(total_minutes)*100
over_high_rate     = SUM(over_high_minutes)/SUM(total_minutes)*100
over_low_rate      = SUM(over_low_minutes) /SUM(total_minutes)*100
avg_SV             = SUM(avg_SV * total_minutes)/SUM(total_minutes)
max_SV/min_SV      = MAX(max_SV)/MIN(min_SV)
over_limit_minutes = SUM(over_high_minutes)+SUM(over_low_minutes)
```

> ⚠️ 铁律：rate 列**绝不直接 AVG**；只能由分钟数重算。完整 SQL（PG 版见 v2 §6.3；MySQL 版用 `ON DUPLICATE KEY UPDATE`）。

### 6.4 调度与查询策略（挂 RuoYi Quartz）

| 任务 | 执行 | 频率 | 范围 |
|------|------|------|------|
| 当日日统计预热 | Quartz Job（`ingestion`） | 每小时 | 重算当天各母线 → daily |
| 历史日补算 | Quartz Job | 每晚 01:00 | 重算昨日 |
| 月统计汇总 | Quartz Job | 每晚 01:30 | 当月 → monthly |
| 年统计汇总 | Quartz Job | 每晚 02:00 | 当年 → yearly |
| 缺数据按需重算 | `StatsController` 进程内调 `statistics` | 按需 | 行缺失时算→写→返回 |
| 手动重算 | `/recompute` 接口 | 触发 | 按日期范围 |

> 用 RuoYi Quartz 比裸 `@Scheduled` 强：任务落库、可在页面启停/改 cron、有执行日志。

---

## 7. API 设计

API 前缀按环境区分（RuoYi-Vue3 脚手架既定约定，非本项目自定义），返回 `{ code, msg, data }`。除登录外均需 JWT。

| 环境 | 前缀 | 来源 |
|------|------|------|
| 本地开发（`npm run dev`） | `/dev-api` | `RuoYi-Vue3/.env.development`（`VITE_APP_BASE_API`），vite `server.proxy` 代理到后端 |
| 生产构建（`npm run build:prod`） | `/prod-api` | `RuoYi-Vue3/.env.production`，部署时由 Nginx 反代到后端（见 §9） |

前缀在网关层（vite dev server 或 Nginx）剥离后转发到 `backend:7000`，**后端 Controller 路径本身不含该前缀**。下表"路径"列均为后端真实路径（实际请求时由网关自动补环境前缀）。

**RuoYi 自带**（直接用）：`/login`、`/getInfo`、`/system/user|role|menu|dict|config|...`、`/monitor/*` 等。

**VQMS 新增**（放在 `com.ruoyi.web.controller.vqms`）：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/vqms/curve` | 原始曲线 `?busbar=0&start=&end=`（读外部源） |
| GET | `/vqms/stats/daily` | 日统计 `?busbar=0&date=`（读主库，缺则重算） |
| GET | `/vqms/stats/daily/range` | 日统计区间（月历） |
| GET | `/vqms/stats/monthly` | 月统计 |
| GET | `/vqms/stats/yearly` | 年统计 |
| GET | `/vqms/export/curve` | 导出原始曲线 xlsx |
| GET | `/vqms/export/stats/{daily|monthly|yearly}` | 导出统计 xlsx |
| POST | `/vqms/stats/recompute` | 手动重算 `?from=&to=` |
| GET | `/vqms/ingestion/status` | 预计算游标/状态 |

导出用 RuoYi 的 POI 工具（`@Excel` 注解 + `ExcelUtil`），单次限区间/行数。

---

## 8. 前端（RuoYi-Vue3 壳 + VQMS 页面）

### 8.1 页面

- **复用 RuoYi-Vue3**：登录、首页布局、菜单、用户/角色/菜单/字典/参数/日志管理。
- **VQMS 新增**（`src/views/vqms/`）：
  1. 电压曲线 —— ECharts 折线 + `high/low_SV` 上下限带，母线切换、时间范围。
  2. 日统计 —— 合格率/超限率卡片 + 24h 曲线 + 分钟明细表。
  3. 月统计 —— 月历热力图（按日合格率着色）+ 汇总卡片 + 日列表。
  4. 年统计 —— 12 月柱状图 + 汇总卡片 + 月列表。
- 各看板页提供"导出 Excel"按钮，调 `/dev-api/vqms/export/*`。

### 8.2 目录落点

```
RuoYi-Vue3/src/
├── api/vqms/        # VQMS 接口封装（curve.js, stats.js, export.js）
├── views/vqms/      # curve/index.vue, daily/, monthly/, yearly/
├── components/      # 复用 RuoYi 组件 + VQMS 的 StatCard/BusbarSwitch
└── router/          # 在 RuoYi 路由表里加 vqms 菜单（可走 RuoYi 菜单管理动态加）
```

---

## 9. Docker Compose 部署

| 容器 | 镜像/构建 | 端口(host:container) | 依赖 |
|------|-----------|----------------------|------|
| `mysql`（或 `postgres`） | mysql:8.4 / postgres:15 | 13306:3306 / 15432:5432 | — （管理+统计，唯一持久库） |
| `redis` | redis:7 | 16379:6379 | — （RuoYi 引入） |
| `backend` | 本地构建（RuoYi-Vue-springboot3/Dockerfile） | 7000:7000 | mysql, redis |
| `nginx` | nginx:stable | 8080:80 → 前端 + /dev-api | backend |

关键配置点：
- **端口映射**（host:container）：`mysql 13306:3306`、`redis 16379:6379`、`backend 7000:7000`、`nginx 8080:80`。容器内沿用镜像默认端口。
- `.env` 管理主库连接、Redis 地址、外部数据源（`source.type` + `SOURCE_HOST=10.0.0.9` / 库 / 账号）、JWT 密钥与过期；**不入库**（仓库 `tmp.md` 曾明文出现密码，上线前需清理历史并轮换）。
- `backend` 通过 `extra_hosts` 或 host 网络访问外部 AVC 盒子数据源（当前 MySQL 5.7 @ 10.0.0.9）。
- 主库用 named volume；RuoYi `sql/ry_*.sql` + VQMS 统计表脚本挂到 `/docker-entrypoint-initdb.d/` 首初始化。
- Nginx：`/` 托管前端 SPA（build 产物），`/prod-api/` 反代到 `backend:7000`（剥离前缀转发；生产前缀由 `RuoYi-Vue3/.env.production` 决定，见 §7）。

> 相比 v2（3 容器），v3 因引入 Redis 增至 **4 容器**。

---

## 10. 落地步骤（基于 RuoYi 改造 VQMS）

| 阶段 | 内容 | 产出 |
|------|------|------|
| **M0 底座就位** | 删后端 `ruoyi-ui`(Vue2)；后端配 MySQL+Redis 跑起来；前端 `npm i && npm run dev` 跑起来；登录通 | 若依原版可登录运行 |
| **M1 业务骨架** | 新建 `ruoyi-vqms` 模块；配外部源只读 DataSource（`@DataSource(Slave)`）；建 `voltage_quality_*` 表 | 双数据源通、统计表就绪 |
| **M2 统计核心** | `statistics`：单分钟判定 + 日/月/年加权聚合算法（单测覆盖） | 算法可跑、可测 |
| **M3 预计算调度** | `ingestion`：注册 Quartz Job（日预热/补算/月年汇总）+ 游标 | 主库有预计算统计 |
| **M4 查询+导出** | `web`：曲线/统计查询接口（缺数据重算）+ 手动重算 + Excel 导出 | API 可用 |
| **M5 前端看板** | `views/vqms/`：曲线/日/月/年 + ECharts + 导出按钮 + 菜单接入 | Web 可用 |
| **M6 联调优化** | 端到端、越限边界数据、日志监控、凭据清理 | 可交付 |

---

## 11. 待决问题与风险

1. **主库选型（§1.4）**：MySQL（推荐，RuoYi 原生）vs PostgreSQL（延续 v2）。**需拍板**，影响 DDL 与部署。
2. **Redis 已引入**：推翻 v2"无 Redis"；属 RuoYi 的既成代价，内部工具可接受。
3. **`ruoyi-ui` 清理**：后端自带 Vue2 前端目录，与 RuoYi-Vue3 重复，M0 删除。
4. **POI 4.1.2 偏旧**：可升 5.x；非阻塞。
5. **`plan_SV` 解码**：`ori_code` 表空，先按原值展示，预留读侧解码（同 v2）。
6. **稳态数据验证**：样例 `high=low=avg` 全合格，越限分支待真实波动数据（同 v2）。
7. **时区**：`save_time` 无时区，统一按 Asia/Shanghai（同 v2）。
8. **大范围导出**：全年明细导出需流式 + 区间/行数上限（同 v2）。
9. **凭据管理**：`tmp.md` 明文密码需清理 Git 历史并轮换；`.env` 不入库（同 v2）。
10. **若依升级路径**：业务集中在 `ruoyi-vqms`，原生模块尽量不改，便于跟随官方发版。

---

## 12. 与 v2 对比小结

| 维度 | v2（从零） | v3（基于 RuoYi-Vue） |
|------|-----------|---------------------|
| 鉴权/缓存 | JWT / 无 Redis | Spring Security+JWT / **+Redis** |
| 管理库 | PostgreSQL | MySQL(推荐) 或 PG |
| 通用后台 | 全自建 | **复用 RuoYi** |
| 业务核心 | source/statistics/ingestion 自建 | **同样自建**（不变） |
| 容器数 | 3 | 4（+Redis） |
| 上手速度 | 慢（要造轮子） | 快（聚焦业务） |
| 代码风格 | 自定 | 继承若依风格 |
