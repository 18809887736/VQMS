# VQMS 项目规划 v3.1（基于 RuoYi-Vue，主库确定为 MySQL）

> 运行环境: Linux · Docker + Docker Compose · **MySQL 8.4** + Redis · Spring Boot 3 单体(RuoYi-Vue) · Vue 3 Web
> 数据源: [his_curve_sv.md](his_curve_sv.md)（外部数据源 `10.0.0.9 / qheatavchisdb.his_curve_sv`，当前 MySQL 5.7，访问层按可替换设计）
> 底座: [RuoYi-Vue-springboot3](RuoYi-Vue-springboot3)（后端 v3.9.2 / SB 3.5.14）+ [RuoYi-Vue3](RuoYi-Vue3)（前端 Vue3+Element Plus+Vite）
> 创建日期: 2026-08-03
> 说明: 本版本在 [项目规划_v3.md](项目规划_v3.md) 基础上，**将 v3 唯一待决项"主库选型"确定为 MySQL（`mysql:8.4`）**。领域模型、统计算法、RuoYi 复用边界、存储切分铁律、API、前端、落地步骤均与 v3 一致；本文件只校正与"主库选型"相关的章节，并补全 v3 因待定而省略的 **MySQL 完整 DDL（§5.2）** 与 **MySQL 完整 rollup SQL（§6.3）**。

---

## 1. 项目概述

### 1.1 为什么有 v3.1（v3 → v3.1 的定位）

v3 把所有选型都收敛了，**唯一留了一个待拍板项**：主库选 MySQL 还是 PostgreSQL（v3 §1.4）。v3.1 将其**确定为 MySQL**。理由见 §1.4。其余一切沿用 v3——本文件存在的意义是：把 v3 里所有"二选一"的描述统一收敛为 MySQL，并把因待定而搁置的 MySQL 专属 DDL / 汇总 SQL 补全，使文档可直接作为落地依据，无需再回 v3 对照翻译。

> ⚠️ 与 v2（`项目规划_v2.md`，PostgreSQL）的关系：v3.1 选 MySQL **正式推翻 v2 的 PG 选型**。v2 的 PG DDL（`BIGSERIAL/NUMERIC/ON CONFLICT`）不再使用，以本文 §5.2 的 MySQL DDL 为准。领域算法、存储切分铁律（原始曲线不进主库）不变。

### 1.2 v3 → v3.1 变更总览（只列与 v3 的差异）

| 章节 | v3（待定） | v3.1（确定 MySQL） |
|------|-----------|--------------------|
| §1.3 存储职责 | "主库（MySQL 或 PG，见 §1.4）" | **主库 = MySQL 8.4** |
| §1.4 | 决策点：A.MySQL / B.PG 二选一 | **决策结论：选 MySQL，给出理由**（§1.4） |
| §3 架构图 | `MySQL/PG` | `MySQL 8.4` |
| §5.2 统计表 DDL | 仅描述"MySQL 用 X / PG 用 Y"，未给完整脚本 | **给出完整 MySQL CREATE TABLE**（4 张表） |
| §6.3 月/年 rollup SQL | 仅一句"MySQL 版用 ON DUPLICATE KEY UPDATE"，SQL 引 v2 PG 版 | **给出完整 MySQL `INSERT...SELECT...ON DUPLICATE KEY UPDATE`**（月度 + 年度） |
| §9 Docker | `mysql:8.4 / postgres:15`，`13306:3306 / 15432:5432` | **仅 MySQL：`mysql:8.4`、`13306:3306`** |
| §11 风险表 | 第 1 条"主库选型需拍板" | **移除该条**（已决） |
| §12 对比小结 | `MySQL(推荐) 或 PG` | `MySQL` |

> **未变化（与 v3 完全一致）**：§1.5 源数据特征、§2 技术栈、§4 RuoYi 复用清单、§5.1 管理表（RuoYi `sys_*`）、§5.3 外部数据源只读设计、§6.1/§6.2 单分钟判定与日聚合逻辑、§6.4 调度策略、§7 API、§8 前端、§10 落地步骤。这些与库无关或已由 RuoYi 决定，本文件不再重复，需要时查 [项目规划_v3.md](项目规划_v3.md)。

### 1.3 存储职责划分

| 存储 | 职责 | 存什么 |
|------|------|--------|
| **主库 MySQL 8.4** | 管理库 + 派生统计（唯一持久库） | RuoYi 的 `sys_*`（用户/角色/菜单/字典/参数/日志/Quartz 任务）+ VQMS 的 `voltage_quality_*` 统计表 + `precompute_cursor` 游标 |
| **Redis** | 缓存/会话辅助（RuoYi 引入） | 登录 token、验证码、限流、字典/参数缓存 |
| **外部数据源**（`10.0.0.9`，现 MySQL 5.7） | 业务数据源（**只读**） | `his_curve_sv` 原始电压曲线 |

> **存储切分铁律（不变）**：原始电压曲线**绝不复制进 MySQL 主库**；主库只存"可重算的派生统计"与管理数据。外部数据源是原始数据唯一真实来源。

### 1.4 主库选型结论：选 MySQL

| 角度 | 结论 |
|------|------|
| **RuoYi 原生** | 若依官方 `ry_*.sql` 建表脚本、MyBatis XML、PageHelper 分页、社区 99% 资料**全部按 MySQL**。选 PG 需迁移方言/分页/建表脚本，是"为了 PG 而迁 RuoYi"，得不偿失。 |
| **零迁移启动** | `mysql:8.4` 起来后直接挂 `ry_2025xxxx.sql` 到 `/docker-entrypoint-initdb.d/` 即可，M0 阶段当天就能登录跑通。 |
| **运维单一** | 外部数据源当前也是 MySQL（5.7 @ 10.0.0.9），主库用 MySQL 8.4，**同引擎同运维栈**，少一套诊断/备份工具链。 |
| **放弃 PG 的代价** | PG 在窗口函数/统计表达力上略强；但本项目的统计是"按分钟数加权重算 rate"（§6.3），纯 `SUM/COUNT` 即可，用不到 PG 专有能力。 |
| **推翻 v2 PG 选型** | v2 当初选 PG 是"自建工程"前提下的偏好；v3 引入 RuoYi 后，"RuoYi 原生"这一权重压倒了 PG 的边际优势。 |

> 选型已锁定，全文不再保留 PG 备选分支。

### 1.5 源数据特征

与 v3 §1.5 完全一致，此处从略。要点：`save_time` 是 `varchar(255)` 非 datetime（按 Asia/Shanghai 解释，异常行跳过）；源表无主键无索引（按 `(save_time, busbar_num)` 去重排序）；每分钟双写母线 0/1（按 `busbar_num` 分组）；`plan_SV` 是原始遥测编码（`ori_code` 表空，暂占位）。

---

## 2. 技术栈

与 v3 §2 完全一致。补充确认与主库相关的两项：

| 层 | 选型 | 版本 |
|----|------|------|
| 主库 | **MySQL**（InnoDB，utf8mb4） | **8.4**（容器 `mysql:8.4`） |
| ORM | MyBatis + PageHelper | 3.0.5 / 2.1.1（RuoYi 原生，MySQL 方言） |
| 连接池 | Alibaba Druid | 1.2.28（RuoYi 原生） |

> 后端其余（Spring Boot 3.5.14 / Java 17 / Spring Security+JWT / Redis / Quartz / fastjson2 / POI / springdoc）与前端（Vue 3.5.26 / Element Plus / Vite）均与 v3 §2 一致。

---

## 3. 系统架构

```
                    ┌──────────────┐
                    │   浏览器      │  RuoYi-Vue3 (Vue3 + Element Plus + ECharts)
                    └──────┬───────┘
                           │ HTTP
                    ┌──────▼───────┐
                    │    Nginx     │  :8080  静态资源 + /api 反代
                    └──────┬───────┘
                           │ /dev-api/**
                    ┌──────▼──────────────────────────────────────┐
                    │   vqms-backend（RuoYi-Vue 单体 Spring Boot）  │  :7000
                    │  ┌────────────────────────────────────────┐  │
                    │  │ ruoyi-vqms  ★VQMS 新增业务模块          │  │
                    │  │   ├─ source/      外部数据源只读(可换方言)│  │
                    │  │   ├─ statistics/  合格率算法(日/月/年)    │  │
                    │  │   └─ ingestion/  Quartz 预计算任务       │  │
                    │  ├────────────────────────────────────────┤  │
                    │  │ ruoyi-framework  安全/缓存/多数据源/AOP   │  │  RuoYi 原生
                    │  │ ruoyi-system     用户/角色/菜单/字典/日志 │  │  （复用）
                    │  │ ruoyi-quartz     定时任务                │  │
                    │  │ ruoyi-generator  代码生成                │  │
                    │  │ ruoyi-common     工具/实体/异常           │  │
                    │  │ ruoyi-admin      启动+Controller         │  │
                    │  └────────────────────────────────────────┘  │
                    └──────┬──────────────┬──────────────┬─────────┘
                      只读  │          读写 │         读写  │
                    ┌───────▼──────┐  ┌────▼──────┐  ┌─────▼─────┐
                    │ 外部数据源    │  │  MySQL     │  │  Redis    │
                    │ 现MySQL5.7   │  │  8.4       │  │ token/缓存│
                    │ 10.0.0.9     │  │ 管理+统计  │  │           │
                    │ his_curve_sv │  │ (唯一持久) │  │           │
                    └──────────────┘  └───────────┘  └───────────┘
```

数据流与 v3 一致：预计算（Quartz 读外部源 → statistics → UPSERT 进 MySQL）、查询（前端 → Controller → 查 MySQL，缺则进程内重算）、管理（RuoYi `sys_*`）、鉴权（RuoYi JWT）。

---

## 4. RuoYi 提供 vs VQMS 自建

与 v3 §4 完全一致（模块结构、复用/自建清单不变）。一句话：RuoYi 解决通用后台，VQMS 自建 `source`/`statistics`/`ingestion` 三个业务包 + 前端 `views/vqms/` 看板。

---

## 5. 数据库设计（MySQL 8.4 —— 唯一持久库）

### 5.1 管理表（直接用 RuoYi 自带 `sys_*`）

RuoYi 的 `sql/` 目录已提供 MySQL 建表脚本（`ry_2025xxxx.sql`、`ry_config_2025xxxx.sql` 等）：`sys_user / sys_role / sys_menu / sys_dept / sys_dict_type / sys_dict_data / sys_config / sys_oper_log / sys_logininfor / sys_job`(Quartz) / `sys_user_role / sys_role_menu` …。**VQMS 不重建这些表，直接复用**。初始管理员账号也由 RuoYi 脚本创建。本节无需自写 DDL。

### 5.2 统计表（VQMS 新增，派生数据，MySQL DDL）

字段与 v2 §5.2 / v3 §5.2 完全一致；这里给出**可直接执行的 MySQL 8.4 建表脚本**（替代 v2 的 PG 版）。完整脚本同步至 [`RuoYi-Vue-springboot3/sql/vqms.sql`](RuoYi-Vue-springboot3/sql/vqms.sql)，风格跟随 RuoYi（`drop table if exists` + `create table`、`engine=innodb`）；脚本**不含 `CREATE DATABASE`/`USE`**，跟随若依约定，库名由 docker `MYSQL_DATABASE` 决定。

```sql
-- 每日统计（粒度：日 × 母线；唯一键 stat_date + busbar_num）
CREATE TABLE voltage_quality_daily (
    id                  BIGINT        NOT NULL AUTO_INCREMENT PRIMARY KEY,
    stat_date           DATE          NOT NULL                  COMMENT '统计日期',
    busbar_num          SMALLINT      NOT NULL                  COMMENT '母线编号(0/1)',
    total_minutes       INT           DEFAULT NULL              COMMENT '总分钟数',
    qualified_minutes   INT           DEFAULT NULL              COMMENT '合格分钟数',
    over_high_minutes   INT           DEFAULT NULL              COMMENT '超上限分钟数',
    over_low_minutes    INT           DEFAULT NULL              COMMENT '超下限分钟数',
    over_limit_minutes  INT           DEFAULT NULL              COMMENT '超限分钟数(超上+超下)',
    qualification_rate  DECIMAL(5,2)  DEFAULT NULL              COMMENT '合格率(%)',
    over_high_rate      DECIMAL(5,2)  DEFAULT NULL              COMMENT '超上限率(%)',
    over_low_rate       DECIMAL(5,2)  DEFAULT NULL              COMMENT '超下限率(%)',
    max_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '当日最高电压',
    min_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '当日最低电压',
    avg_SV              DECIMAL(12,2) DEFAULT NULL              COMMENT '当日平均电压(按分钟数加权)',
    computed_at         DATETIME      DEFAULT CURRENT_TIMESTAMP COMMENT '本次计算时间',
    UNIQUE KEY uk_daily (stat_date, busbar_num),
    KEY idx_vqd_busbar_date (busbar_num, stat_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='电压质量-日统计';

-- 每月统计（粒度：月 × 母线；唯一键 stat_year + stat_month + busbar_num）
CREATE TABLE voltage_quality_monthly (
    id                  BIGINT        NOT NULL AUTO_INCREMENT PRIMARY KEY,
    stat_year           SMALLINT      NOT NULL                  COMMENT '年',
    stat_month          SMALLINT      NOT NULL                  COMMENT '月(1-12)',
    busbar_num          SMALLINT      NOT NULL                  COMMENT '母线编号(0/1)',
    total_minutes       INT           DEFAULT NULL              COMMENT '总分钟数',
    qualified_minutes   INT           DEFAULT NULL              COMMENT '合格分钟数',
    over_high_minutes   INT           DEFAULT NULL              COMMENT '超上限分钟数',
    over_low_minutes    INT           DEFAULT NULL              COMMENT '超下限分钟数',
    over_limit_minutes  INT           DEFAULT NULL              COMMENT '超限分钟数',
    qualification_rate  DECIMAL(5,2)  DEFAULT NULL              COMMENT '合格率(%)',
    over_high_rate      DECIMAL(5,2)  DEFAULT NULL              COMMENT '超上限率(%)',
    over_low_rate       DECIMAL(5,2)  DEFAULT NULL              COMMENT '超下限率(%)',
    max_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '月内最高电压',
    min_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '月内最低电压',
    avg_SV              DECIMAL(12,2) DEFAULT NULL              COMMENT '月平均电压(加权)',
    computed_at         DATETIME      DEFAULT CURRENT_TIMESTAMP COMMENT '本次计算时间',
    UNIQUE KEY uk_monthly (stat_year, stat_month, busbar_num),
    KEY idx_vqm_year_busbar (stat_year, busbar_num)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='电压质量-月统计';

-- 每年统计（粒度：年 × 母线；唯一键 stat_year + busbar_num）
CREATE TABLE voltage_quality_yearly (
    id                  BIGINT        NOT NULL AUTO_INCREMENT PRIMARY KEY,
    stat_year           SMALLINT      NOT NULL                  COMMENT '年',
    busbar_num          SMALLINT      NOT NULL                  COMMENT '母线编号(0/1)',
    total_minutes       INT           DEFAULT NULL              COMMENT '总分钟数',
    qualified_minutes   INT           DEFAULT NULL              COMMENT '合格分钟数',
    over_high_minutes   INT           DEFAULT NULL              COMMENT '超上限分钟数',
    over_low_minutes    INT           DEFAULT NULL              COMMENT '超下限分钟数',
    over_limit_minutes  INT           DEFAULT NULL              COMMENT '超限分钟数',
    qualification_rate  DECIMAL(5,2)  DEFAULT NULL              COMMENT '合格率(%)',
    over_high_rate      DECIMAL(5,2)  DEFAULT NULL              COMMENT '超上限率(%)',
    over_low_rate       DECIMAL(5,2)  DEFAULT NULL              COMMENT '超下限率(%)',
    max_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '年内最高电压',
    min_SV              DECIMAL(10,0) DEFAULT NULL              COMMENT '年内最低电压',
    avg_SV              DECIMAL(12,2) DEFAULT NULL              COMMENT '年平均电压(加权)',
    computed_at         DATETIME      DEFAULT CURRENT_TIMESTAMP COMMENT '本次计算时间',
    UNIQUE KEY uk_yearly (stat_year, busbar_num)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='电压质量-年统计';

-- 预计算游标（各母线最后已计算日期，断点续算/全量回填用）
CREATE TABLE precompute_cursor (
    busbar_num  SMALLINT  NOT NULL PRIMARY KEY              COMMENT '母线编号(0/1)',
    last_date   DATE      DEFAULT NULL                      COMMENT '最后已计算日期',
    updated_at  DATETIME  DEFAULT CURRENT_TIMESTAMP
                                  ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预计算游标';
```

要点：
- 字段类型对照 v2 PG 版：`BIGSERIAL`→`BIGINT AUTO_INCREMENT`、`SMALLINT/INTEGER`→`SMALLINT/INT`、`NUMERIC(p,s)`→`DECIMAL(p,s)`、`TIMESTAMPTZ DEFAULT now()`→`DATETIME DEFAULT CURRENT_TIMESTAMP`。语义等价。
- `utf8mb4`（非 utf8mb3）以备中文注释/字典；`InnoDB` 支持事务与外键（RuoYi 要求）。
- 业务唯一键用 `UNIQUE KEY`，承载 `INSERT ... ON DUPLICATE KEY UPDATE` 的 UPSERT 语义（替代 PG 的 `ON CONFLICT ... DO UPDATE`）。
- 可借 RuoYi 代码生成器对这 4 张表生成实体/Mapper 雏形再改。

### 5.3 外部数据源（只读，不复制，DB 可替换 —— 与 v3 §5.3 一致）

业务数据不迁入主库，始终直接读外部源库 `his_curve_sv`（结构见 [his_curve_sv.md](his_curve_sv.md)）。通过 `ruoyi-vqms/source` 的 `HisCurveSvReader` 接口接入，只读防误写。**DB 兼容性设计**（与主库选型无关——这是"外部源"的可替换性，不要混淆）：当前 MySQL 5.7（`10.0.0.9`），以后可切 MySQL 8 / SQLite / PostgreSQL——

- 接口（`HisCurveSvReader`）返回 JDBC 无关领域对象；每方言一个实现（现 `Mysql57CurveReader`），按 `source.type` 选 Bean。
- `save_time` 为 `varchar`、跨方言日期解析语法不同（`STR_TO_DATE`/`strftime`/`to_timestamp`），解析封在实现内或 SELECT 原始字符串在 Java 侧统一解析。
- 配置走 `.env` + `sys_config`：`source.type=mysql57|mysql8|sqlite|postgres`、`source.driver/url`、账号；切换只改配置 + 换 Bean。
- 只用通用 `SELECT ... WHERE save_time BETWEEN ? AND ? AND busbar_num=?`，无 DB 专有写法。
- **承载方式**：复用 RuoYi 的多数据源机制（`@DataSource(value=DataSourceType.SLAVE)` 注解 + `AbstractRoutingDataSource`），把外部源配为只读 `slave` DataSource，与主库 `master` 隔离。

> 注意区分：**主库 MySQL 是确定的**（§1.4）；**外部源 DB 是可替换的**（本节）。两者是不同存储、不同设计目标，勿混。

---

## 6. 电压质量统计算法

§6.1（单分钟判定）、§6.2（日聚合逻辑）、§6.4（调度策略）与 v3 完全一致，此处从略。下面只补全 v3 搁置的 **MySQL 版月/年 rollup SQL**。

### 6.1 / 6.2 / 6.4

见 [项目规划_v3.md](项目规划_v3.md) §6.1 / §6.2 / §6.4。要点不变：单分钟按 `average_SV` 对 `[low_SV, high_SV]` 判定（边界算合格）；日聚合 `COUNT/MAX/MIN/AVG`，结果 UPSERT 进 `voltage_quality_daily`；调度挂 RuoYi Quartz。

### 6.3 月 / 年聚合（MySQL 完整 SQL，加权平均，不可对 rate 直接取平均）

> ⚠️ 铁律（不变）：rate 列**绝不直接 AVG**；只能由分钟数重算。`avg_SV` 按 `total_minutes` 加权。MySQL 的 `/` 对整数也返回 DECIMAL，不会截断；`NULLIF(...,0)` 保证全天无数据时 rate 落为 NULL 而非除零。

```sql
-- 月度汇总：由 voltage_quality_daily → voltage_quality_monthly
-- :month_start / :next_month_start 为 yyyy-MM-dd 边界（左闭右开）
INSERT INTO voltage_quality_monthly (
    stat_year, stat_month, busbar_num,
    total_minutes, qualified_minutes, over_high_minutes, over_low_minutes, over_limit_minutes,
    qualification_rate, over_high_rate, over_low_rate, max_SV, min_SV, avg_SV)
SELECT YEAR(stat_date), MONTH(stat_date), busbar_num,
       SUM(total_minutes),
       SUM(qualified_minutes),
       SUM(over_high_minutes),
       SUM(over_low_minutes),
       SUM(over_high_minutes) + SUM(over_low_minutes),
       SUM(qualified_minutes) / NULLIF(SUM(total_minutes), 0) * 100,
       SUM(over_high_minutes) / NULLIF(SUM(total_minutes), 0) * 100,
       SUM(over_low_minutes)  / NULLIF(SUM(total_minutes), 0) * 100,
       MAX(max_SV),
       MIN(min_SV),
       SUM(avg_SV * total_minutes) / NULLIF(SUM(total_minutes), 0)
FROM voltage_quality_daily
WHERE stat_date >= :month_start AND stat_date < :next_month_start
GROUP BY YEAR(stat_date), MONTH(stat_date), busbar_num
ON DUPLICATE KEY UPDATE
    total_minutes      = VALUES(total_minutes),
    qualified_minutes  = VALUES(qualified_minutes),
    over_high_minutes  = VALUES(over_high_minutes),
    over_low_minutes   = VALUES(over_low_minutes),
    over_limit_minutes = VALUES(over_limit_minutes),
    qualification_rate = VALUES(qualification_rate),
    over_high_rate     = VALUES(over_high_rate),
    over_low_rate      = VALUES(over_low_rate),
    max_SV             = VALUES(max_SV),
    min_SV             = VALUES(min_SV),
    avg_SV             = VALUES(avg_SV),
    computed_at        = CURRENT_TIMESTAMP;

-- 年度汇总：由 voltage_quality_monthly → voltage_quality_yearly
-- :year_start / :next_year_start 为 yyyy-01-01 边界（左闭右开）
INSERT INTO voltage_quality_yearly (
    stat_year, busbar_num,
    total_minutes, qualified_minutes, over_high_minutes, over_low_minutes, over_limit_minutes,
    qualification_rate, over_high_rate, over_low_rate, max_SV, min_SV, avg_SV)
SELECT stat_year, busbar_num,
       SUM(total_minutes),
       SUM(qualified_minutes),
       SUM(over_high_minutes),
       SUM(over_low_minutes),
       SUM(over_high_minutes) + SUM(over_low_minutes),
       SUM(qualified_minutes) / NULLIF(SUM(total_minutes), 0) * 100,
       SUM(over_high_minutes) / NULLIF(SUM(total_minutes), 0) * 100,
       SUM(over_low_minutes)  / NULLIF(SUM(total_minutes), 0) * 100,
       MAX(max_SV),
       MIN(min_SV),
       SUM(avg_SV * total_minutes) / NULLIF(SUM(total_minutes), 0)
FROM voltage_quality_monthly
WHERE stat_year = :year
GROUP BY stat_year, busbar_num
ON DUPLICATE KEY UPDATE
    total_minutes      = VALUES(total_minutes),
    qualified_minutes  = VALUES(qualified_minutes),
    over_high_minutes  = VALUES(over_high_minutes),
    over_low_minutes   = VALUES(over_low_minutes),
    over_limit_minutes = VALUES(over_limit_minutes),
    qualification_rate = VALUES(qualification_rate),
    over_high_rate     = VALUES(over_high_rate),
    over_low_rate      = VALUES(over_low_rate),
    max_SV             = VALUES(max_SV),
    min_SV             = VALUES(min_SV),
    avg_SV             = VALUES(avg_SV),
    computed_at        = CURRENT_TIMESTAMP;
```

> 关于 `VALUES(col)`：MySQL 8.0.20 起将 `VALUES()` 函数标为 deprecated（仍可用）。为与 RuoYi 现有 SQL 风格一致、可读性优先，本规划沿用经典写法。若未来升至 MySQL 9.x 需移除，可改为 `INSERT INTO ... AS new ... ON DUPLICATE KEY UPDATE total_minutes = new.total_minutes, ...` 别名语法。

---

## 7. API 设计

与 v3 §7 基本一致（`{code,msg,data}`、RuoYi 自带接口直接用、VQMS 新增业务接口）。**唯一订正**：API 前缀按环境区分，非单一 `/dev-api`（这是 RuoYi-Vue3 脚手架的既定约定，非本项目自定义）：

| 环境 | 前缀 | 来源 |
|------|------|------|
| 本地开发（`npm run dev`） | `/dev-api` | `.env.development`，vite `server.proxy` 代理到后端 |
| 生产构建（`npm run build:prod`） | `/prod-api` | `.env.production`，部署时由 Nginx 反代到后端（见 §9 / `RuoYi-Vue3/nginx.conf`） |

两者均在网关层（vite dev server 或 Nginx）剥离前缀后转发到 `backend:7000`，**后端 Controller 路径本身不含该前缀**（如 `com.ruoyi.web.controller.vqms` 下均是 `/vqms/curve`、`/vqms/stats/daily` 等，不写 `-api`）。v3 §7 接口表中列出的 `/vqms/*` 即后端真实路径，实际请求时由网关自动补上表中的环境前缀。

---

## 8. 前端

与 v3 §8 完全一致（RuoYi-Vue3 壳 + `src/views/vqms/` 四个看板 + ECharts + 导出按钮）。略。

---

## 9. Docker Compose 部署

| 容器 | 镜像/构建 | 端口(host:container) | 依赖 |
|------|-----------|----------------------|------|
| `mysql` | mysql:8.4 | 13306:3306 | — （管理+统计，唯一持久库） |
| `redis` | redis:7 | 16379:6379 | — （RuoYi 引入） |
| `backend` | 本地构建（RuoYi-Vue-springboot3/Dockerfile） | 7000:7000 | mysql, redis |
| `nginx` | nginx:stable | 8080:80 → 前端 + /dev-api | backend |

关键配置点：
- **端口映射**（host:container）：`mysql 13306:3306`、`redis 16379:6379`、`backend 7000:7000`、`nginx 8080:80`。容器内沿用镜像默认端口。
- **MySQL 配置**：`MYSQL_DATABASE`/`MYSQL_ROOT_PASSWORD`/字符集 `utf8mb4` 走 `.env`；`command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci`；主库用 named volume 持久化 `/var/lib/mysql`；`ry_*.sql` + `quartz.sql` + `vqms.sql` 挂到 `/docker-entrypoint-initdb.d/` 首初始化。
- **数据库账号分离（最小权限）**：`MYSQL_ROOT_PASSWORD` 仅用于首次初始化（建库建表 + 建应用账号 + 授权）；后端运行时用**专属应用账号**（如 `vqms_app`，凭据走 `.env` 的 `DB_USER` / `DB_PASSWORD`）。init 脚本末尾追加：`CREATE USER 'vqms_app'@'%' IDENTIFIED BY '...'; GRANT SELECT,INSERT,UPDATE,DELETE ON ry_vqms.* TO 'vqms_app'@'%'; FLUSH PRIVILEGES;` —— 不授 DDL / GRANT / 跨库权限。RuoYi `application.yml` 的主库 datasource 指向该应用账号，**不填 root**。（本项目 `tmp.md` 曾明文泄露 root 密码，root 扩散面务必收窄。）
- `.env` 管理主库连接、Redis 地址、外部数据源（`source.type=mysql57` + `SOURCE_HOST=10.0.0.9` / 库 / 账号）、JWT 密钥与过期；**不入库**（仓库 `tmp.md` 曾明文出现密码，上线前需清理历史并轮换）。
- `backend` 通过 `extra_hosts` 或 host 网络访问外部 AVC 盒子数据源（当前 MySQL 5.7 @ 10.0.0.9）。
- Nginx：`/` 托管前端 SPA（build 产物），`/prod-api/` 反代到 `backend:7000`（剥离前缀转发；生产前缀由 `RuoYi-Vue3/.env.production` 的 `/prod-api` 决定，见 §7）。

> 相比 v2（3 容器 / postgres），v3.1 因引入 Redis 增至 4 容器，且 db 由 postgres 换为 mysql。

---

## 10. 落地步骤

与 v3 §10 完全一致（M0 底座 → M1 业务骨架 → M2 统计核心 → M3 预计算 → M4 查询导出 → M5 前端 → M6 联调）。仅一处因主库确定而简化：**M0 后端配 MySQL（非 PG）+ Redis**，`ry_*.sql` 直接挂载，无需方言迁移。略。

---

## 11. 待决问题与风险

（相对 v3 §11，**移除原第 1 条"主库选型需拍板"**——已决为 MySQL。其余风险项不变：）

1. ~~主库选型：MySQL vs PG~~ → **已定 MySQL 8.4**（本文件）。
2. **Redis 已引入**：推翻 v2"无 Redis"；属 RuoYi 的既成代价，内部工具可接受。
3. **`ruoyi-ui` 清理**：后端自带 Vue2 前端目录，与 RuoYi-Vue3 重复，M0 删除。
4. **POI 4.1.2 偏旧**：可升 5.x；非阻塞。
5. **`plan_SV` 解码**：`ori_code` 表空，先按原值展示，预留读侧解码（同 v2/v3）。
6. **稳态数据验证**：样例 `high=low=avg` 全合格，越限分支待真实波动数据（同 v2/v3）。
7. **时区**：`save_time` 无时区，统一按 Asia/Shanghai（同 v2/v3）。
8. **大范围导出**：全年明细导出需流式 + 区间/行数上限（同 v2/v3）。
9. **凭据管理**：`tmp.md` 明文密码需清理 Git 历史并轮换；`.env` 不入库（同 v2/v3）。
10. **若依升级路径**：业务集中在 `ruoyi-vqms`，原生模块尽量不改，便于跟随官方发版。
11. **`VALUES(col)` deprecated**（v3.1 新增）：见 §6.3 末注；升 MySQL 9.x 时需切别名语法。

---

## 12. 与 v2 / v3 对比小结

| 维度 | v2（从零） | v3（基于 RuoYi） | v3.1（本文件） |
|------|-----------|-----------------|----------------|
| 鉴权/缓存 | JWT / 无 Redis | Spring Security+JWT / +Redis | 同 v3 |
| **管理库** | **PostgreSQL** | **MySQL(推荐) 或 PG（待定）** | **MySQL 8.4（确定）** |
| 通用后台 | 全自建 | 复用 RuoYi | 同 v3 |
| 业务核心 | source/statistics/ingestion 自建 | 同样自建（不变） | 同样自建（不变） |
| 容器数 | 3（postgres/backend/nginx） | 4（db+redis+backend+nginx） | 4（**mysql**+redis+backend+nginx） |
| DDL/rollup SQL | PG 版（v2 §5.2/§6.3） | 待定（按所选库） | **MySQL 完整版（本文件 §5.2/§6.3）** |
| 上手速度 | 慢 | 快 | 快（且主库零迁移） |

---

> **下一步**：以本文件为落地依据。M0 阶段起，主库一律按 MySQL 8.4 执行——`ry_*.sql` + `quartz.sql` + `vqms.sql` 挂 `/docker-entrypoint-initdb.d/`；预计算 rollup 用 §6.3 的 `ON DUPLICATE KEY UPDATE` SQL。[CLAUDE.md](CLAUDE.md) 已同步至 v3.1/MySQL（authoritative 指向本文件）。
